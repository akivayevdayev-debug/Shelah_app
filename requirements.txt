# Sync status (2026-08-16): security-audit dependency bumps — PyJWT,
# cryptography, fastapi/starlette pins moved forward to close known CVEs
# (see docs archive / commit history for the audit that drove this pass).
# All four verified against the full pytest suite before pinning.
# Sync status (2026-08-17, plan.md §8.C.4 pip-audit pass): starlette bumped
# 1.0.1 -> 1.3.1, the minimal version closing PYSEC-2026-248/249/2280/2281
# (GHSA-jp82-jpqv-5vv3, GHSA-82w8-qh3p-5jfq, GHSA-wqp7-x3pw-xc5r,
# GHSA-x746-7m8f-x49c) -- see docs/SECURITY.md. Deliberately not jumped to
# latest (1.6.0): smallest bump that closes the known CVEs, re-verified
# against the full pytest suite before pinning, consistent with this file's
# existing minimal-bump precedent above.
flask==3.1.3
fastapi==0.141.1
starlette==1.3.1
uvicorn==0.35.0
# redis: backend/rate_limit.py's Upstash-backed shared store (plan.md §16.3-L2
# / §16.1 D3). Replaces Flask-Limiter, which used to sit on app.py alongside
# a second, independent in-process limiter in asgi.py -- both removed
# (plan.md §16.8.1). Pinned to the version already resolved in this
# environment; redis.asyncio (used here) has shipped in-package since 4.2.
redis==7.4.0
python-dotenv==1.2.2
requests==2.33.1
httpx==0.28.1
pyluach==2.3.0
# tzdata: explicit fallback tz database for stdlib zoneinfo (backend/
# zmanim_engine.py) -- zoneinfo prefers system tzdata but Vercel's
# serverless runtime isn't guaranteed to ship it, so this package (which
# pytz used to provide implicitly, bundled) keeps timezone resolution
# correct regardless of the host's OS-level tzdata (SonarCloud python:S6890
# / security audit SonarCloud backlog: don't use pytz on Python 3.9+).
tzdata==2026.2
timezonefinder==8.2.2
zmanim==0.3.1
anthropic==0.89.0
google-genai==2.6.0
tenacity==9.1.2
PyJWT==2.13.0
cryptography==50.0.0
# supabase 2.28.3 -> 2.31.0 (2026-08-21, Python 3.14 migration): required
# bump, not optional. storage3 2.28.3 (pulled in transitively) hard-requires
# pyiceberg>=0.10.0, which ships no cp314 wheel on PyPI as of this date --
# blocks the hash-locked, --only-binary=:all: install under Python 3.14.
# storage3 2.31.0 (pulled in by supabase 2.31.0) makes pyiceberg an opt-in
# extra ("iceberg") instead, so it drops out of the resolved tree entirely --
# this app doesn't use Iceberg catalog features. Verified: full lock
# resolves with wheel coverage for all packages on cp314-manylinux2014, and
# the pytest suite passes against the bumped version.
supabase==2.31.0
python-docx==1.1.2
reportlab==4.2.2
sentry-sdk==2.63.0
