# Development and testing dependencies — not installed in production/Vercel
pytest==8.4.0
pytest-cov==6.2.0
pytest-asyncio==0.25.3
httpx==0.28.1
respx==0.22.0
responses==0.25.7
freezegun==1.5.1
ruff==0.11.13
# CI tooling (ci.yml "Security scan" / "Dependency vulnerability scan" steps).
# Listed here, and so hash-locked in requirements-dev.lock.txt, instead of
# being `pip install`ed unpinned inside the workflow (SonarCloud
# githubactions:S8544 / S8541 on those two steps).
pre-commit==4.6.2
pip-audit==2.10.1
