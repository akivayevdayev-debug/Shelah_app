# Claude Code Prompts — one per plan.md section

Copy-paste each prompt into Claude Code **one at a time, in order within each track**. Every prompt assumes Claude Code is run from the repo root with `plan.md` present. Backend prompts (1–4) are strictly sequential; other tracks can run independently.

Universal rules baked into every prompt: golden-master tests before moves, move-verbatim not rewrite, back-compat shims, no new import cycles, reconcile §2 duplicates, `pytest -q` green before done, `graphify update .` after code changes, stop when the prompt's scope is complete — do not continue into the next section.

---

## Status — verified against the repo 2026-07-31, with audit layers added 2026-08-19 and 2026-08-26

> **⚠️ 2026-08-19 — read this before running Prompts 29a/29b/30.** A full architectural audit (`DECISIONS.md`, compiled by deep-reading current code) produced five new `plan.md` sections — **§20** (cost-ledger integrity), **§21** (RLS verification), **§22** (`ask_pipeline` decision), **§23** (schema/doc provenance), **§24** (sweep) — plus **§16.8**, which reconciles §16's four-defect analysis against what has actually shipped since it was written. New Prompts **33a/33b/33c, 34, 35, 36, 37** cover them. Three consequences for prompts that already existed:
>
> - **Prompt 29a is partly obsolete.** Its D2 and D4 steps are already done in the code; running them as written risks *regressing* a fix. See the correction banner on that prompt. **✅ Now fully done 2026-08-22, and confirmed actually committed 2026-08-26 (`a519a21`) — see its row below.**
> - ~~**Prompt 29b is blocked on Prompt 33a.** Its cost breaker reads `ai_usage_log.cost_usd`, which is \$0.00 for most production rows today.~~ **✅ 33a shipped 2026-08-19, 29b shipped 2026-08-22 — see its row below and Prompt 45/§33.**
> - **Prompt 30 is superseded by Prompt 35**, which makes the recommendation Prompt 30 deliberately withheld. **Prompt 35's own recommendation was then re-scoped 2026-08-21 (Prompt 40 STEP 3 / `plan.md` §27.3) — see the correction banners on both Prompt 30 and Prompt 35, and their rows below. ✅ Executed 2026-08-26, sixth pass.**
>
> **⚠️ 2026-08-26 — table extended through Prompt 50, three stale rows corrected.** This table previously stopped at Prompt 39 even though Prompts 40–50 had long existed below in the prompt bodies and in the "Suggested execution order" table — added here so the two tables agree. Three existing rows were also corrected for drift: **29a** (row said "partial," code has been fully shipped and committed since 2026-08-22), **31** (row said "not yet executed," but Option 2 — deferral — *is* the executed decision; nothing further was ever pending), and **32** (row's blocking condition on 31 is now resolved). See each row for detail.

Re-verify before running anything; this snapshot ages fast. Prompts 1–11 were re-audited end-to-end on 2026-07-31 (backend items via direct grep/read; frontend items 5/8/9/10/11 via five independent read-only investigation passes, each required to cite `file:line` for every claim). Full evidence trail for that pass lives in `plan.md` §18. Numbers/percentages below are current as of that date and will drift.

| Prompt | Section | Status | Evidence |
|---|---|---|---|
| 1 | §4 Phase 1 — text_engine | ✅ **Done** | `backend/utils/text_engine.py` (242 L) has every listed symbol, zero duplicate `HEBREW_DIACRITICS_RE` outside it, `helpers.py` re-imports the canonical copy, `tests/test_text_engine.py`, imported by `app.py` |
| 2 | §4 Phase 2 — search_provider | ✅ **Done** | `backend/utils/search_provider.py` (926 L) has every listed symbol, zero duplicate `HEBREW_WORD_GLOSSARY`/translate-fn copies outside it, `tests/test_search_provider.py`, imported by `app.py` |
| 3 | §4 Phase 3 — circuit breakers | ✅ **Done** | `is_healthy`/`record_success`/`record_failure` wired for `sefaria`/`web`/`translate_google`/`translate_mymemory` in `search_provider.py`; every `requests.*` call there carries an explicit `timeout=` and a narrow `except (requests.RequestException, TimeoutError)`. One local-file (`json.loads`) bare `except Exception:` at `search_provider.py:560` — not a network call, outside this phase's literal scope, but worth narrowing in a future pass. `hebcal` calls (`zmanim_engine.py`/`calendar_service.py`/`search.py`) are outside `search_provider.py` and still have **no** circuit-breaker wiring at all — a real gap, but never in Phase 3's stated scope ("external calls now in `backend/utils/search_provider.py`"), so not a Phase-3 failure, just a pre-existing hole worth a follow-up. |
| 4 | §4 Phase 4 — streamline app.py | ✅ **Done** | `app.py` is **1,999 lines** (was 5,023 pre-refactor). Zero leftover migrated blocks (`grep` for every Phase 1/2 symbol name at `app.py` top-level returns nothing) — only re-export shims remain, and per §18.3, those shims are now themselves dead weight nothing outside `app.py` consumes; flagged for a future cleanup prompt rather than fixed here. |
| 4b | §4 Phase 5 — concurrency | ✅ **Done** (re-confirmed 2026-07-31) | `claude-loop-bridge` executor in `backend/claude.py`; `TTLCache` (thread-safe, internal lock) + writer-lock/reader-lock-free swap pattern in `backend/sefaria_library.py`; `tests/test_loop_bridge.py` + `tests/test_sefaria_library_concurrency.py` green |
| 5 | §7a AI source-box fix | ✅ **Done** (completed 2026-08-01) | Both `mirrorAiSourcesInto()` and the main `populateAiModal()` render now use one delegated `click` listener per container (`event.target.closest('a.source-local-link')`) instead of a per-anchor loop; dead `.ai-source-card` CSS deleted; the duplicated `.ai-source-box` stagger-delay rule consolidated into `loading.css`'s canonical `.animate-fade-up` definition; `#aiSources` and the skeleton region both carry `role="status"`/`aria-busy`, cleared on every fetch exit path (success/error/network-failure). |
| 6 | §7b observability | ✅ **Done** | Confirmed via this session's Prompt 6b STEP 0 reconciliation — request-ID contextvar propagation, Sentry init, cost-meter call-site coverage (incl. both previously-uninstrumented sync sites) all present. |
| **6b** | **§7b-frontend — browser Sentry + `/api/client-errors` retirement** | ✅ **Done** (completed 2026-07-31) | Browser SDK loaded from Sentry's CDN with a computed (not memorized) SRI hash, true no-op when `SENTRY_DSN_BROWSER` unset (byte-identical page, test-verified); question/answer text scrubbed from breadcrumbs/events (dedicated test asserts it, caught and fixed 2 real leaks during development); noise filtering + dedupe/throttle; `/api/client-errors` now same-origin-gated, `stack` capped at 2,000 chars, client IP never forwarded; CSP updated with Sentry hosts; `docs/OBSERVABILITY.md` "Sentry Project Setup" section added. 1255 pytest + 14 Node tests green. **Not verified** (no live environment available): zero CSP console violations in an actual browser, and a forced error actually arriving at Sentry in a preview deploy — both require a deployed environment with real DSNs. |
| 7 | §7c docs pass | ✅ **Done** (completed 2026-08-01) | `README.md` now cross-links `docs/SERVICE_ARCHITECTURE.md`, `docs/API.md`, `docs/OBSERVABILITY.md`, `docs/FRONTEND.md`, `docs/DATABASE.md`, `docs/DEVELOPER_NOTES.md` in a new "Further Documentation" section. The 6 stale docs (`MANIFEST.md`, `INTEGRATION_GUIDE.md`, `QUICK_START.md`, `SOURCES_REGISTRY.md`, `CONNECTION_QUICK_REFERENCE.md`, `WORD_LIMIT_FEATURE.md`) moved to `docs/archive/` with one-line redirect stubs left at the original paths, matching the existing archive pattern. |
| 8 | §7d motion overhaul | ✅ **Done** (completed 2026-08-01) | motion.dev (Motion One) now loads via a CDN `<script>` with a computed-and-cross-verified SRI hash (`motion@12.43.0/dist/motion.js`), pinned before `static/js/motion.js` — `window.Motion` is populated in production. ~48 hardcoded transition durations across `style.css`/`ai.css`/`sidebar.css`/`calendar.css` migrated onto `--motion-dur-*`/`--motion-ease-*` tokens (new `--motion-ease-decel` token added for a curve repeated 5+ times with no existing match); `transition: all` sites replaced with named properties. Found and fixed a genuine WCAG 2.3.3 bug: `sidebar.css`'s `#loadingOverlay .font-hebrew` used `!important` on an animation that outranked the global reduced-motion fallback on specificity — added a scoped reduced-motion override. |
| 9 | §7e dark mode overhaul | ✅ **Done as scoped — AA contrast fixes landed 2026-08-15** | 7 of 8 feature sheets migrated: `ai.css` (31→13 occurrences via genuine auto-flip-token collapses, not just value tokenization — 2 new local-token pairs added for badge colors + skeleton shimmer), `reader.css` (16→14), `sidebar.css` (12→11, plus 2 real bugs fixed: dead `--text-primary`/`--navy`-style token references silently falling back to hardcoded hex), `calendar.css` (11→10), `halacha.css`/`prayer.css`/`typography.css` confirmed already fully token-based (0 changes needed). **`style.css` (484 of the ~582 total occurrences) intentionally left as-is** — verified its dark-mode rules are NOT raw-hex debt (a scripted redundancy analysis found only 17 trivially-duplicate declarations out of ~962 selector/property pairs); the real reason the count can't drop is that `style.css`'s LIGHT-mode rules mostly use one-off hardcoded hex rather than the token system, so there's no way to prove most dark overrides are redundant without inventing ~400+ new tokens with no browser to visually verify them against — user chose to leave this alone rather than risk unverified regressions. **AA contrast audit findings resolved 2026-08-15** (`docs/ACCESSIBILITY_AUDIT.md`): call-site audit found `--ink-secondary` (light theme) has real small-text consumers (calendar Hebrew-date label, reader subtitle, segment badges) that needed the full 4.5:1 threshold — darkened `#64748b`→`#5a6b85`, now 5.06:1/4.63:1 with margin, visually verified in-browser both themes. `--accent-gold`'s 1.97:1 light-mode fail has zero live text consumers (confirmed by grep) — no code fix needed, added a warning comment at the token definition instead. `--ink-on-accent`'s 3.12:1 dark-mode fail also has zero consumers anywhere in the repo — left as-is, flagged in the audit doc. |
| 10 | §7f loading-states overhaul | ✅ **Done** (completed 2026-08-01) | `#prayerBooksNavList` (3 `.sk-tree-row` skeletons), `#popularTextsGrid` (4 card skeletons matching the real card shape), and `#selectionInsightsHint`/`#selectionInsightsBody` (its existing `.sk-reader-para` skeleton kept, static ARIA baseline added) all now carry `role="status"`/`aria-live="polite"`/`aria-busy`, cleared on every JS exit path (success/empty/error). Rebuilt `shelah-shimmer` in `tokens.css` as a `translateX`-driven `::after`-overlay animation (transform-only, per ENGINEERING_RULES.md) instead of animating `background-position` directly — required restructuring `loading.css`'s shared skeleton base rule and discovering `ai.css`'s own `.ai-src-skeleton-header`/`-line` background rules were already 100% shadowed by `loading.css`'s later-loading equal-specificity rule (cleaned up as dead code rather than left duplicated). Deleted the dead `--ai-loading-color` token (from `ai.css` **and** its two orphaned copies in `tokens.css` — confirmed zero consumers anywhere) and the orphaned `#aiSynthesis p.animate-pulse` rule. |
| 11 | §7g frontend platform fixes | ✅ **Done as scoped — CSP nonce deliberately deferred, see Prompt 31 (decided 2026-08-15)** | Item 1 (Tailwind build step) — done, unchanged. Item 2 (CSP): `Strict-Transport-Security` header added (`max-age=63072000; includeSubDomains; preload`); the stale comment blaming the Tailwind CDN JIT compiler corrected — that blocker was already resolved. The nonce/`'unsafe-inline'` removal part of this item is **resolved as Option 2 (defer)** — confirmed with the user 2026-08-15: `templates/index.html`'s 112 inline `onclick=` handlers and 43 inline `style=` attributes make the full refactor large, high-risk, multi-session work (CSP2+ browsers drop `'unsafe-inline'` entirely once a nonce is present, so a partial change breaks every handler); CSP stays as-is until/unless `index.html`'s markup is refactored for other reasons first. See **Prompt 31** for the full writeup. Item 3 (SRI): unchanged — Google Fonts' CSS endpoint still has no `integrity` (confirmed not fixable: Google serves different `@font-face` payloads per User-Agent, so no single static hash is valid — documented in Prompt 31). Item 4 (fonts): `terms.html`/`privacy.html`/`accessibility.html` now have `rel="preconnect"` for `fonts.googleapis.com`/`fonts.gstatic.com`, matching `index.html`. Item 5 (SEO/RTL): all three legal pages now have OG/Twitter-card/canonical metadata matching `index.html`'s pattern; `toggleLanguage()` in `components/legal_scripts.html` now sets `html.lang` alongside `dir` (was previously dir-only, producing mismatched `lang="en" dir="rtl"` after switching to Hebrew — confirmed fixed). Server-side dynamic lang/dir via Jinja (stretch goal) evaluated, not implemented: feasible (no Clerk/session dependency needed — the preference is anonymous/per-browser) but requires a genuine small feature addition (mirror the preference into a cookie, read it in all 4 routes, template the `<html>` tag) rather than a bug fix — left as a future prompt candidate. |
| 12 | §8.A legal docs | ✅ **Done as attorney-review-gated scaffolding** (completed 2026-08-16) | `terms.html` rewritten to 20 numbered sections covering every §8.A.1 clause (definitions, 13+/16+ eligibility, user-content license, indemnification, assumption of risk, dispute-resolution/arbitration placeholder, "AS IS"/damages-cap, entire agreement) — the prior "§12 missing, numbering jumps 11→13" gap was already closed by earlier uncommitted work and is now moot under the new 20-section structure. `privacy.html` expanded per §8.A.2: GDPR Art. 6 legal-bases table, full sub-processor list (added Hebcal/Sefaria/MyMemory/Sentry to the existing Clerk/Supabase/Vercel/Google/Anthropic), international-transfer mechanism, Do-Not-Sell/Share + GPC honoring, EU rep/DPO determination placeholder, explicit breach-notification commitment (GDPR Art. 33 72h), 13+/16+-EU children's-privacy nuance. New `templates/ai-disclosure.html`, `acceptable-use.html`, `dmca.html`, `licenses.html` served by a new `backend/routes_legal.py` blueprint (registered in `app.py`'s `_BLUEPRINTS`, keeping new routes out of `app.py` per `.agents/ENGINEERING_RULES.md`); every legal page's footer now cross-links all 7 pages (test-enforced, see below). `THIRD_PARTY_LICENSES.md`'s stale `§6` cross-reference fixed to `§9` to match the renumbered terms.html. Click-through consent is now versioned end-to-end: `LEGAL_TERMS_VERSION`/`LEGAL_PRIVACY_VERSION` constants in `app.py` flow into the consent modal's localStorage key (`templates/index.html`) — so bumping either constant re-shows the modal for every user — and into `accept_legal()`'s newly-persisted `legal_terms_version`/`legal_privacy_version` columns (additive migration `scripts/migrate_user_preferences_legal_version.sql`, alongside the pre-existing `legal_accepted`/`age_attested` columns). Every doc carries a "⚠️ DRAFT — pending attorney review" banner and explicit `[Placeholder]` markers on the clauses that are genuinely undecided (damages-cap dollar figure, arbitration/venue choice, transfer-mechanism confirmation, EU rep/DPO determination, DMCA agent registration) per the original "draft ALL legal documents as attorney-review scaffolding" instruction — none of this is a substitute for the attorney review §8's preamble requires as a hard launch gate. New `tests/test_routes_legal.py` (route 200s + cross-link parametrized test over all 7 pages) plus 2 new `accept_legal` version-storage tests in `test_routes_user.py`. Full pytest green (1,000+ tests) except one pre-existing, unrelated local-environment failure — `TestSentryBrowserIntegration::test_unset_dsn_emits_no_sentry_script_tag` fails only because this machine's real `.env` has a live `SENTRY_DSN_BROWSER` value (confirmed via `git stash` to a clean checkout: fails identically there too, and passes when the var is blanked); not a code defect, not caused by this prompt, and CI has no local `.env` to trigger it. **Follow-up (same day, 2026-08-16):** at the user's request the checkbox-gated modal described above was replaced with a permanent, non-blocking footer (`.site-footer` in `templates/index.html`) — plain browsewrap notice text ("by using this site you agree to the following…") linking all 7 legal docs, no checkboxes, no Accept/Decline gate. `/api/accept-legal` and the version-tracking columns still exist and are still tested, but the footer no longer calls them — they're dormant, ready for a real clickwrap moment (e.g. at Clerk sign-up) if the attorney review or later product decision calls for one. `templates/terms.html`/`privacy.html` prose describing the old click-through/checkbox mechanism was corrected to match. Separately, all 7 legal pages got a shared `static/css/legal.css` (replacing ~250 duplicated inline `<style>` lines per page), an auto-built sticky table of contents, 16px+ body type, and a fix for a real cross-page dark/light-mode bug (`legal_scripts.html` was reading/writing a different localStorage key than `templates/index.html` and never set the `data-theme` attribute tokens.css keys off, so a theme choice on one page silently didn't apply on the other, and an explicit "light" choice could lose to OS dark mode). |
| 13 | §8.B AI safety | ✅ **Done** (re-verified and gaps closed 2026-08-16) | The 2026-07-31 snapshot below was true for items 2–6 but item 1 was never actually wired end-to-end — re-verification found `safety_class`/`rabbinic_disclaimer` were computed and stored server-side but **never present on the `/ask` JSON response**, so the frontend had no way to render item 1's "persistent, unavoidable disclaimer" or B-AGE.7's "referral variant" — `grep` for `safety_class`/`rabbinic_disclaimer`/`renderDisclaimerBanner` in `static/js/`/`templates/index.html` returned zero hits before this pass. Fixed: `meta.safety_class`/`meta.rabbinic_disclaimer` now returned on every `/ask` response shape in both `app.py` (`_run_ask_question_ai_synthesis`, `_security_blocked_ask_payload`, `_ask_question_strict_payload`, `_run_ask_question_fallback`) and `asgi.py` (`_run_ask_async_ai_synthesis`, `_ask_async_strict_block`, `_run_ask_async_fallback`); `templates/index.html` gained a persistent, non-dismissible `#aiDisclaimerBanner`/`#readerDisclaimerBanner` (shared `.ai-disclaimer-banner` class, `renderDisclaimerBanner()`), rendered on every answer in both the AI modal and reader view, with a visually distinct prominent variant (icon + amber tokens, not color alone) for the `medical`/`mental_health_or_self_harm`/`abuse_or_minor_safety`/`dangerous_or_illegal` classes — verified in-browser both themes (light/dark), WCAG AA contrast computed and checked (`--ai-warning-*` tokens in `tokens.css` previously had no light-mode value at all, so were dead code — completed the pair). Two further divergent-duplication bugs (plan.md §2 anti-pattern) found and fixed while wiring this: (a) `_security_blocked_ask_payload` never called `_store_ask_history` at all, so item 6's defensibility logging silently skipped exactly the highest-liability interactions (medical/self-harm/abuse referrals, domain refusals) on the Flask path — now logged with `safety_class`/`prompt_version`; (b) the async `_store_ask_history` call in `asgi.py` omitted `safety_class`/`prompt_version` entirely (silently defaulting), and the sync `run_protected_ai_wrapper`'s domain-block branch never built a `structured` payload at all (async's did) so `dangerous_or_illegal` domain refusals on the Flask path always reported `safety_class: "ok"` — consolidated both entrypoints onto one shared `claude._build_input_block_result()` helper rather than patching the sync copy separately. `classify_safety()` (`claude.py:845`), `SAFETY_REFERRAL_CLASSES`/referral text (`claude.py:263-319`), `_build_source_attribution_note` (item 5, `backend/helpers.py:818`), and the single `NO_IMPERSONATION_DIRECTIVE` source of truth (item 3 — confirmed no other system-prompt constant exists repo-wide) were all re-verified correct, unchanged. New/extended tests: `tests/test_ask.py::TestSafetyClassMetaPropagation` (meta parity across Flask+FastAPI for ok/medical/domain-refusal, `_store_ask_history` wiring regression for both transports), `tests/test_rag.py::TestStoreAskHistory` (safety_class/prompt_version payload assertions). Full suite green (1339 passed, 3 pre-existing xfailed); verified live in-browser (medical + self-harm queries, light+dark, AI modal + reader view, zero new console errors). |
| 14 | §8.B-AGE | ✅ **Done** (verified 2026-07-31) | `AGE_APPROPRIATE_DIRECTIVE` (`claude.py:991-993`) + `NO_IMPERSONATION_DIRECTIVE` (`claude.py:997-999`), wired into `CORE_SYSTEM_PROMPT`/`SIMPLE_SYSTEM_PROMPT`; see `docs/AGE_AND_SAFETY_POLICY.md` |
| 15 | §8.C security hardening | ✅ **Done** (completed 2026-08-17) | Full findings report in `docs/SECURITY.md` (also serves as §8.C.7's deliverable). **Critical, action-required finding:** a live Google/Gemini API key was found committed in `test_results.txt` across 3 commits (2026-05-16 to 2026-06-17) and still tracked in the working tree — removed from the current tree + `.gitignore`d in this pass, but **still live in git history** until the repo owner rotates the key (not done here, per the prompt's own "flag for rotation — do not rotate yourself") and decides whether to rewrite history (destructive, needs explicit go-ahead — not done here). Full `gitleaks detect` history scan (174 commits) found nothing else real. Concrete fixes shipped: `starlette` bumped `1.0.1`→`1.3.1` (closes 4 CVEs found by a fresh `pip-audit` run against the actual CI lock files, not just a local env) with full pytest re-verified green; three input-validation gaps fixed with regression tests (`routes_devtools.py` segment-report crash-to-500 on non-string JSON, `sefaria_library.py::get_category_contents` and `routes_calendar.py::get_holidays` both missing URL-encoding/bounds on attacker-controlled values interpolated into outbound third-party URLs); `/api/devtools/rls-audit` was missing `ask_history` from its reported tables (has its own RLS policy in `scripts/migrate_ask_history.sql`, just wasn't surfaced) — fixed + regression-tested, plus a new regression test pinning `STRICT_SUPABASE_RLS` to `True`. CI (`ci.yml`) gained `pip-audit` (both lock files, non-blocking) and `pre-commit run --all-files` (bandit+gitleaks, closes the local-only/`--no-verify`-bypassable gap) on every push. Four parallel read-only audits (input validation across every route file, SRI completeness across all 7 legal pages + index.html, deep AuthN/Z review of `_verify_clerk_token`/session policy/IDOR, a repo-wide hardcoded-credential sweep) came back otherwise clean — RLS enforcement, CSP headers (nonce-based hardening remains deliberately deferred, see Prompt 31 — not reopened), SRI, and JWT verification were all already solid from prior prompts. One config-dependent item flagged for the repo owner to verify (not code-changed): confirm `CLERK_AUDIENCE` is actually set in production Vercel env, since its absence silently skips JWT audience verification. Full pytest green (1300+ tests, 91% coverage) after every change; `graphify update .` run. §8.D–H (Prompts 16–19) remain untouched by this pass. **⚠️ Reopened in part 2026-08-19 — see Prompt 34 / `plan.md` §21.** §8.C.2 (Supabase RLS) was marked done on the premise that RLS functions. That premise is **unverified**: `auth.uid()` only resolves a Clerk JWT if Supabase is configured to trust Clerk as a third-party issuer — a dashboard setting captured nowhere in this repo. If unset, RLS silently returns zero rows on all **four** production call sites of `_get_user_scoped_supabase_client()` (`routes_user.py:227,299`; `rag.py:308,405` — the `rag.py` pair has no user-visible symptom). `routes_privacy.py:13-16` says in its own docstring that the user-scoped client "would 403 for exactly the users this feature exists to serve," which reads as evidence it does **not** resolve. Also outstanding from this prompt's own findings: devtools diagnostic routes are gated only by "is a valid Clerk user" — there is no admin/role distinction anywhere in the auth model, so any signed-up user can read deployment config. |
| 16 | §8.D privacy operations | ✅ **Done** (polish landed 2026-08-20) | DSR export/delete flow, consent versioning, DPA checklist, GDPR Art. 30 RoPA table, retention-enforce cron, breach response plan, and DPIA were already implemented (`docs/PRIVACY_OPERATIONS.md`, `docs/DPIA.md`) — re-confirmed accurate against current repo state, including that the retention cron is correctly registered in `vercel.json` (`"0 14 * * *"`) against the real route. This pass closed the one real remaining gap named in the prior snapshot below: `_delete_clerk_user()` (`backend/routes_privacy.py`) now calls `_capture_backend_error("clerk_account_delete_skipped_no_secret_key", ...)` when `CLERK_SECRET_KEY` is unset, so a partially-completed DSR deletion (Supabase rows gone, Clerk identity retained) now reaches Sentry/structured logs instead of being visible only in a `clerk_error` HTTP response field nobody watches in production. Response shape/status codes unchanged (still 207 + `clerk_error`); new regression test `TestDeleteClerkUser::test_missing_secret_key_reaches_capture_backend_error` in `tests/test_routes_privacy.py` (30 tests in that file, all green). Still an explicit repo-owner action, not engineering: `CRON_SECRET` ships empty in `.env.example:102`, so whether the deployed retention cron actually authenticates is unverified from the repo alone (→ Prompt 37); DSR endpoints still deliberately bypass RLS via the service-role client (→ Prompt 34). Full pytest green. |
| 17 | §8.E reliability & observability | ✅ **Done as scoped — one real gap found and left open, see evidence** | `docs/RUNBOOKS.md` gained six sections previously stubbed "still to be written": uptime monitoring (5-min static-asset primary check + `/api/health`/`/api/stack/health` secondary, with the honest caveat that both are `@require_clerk_auth`-gated — `routes_devtools.py:58,254` — so an anonymous external monitor must either treat `401` as healthy or point at the unauthenticated `/api/devtools/heartbeat` instead), backups & recovery (Supabase backup/PITR tier explicitly left for the operator to confirm in-dashboard, not asserted; `scripts/migrate_customs_to_supabase.py` verified safely re-runnable via deterministic sha256 row IDs + upsert, but that is re-seedability, not a tested restore), incident response (cross-references `docs/PRIVACY_OPERATIONS.md` §6 rather than duplicating it), rollback procedure (Vercel dashboard Promote-to-Production as the lead path — no `.vercel/` link in this repo and CLI availability isn't guaranteed), deploy checklist, and an honest rate-limiting/cost-ceiling status section citing §16.8's known `memory://`/two-independent-limiters gap rather than re-litigating it. Separately, closed a real Phase-3 circuit-breaker gap flagged since Prompt 3's own status row: `health.is_healthy('hebcal')`/`record_success`/`record_failure` (already registered in `backend/health_check.py`'s `_PROBES` but never called anywhere, confirmed by a repo-wide grep before this pass) are now wired into all four previously-unguarded hebcal call sites — `backend/zmanim_engine.py` (`_get_hebcal_day_times()`, `get_monthly_events()`), `backend/search.py` (`get_daily_learning()`), `backend/calendar_service.py` (`PyluachEngine.get_parasha()`), `backend/routes_calendar.py` (`/api/holidays`) — matching the file's own pre-existing `nominatim` pattern (`routes_calendar.py:55-85`), with 16 new circuit-breaker tests and zero change to any site's existing fallback output. Added a genuine circuit-breaker-*state*-driven `/ask` degradation test (`tests/test_ask.py::TestAskDegradationPath`, both transports) — the prior test coverage only simulated a raised exception, never actual open-circuit state. **One gap surfaced and deliberately left open**, out of this pass's file scope: `health.is_healthy('claude')`/`('gemini')` is never consulted anywhere before the *primary* `/ask` AI call (`backend/claude.py`/`app.py`/`asgi.py`) — only the fallback stage (`get_halakhic_sources()`, gated on `sefaria`/`web`) is circuit-breaker-driven, so the primary stage can still only degrade via a raised exception, not circuit state. **→ Prompt 39.** Full pytest green, 91.43% coverage. |
| 18 | §8.F quality, accessibility & content integrity | ✅ **Done as scoped — one coverage hole in the new a11y gate, see evidence** | `pytest.ini`'s `--cov-fail-under` ratcheted `60`→`85` against a measured 91.39–91.43% actual (RLS-audit tests already run in every offline suite pass by construction; `pip-audit` already runs in CI as a deliberately-non-blocking advisory step per the existing decision recorded around `plan.md` line 1968 — left unchanged, not flipped to blocking). New `docs/CONTENT_QA.md`: confirms by repo-wide grep that no rabbinic-advisor/hechsher endorsement exists, documents the real customs-corpus pipeline (`backend/customs.py`'s schema-only `validate_all_customs_at_startup()` — required-fields + valid-JSON check, not a halakhic-accuracy check; `scripts/migrate_customs_to_supabase.py`'s deterministic-upsert ETL; `scripts/crawl_library_leaves.py`'s Sefaria-link-health-only check), and states plainly that no human reviews individual AI-synthesized answers, cross-referencing rather than contradicting `docs/DPIA.md` §2. New automated WCAG2AA gate: `pa11y-ci` + `.pa11yci.json` + a CI "Accessibility scan" step in `.github/workflows/ci.yml` against all 8 legal/public pages — actually executed locally against a live instance this session (8/8 pages, 0 violations), not just wired and assumed; also fixed a real `.gitignore` bug found in the same work (the blanket `.*` rule was silently excluding `.pa11yci.json` from git, which would have 404'd the CI step on a fresh checkout — fixed with an explicit `!.pa11yci.json` exception). Localization: added a machine-translation label (`machine_translated` field + bilingual UI badge) to the previously-unlabeled `/api/word/meaning` translation surface, matching `_build_source_attribution_note`'s existing labeling convention; confirmed dynamic lang/dir (Prompt 11, client-side-only by design) is unaffected. **One real gap:** the new `pa11y-ci` CI gate only ever renders/checks each page's default *light* theme — headless Chrome's `prefers-color-scheme` plus this app's own localStorage theme-detection script both resolve to light in the CI browser, so dark theme is never exercised by this automated gate (the existing manual `docs/ACCESSIBILITY_AUDIT.md` contrast pass does cover dark tokens, so this isn't a fully unaudited hole, but the two mechanisms don't overlap). **→ Prompt 39.** Full pytest green; CI YAML confirmed valid. |
| 19 | §8.G+§8.H business scaffolding & launch gate | ✅ **Done — documentation only, as scoped; one stale-plan-wording finding fixed inline** | New `docs/LAUNCH_CHECKLIST.md` reproduces plan.md §8.H's 9-line checklist verbatim, each line linked to real evidence with an honestly done/partial/not-started status re-derived directly from `plan.md` + this table (not copied from a summary) — plus a "Business & compliance — needs human counsel" section covering all 5 `plan.md` §8.G items in plain language, explicitly not legal/business advice. Cross-linked from `README.md`'s Further Documentation table (8 new rows: SECURITY/PRIVACY_OPERATIONS/DPIA/RUNBOOKS/AGE_AND_SAFETY_POLICY/LAUNCH_CHECKLIST/ACCESSIBILITY_AUDIT/CONTENT_QA) and one cross-link sentence each added to `docs/SECURITY.md` and `docs/PRIVACY_OPERATIONS.md`. **Two findings surfaced by this pass:** (a) `plan.md` §8.H's own checklist line 3 wording ("age gate ... enforced at sign-up + stored with consent") no longer matched the actual 2026-08-16 product decision (a non-blocking browsewrap notice with no persisted attestation, per `docs/AGE_AND_SAFETY_POLICY.md`) — **fixed inline in this same pass**, see `plan.md` §8.H's corrected line 3; (b) `docs/SECURITY.md` §2's RLS section still reads as more settled than the `plan.md` §21 finding (RLS may silently return zero rows rather than deny cross-user access) — left as Prompt 34's job, not duplicated here. Full pytest green (this track touched no code). |
| 20 | §9 agentic tools | ✅ **Done as scoped** (completed 2026-08-21) | `backend/ai_tools.py` (new, 983 L) — all 22 tools from plan.md §9.2/§9.2b (`search_judaic_texts` through `format_source_citation`), each a frozen `ToolSpec` (JSON schema + async handler + optional `health_check` service + timeout + `last_resort` flag), wrapping existing backend functions per-tool. `execute_tool()` is the single fail-open boundary: timeout, narrow exception catch, `health.record_success`/`record_failure`, never raises — a handler bug degrades to `{"error": ...}`, not a crashed loop. `backend/ask_pipeline.py` gained `run_agentic_ask()` (new, ~200 L) — the orchestration loop (tool_use → parallel `asyncio.gather` execute → tool_result → repeat, 4-round hard cap, final round omits `tools` to force text) — alongside the pre-existing, still-untouched `run_ask_pipeline`/`AskPipelineResult` (see the ask_pipeline.py tension noted below). `backend/claude.py` gained the `AI_AGENTIC_TOOLS` flag (unconditional off-default, no environment carve-out, unlike `CLERK_ENFORCE_AUTH`) and `_call_anthropic_agentic_turn()` (the raw Anthropic Messages API tool-use call — Anthropic-only, Gemini scoped out as deliberate follow-up). `app.py`/`asgi.py` both branch on the flag at their one `/ask` AI-synthesis call site; when off, both call the exact same `ask_claude`/`ask_ai_async` functions as before (confirmed independent via `inspect.getsource` — neither references `run_agentic_ask`), so flag-off behavior is unchanged. **Web-search last-resort gate (§9.3):** `web_search` is absent from the tool list offered to the model (`ai_tools.get_tool_schemas(include_web_search=...)`) until a `search_judaic_texts`/`search_library` call has returned an insufficient result in the current turn (`_tool_result_is_insufficient()` — structural check, not per-tool special-casing); `run_agentic_ask`'s result dict carries `used_web_search`, which both `app.py` and `asgi.py` now check (not the flag again) to set `needs_web_warning` — this is a real gap plan.md §9.3 point 3 didn't make mechanical and had to be designed during implementation. **Docs/rules (§9.7):** `docs/AI_TOOLS.md` (new) and the standing rule added to `.agents/ENGINEERING_RULES.md`. **Tests:** `tests/test_ai_tools.py` (~65 tests, registry + all 22 tools), `tests/test_agent_loop.py` (14 tests, scenarios a–g), `tests/test_claude_agentic_turn.py` (7 tests, real SDK `Message` parsing against the suite's respx mock) — all new. `pytest -q` green with the flag **both unset and `=true`** (the prompt's explicit acceptance bar), 91.4–91.6% coverage. `graphify update .` run. **Two real bugs found in my own Prompt-20 code, fixed directly rather than deferred (not pre-existing app bugs):** (1) `text_engine.format_source_citation`'s `rpartition(" ")`-based split broke for any multi-word book title, not just the comma case — replaced with an anchored regex requiring the whole trailing suffix to be locator-shaped; (2) `ai_tools.execute_tool()` called `health.is_healthy()` bare inside an async function — a blocking-I/O-on-the-event-loop violation identical to the one already fixed elsewhere per §26.1, since `is_healthy()` can perform an inline blocking re-probe — wrapped in `asyncio.to_thread`. **Two pre-existing tests were flag-blind and made agentic-aware** (`tests/test_ask.py::test_anthropic_failure_meta_fallback_true` and `::test_fastapi_total_budget_timeout_falls_back_gracefully` now patch both the legacy entry point and `ask_pipeline.run_agentic_ask`, so the failure/timeout injection fires regardless of which path is live). **Deviations from plan.md's literal text (documented in `docs/AI_TOOLS.md`, not silent):** `get_community_profile`/`get_prayer_text` reimplemented against backend-only data rather than wrapping the named `app.py`-only functions (import-discipline conflict — §9.2b itself names functions that live in `app.py`, and `ai_tools.py` may only import `backend.*`); `_THREAD_POOL` not reused for parallel tool dispatch (same import-discipline conflict — `asyncio.gather` over already-async `execute_tool()` coroutines used instead); Anthropic-only agentic mode, Gemini function-calling scoped as follow-up. **A tension surfaced, not resolved here (out of this prompt's scope):** Prompt 35 (§22) recommended *deleting* `backend/ask_pipeline.py` as dead code (verified 2026-08-19, zero production importers at the time) — this prompt has since made half that module live and production-reachable (`run_agentic_ask`, gated by the flag), while the other half (`run_ask_pipeline`/`AskPipelineResult`) remains genuinely dead. See the new findings section (`plan.md`, end) for the concrete recommendation. |
| 21 | §10.1 README | ✅ **Done as scoped** (completed 2026-08-21) | Docs-only pass against all 10 plan.md §10.1 items, verified/fixed against the live repo rather than trusted from prior prose. **Already true, confirmed unchanged:** the Required env-var table (7 vars) was already accurate against code; `docs/SERVICE_ARCHITECTURE.md`/`docs/AGE_AND_SAFETY_POLICY.md`/`docs/SECURITY.md` all already existed; `pytest.ini`'s `--cov-fail-under=85` confirmed current (measured ~91%); `RATELIMIT_STORAGE_URI` was already the correct env-var name in README — the specific stale bug plan.md's prose named (`RATE_LIMIT_PER_MIN`) was already absent, fixed by an earlier uncommitted pass. **Item 1 (badges):** added MIT license badge (README.md is my responsibility per this row's scope note; `LICENSE`/`package.json`/`THIRD_PARTY_LICENSES.md` themselves left untouched — Prompt 22's job, confirmed done in the row above), Python 3.12 badge (no `runtime.txt`/`.python-version` pin exists; sourced from `.github/workflows/ci.yml`'s `python-version: "3.12"`), a Vercel-deployed badge, and a GitHub Actions CI status badge (`akivayevdayev-debug/Shelah_app` confirmed via `git remote -v`) — existing SonarCloud badge kept. Added a prominent italic "educational information only, not legal/halachic advice" line directly under the H1, above the badges. **Item 2 (screenshots/demo):** added a Screenshots & Demo section with 3 Markdown image placeholders + explanatory `<!-- TODO -->` HTML comments (reader view, AI answer with rendered sources, zmanim/calendar) — no real images exist, none invented. Live-demo link: `WebFetch`-verified the only candidate URL found repo-wide (`https://shelah-app.vercel.app`, referenced only in `docs/archive/*` as an example) returns `HTTP 403` — not confirmed as a real live deployment, so left as an explicit TODO per the prompt's instruction rather than asserting an unverified URL. **Item 3 (feature list):** refreshed wording for community-lens answers, bilingual EN/HE + RTL, PWA/offline, dark mode. Agentic AI tool-use bullet deliberately **not added** — `backend/ai_tools.py`/`AI_AGENTIC_TOOLS` re-checked via grep both at the start and again as the final action of this pass (concurrent Prompt 20 work); still absent both times, confirmed by Prompt 20's own row above (`❌ Not started`). **Item 4:** new "AI & Safety" section — multi-model RAG (Gemini primary/Claude fallback), the real 4-tier source-priority order transcribed from `backend/claude.py::CORE_SYSTEM_PROMPT` (Sefaria direct → broad Sefaria/HebrewBooks/Halachipedia → Acharonim/poskim → internal knowledge, web as documented last resort), the persistent non-dismissible disclaimer + `safety_class` referral variant, the 13+/16+-EU age posture linked to `docs/AGE_AND_SAFETY_POLICY.md`, and a link to `/ai-disclosure` (route confirmed live at `backend/routes_legal.py:21-27`). **Item 5:** added an explicit inline link to `docs/SERVICE_ARCHITECTURE.md` inside the Architecture Overview section itself (previously only reachable via the Further Documentation table at the bottom). **Item 6 (env-var reconciliation — most safety-critical item):** grepped every `os.environ`/`os.getenv` call across `backend/*.py`, `app.py`, `asgi.py` (asgi.py has zero direct reads, confirmed) and reconciled the full Optional table against it. Real gaps found and fixed: added 5 previously-undocumented real vars — `PER_USER_DAILY_BUDGET_USD` (`backend/cost_meter.py:358`, default `2.00`, the actual per-request budget enforcer), `AI_TOTAL_BUDGET_SECONDS` (`backend/claude.py:120`, default `45`, via the `_int_env` helper — cross-checked against `tests/test_config_lint.py`'s dead-config lint, confirmed live-referenced not dead), `DEPLOY_HASH` (`app.py:1427`, default `"v8"`, service-worker cache versioning), `FLASK_DEBUG` (`app.py:2135`, only affects the `python3 app.py` direct-run path), and `SEFARIA_API`/`SEFARIA_V3_API` (`backend/sefaria_library.py:31-32`) plus the 5 `SUPABASE_*_TABLE` name-override vars and the Vercel-platform-injected `VERCEL`/`VERCEL_ENV`/`VERCEL_GIT_COMMIT_SHA` trio. Fixed one stale/wrong description: `CLERK_SECRET_KEY`'s old text claimed "not used by the main app" — false, `backend/routes_privacy.py:180` reads it for the Clerk-identity-deletion half of `/api/user/delete-account`; split `CLERK_TOKEN_TEMPLATE` (script-only, unchanged) into its own row and corrected `CLERK_SECRET_KEY`'s description accordingly. Refined `CLERK_ENFORCE_AUTH`'s default cell to reflect its actual runtime-conditional logic (`backend/auth.py:73-78`: `true` when `VERCEL=1`/`FLASK_ENV=production`, else `false`) rather than a flat `false`. Added an explicit "not actually environment variables" footnote for `STRICT_SUPABASE_RLS` (hardcoded `True` literal, `app.py:904` — the name only appears in test monkeypatches) and `RATE_LIMIT_ASK`/`RATE_LIMIT_DEFAULT` (both literals, `app.py:809-813`, per that file's own comment) — directly resolves the ambiguity in plan.md §10.1's own item 6 prose, which cited these as if they were grep-able env vars. Never included a real secret value anywhere. **Item 7 (testing):** expanded with the offline-suite mechanics, the confirmed 85%-coverage gate, and an explanation of this repo's golden-master convention (`plan.md` §6.2) grounded in the actual `app.py`→`backend/` extraction history, plus an enumerated list of what `ci.yml` actually runs (ruff non-blocking, pre-commit blocking, pip-audit non-blocking, pa11y-ci light+dark). **Item 8:** created `CONTRIBUTING.md` (dev setup, test/coverage instructions, `.agents/ENGINEERING_RULES.md` reference, solo-project-calibrated PR process) and `CODE_OF_CONDUCT.md` (Contributor Covenant 2.1, enforcement contact matching `docs/SECURITY.md`'s existing address) at repo root — neither existed before; both linked from new README sections. **Item 9:** new "Credits & Licenses" section summarizing `THIRD_PARTY_LICENSES.md`'s coverage (Sefaria/Hebcal/Wikipedia/Halachipedia/SILEOT/OSS libs) and linking it. **Item 10 (security policy):** chose **option (b)** — link `docs/SECURITY.md` directly (which already has a "Reporting a vulnerability" section with a real contact email) rather than creating a redundant root `SECURITY.md` stub, because a web search of GitHub's own documentation confirmed `docs/` is one of GitHub's three officially auto-detected security-policy locations (root, `docs/`, `.github/`) — `docs/SECURITY.md` already gets the Security-tab surfacing a root file would add, so a second file would only be a sync-drift risk with no functional gain. **Deliberately left as TODO (not fixed here, flagged for follow-up):** the 3 screenshot images and the live-demo URL (both above); two unrelated dead-config bugs surfaced incidentally while grepping for item 6 but out of this docs-only prompt's scope to fix in code — `RATELIMIT_ENABLED` is set by `tests/conftest.py:35` and `ci.yml` but read by zero application code (confirmed by grep; Flask-Limiter doesn't consume it either), and `backend/rag.py::_env_int()` (line 261) is defined but never called anywhere. Full env-var and section content re-verified by re-reading the finished `README.md` was not required since Edit/Write tool errors would have surfaced any failure; no application code was touched, only `README.md`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, and this table row. |
| 22 | §10.2 LICENSE | ✅ **Done** | `LICENSE` verified byte-for-byte against plan.md §10.2's exact block (MIT, "Copyright (c) 2026 Akiva Yevdayev") — no discrepancy found, no change needed. **Real bug found and fixed:** `package.json` declared `"license": "ISC"` (line 23) despite the repo actually being MIT-licensed — the prior "verify package metadata" flag was correct to be suspicious; fixed to `"MIT"`. No Python packaging metadata exists in this repo (no `pyproject.toml`/`setup.py`/`setup.cfg`) — correctly N/A, not invented. `THIRD_PARTY_LICENSES.md` extended with real findings from re-verifying every claim against primary sources rather than trusting the existing prose: (1) **DOMPurify was miscategorized** — it was grouped under "Open-source libraries (MIT-licensed)" alongside Tailwind/DaisyUI/marked, but DOMPurify `3.4.9`'s own `package.json` declares `"license": "(MPL-2.0 OR Apache-2.0)"`, not MIT; moved to its own correctly-labeled section explaining the dual license and why the unmodified-CDN-bundle use here doesn't trigger MPL-2.0's file-level copyleft. (2) **SILEOT font** — upgraded from "assumed... not yet verified" to an evidence-based identification: `static/fonts/SILEOT.woff`/`.woff2` matches SIL International's own canonical distribution filename (`SILEOT.ttf`) for the "Ezra SIL" Hebrew biblical OpenType typeface, licensed SIL OFL 1.1 (Reserved Font Names "SIL"/"Ezra"), with a Hebrew-layout-intelligence component under MIT/X11 (Hancock/Hudson) — a residual caveat is kept (filename/public-terms match, not a byte-level WOFF `name`-table decode) rather than declaring it fully closed, so `docs/LAUNCH_CHECKLIST.md`'s attorney-review flag is softened, not removed. (3) **New "Backend (Python) dependencies" section added** — this file previously covered only frontend JS libs and content sources, entirely omitting the Python stack; swept all direct `requirements.txt` deps via `pip show`/PyPI metadata and confirmed all but one are permissive (MIT/BSD-3-Clause/Apache-2.0). **Found one real non-permissive dependency:** `zmanim` `0.3.1` (`backend/zmanim_engine.py`) carries `License :: OSI Approved :: GNU Lesser General Public License v2 or later (LGPLv2+)` per its installed dist-info — a weak-copyleft license, not previously documented anywhere in this repo's licensing docs; added with an explanation of why library use (not redistribution/modification) doesn't obligate this project's own MIT code, consistent with the file's own "not simply MIT/BSD/Apache-2.0 permissive deserves a mention" standard. Sefaria caveat (per-text licensing varies) and Wikipedia CC-BY-SA share-alike note were both already present and accurate — Sefaria's wording tightened slightly (added CC0/CC-BY-NC to the mix, not just CC-BY/public-domain) but not overhauled. Hebcal section and the top-of-file "none of it is relicensed by Sh'elah's MIT license" statement were already present and unambiguous — no change needed. **Residual gap (not fixed, out of this prompt's file scope):** `templates/licenses.html` (the in-app attribution page) still mirrors the *old* THIRD_PARTY_LICENSES.md text — old DOMPurify-as-MIT grouping, old hedged SILEOT wording, no `zmanim` LGPL mention — now out of sync with the corrected root doc; flagged as a background task rather than edited here, since this prompt's scope was `LICENSE`/`THIRD_PARTY_LICENSES.md`/`package.json` only, not `templates/`. **README MIT badge** (plan.md §10.2's closing parenthetical) was intentionally left untouched — owned by the concurrent Prompt 21 agent; at the time this row was written, `README.md`'s badges row (line 2) had only the SonarCloud quality-gate badge, no MIT badge yet, and README.md §"License" (line 310-311) already links to `LICENSE` in prose (pre-existing, unrelated to the badge). |
| 23 | §10.3 GitHub profile | ✅ **Done 2026-08-21** (item 1 only) | `github.com/akivayevdayev-debug/akivayevdayev-debug` created and pushed. Items 2-4 (pin repos, Sh'elah repo polish, bio/links) closed as descoped, not pursued. |
| 24 | §11 Vercel deploy fix | ✅ **Done 2026-08-21** (operator-verified live preview deploy) | `api/index.py` + modern `vercel.json` shipped, memory key already removed (see Prompt 27). Operator ran a real `vercel deploy`, confirmed the live preview works, and closed this out. Per-item sub-checks (CDN headers, cold-start import path) were not individually re-verified — reopen just that sub-item if one turns out to have been missed. The `vercel.json` "no `rewrites` key" discrepancy (§29.2/§30, Prompt 42 STEP 2) is unblocked by this landing but still separately open. |
| 25 | §12 Phase 6 — product surface | ✅ **Done 2026-08-21** (search filter UI closed as descoped) | `about.html`/help+glossary, `robots.txt`/`sitemap.xml`, feedback endpoint, breadcrumbs, citation deep-links, and consent-gated analytics all landed. §12.3.2 (search filter UI) deliberately not built — see Prompt 42/§29.1. Seven independent findings from implementing this surfaced separately in Prompt 42/§29. |
| 26 | §13 Phase 7 — Sonar debt | ⏸ **Deferred by design** | Run after §4 is confirmed complete |
| 27 | §14 Phase 8 — Vercel cost (config) | ✅ **Done** | `vercel.json` has no `memory` key, `regions: ["iad1"]` set |
| 28 | §14 Phase 8 — Vercel cache tiers | ✅ **Done 2026-08-22** | New `backend/cache_policy.py` (4-tier `Cache-Control` classification) wired into both `app.py`'s Flask hook and `asgi.py`'s middleware — closing a gap where the native `/ask`/`/api/async/health` routes previously shipped with no `Cache-Control` header at all. Cross-user cache-leak risk in the zmanim/holidays session-fallback branches closed via `flask.g.cache_tier_force_private`. FOT/invocation-reduction audits (§14.5/§14.6) verified clean, no fix needed. Full write-up: `docs/VERCEL_COST_OPTIMIZATION.md`; `plan.md` §14.10. **Baseline capture and preview-deploy verification (§14.7.1/§14.7.4) remain pending — operator/deploy-only, see Prompt 45/§33.4.** 91.09% coverage (61 new/modified tests), `graphify update .` run. |
| **29a** | **§16 Phase 9a — rate limiting** | ✅ **Done 2026-08-22, commit `a519a21`; verified fully committed and importable 2026-08-26.** | Superseded the 2026-08-15 partial reaudit below (kept for lineage). The reduced scope §16.8.2 identified shipped in full: `backend/rate_limit.py` is the single ASGI-middleware enforcement point for both transports (D1 — no more parallel `asgi.py` limiter), backed by `RATE_LIMIT_REDIS_URL`/Upstash with a loud-warning in-process fallback (D3), Flask-Limiter and `asgi.py`'s independent limiter both deleted, `_resolve_client_ip()` stays the single canonical trusted-key function (D2), `RATE_LIMIT_DEFAULT` blankets unlisted routes (D4). Full `pytest -q` green, `graphify update .` clean. **2026-08-26 re-verification (Prompt 49/§37):** `backend/rate_limit.py` had shipped in the working tree only — a clean-room `git archive HEAD` export showed `main` still didn't import (`backend/routes_devtools.py` still referenced the deleted Flask-Limiter symbols, and `rate_limit.py` itself was untracked). Fixed and committed as `a519a21`; confirmed `python -c "import app"`/`"import asgi"` both exit 0 against current `main`. See `plan.md` §16.9 (full reconciliation) and **Prompt 43/`plan.md` §31** for follow-up — closed 2026-08-26: `tests/test_rate_limit_middleware.py` now covers the fail-open/fail-closed posture, and `RATE_LIMIT_REDIS_URL` is confirmed set in production. <details><summary>2026-08-15 partial reaudit (superseded, kept for lineage)</summary>Re-verified against the then-current uncommitted diff: D1 fixed pragmatically via a second parallel `asgi.py` limiter rather than one unified middleware; D2 fully fixed (canonical `_resolve_client_ip()`); D3 (shared store) still open, blocked on Upstash provisioning; D4 fixed for the Flask-mounted surface; body caps added. The "two independently-drifting limiters" finding this row originally led with is exactly what STEP 1's unified middleware (shipped 2026-08-22) closed.</details> |
| **29b** | **§16 Phase 9b — WAF + cost breaker** | ✅ **Done 2026-08-22 — L3 + frontend UX shipped, L1 spec'd not applied** | `backend/cost_meter.py::is_global_cost_breaker_tripped()` — a real cross-instance-safe daily USD ceiling, cached 30s, fail-open on cache/Supabase errors, reusing `backend/rate_limit.py`'s shared store (✅ a deliberate deviation from §16.3-L3's literal "reuse `search_provider.py`'s breaker" text — **blessed and corrected in place 2026-08-23, see Prompt 45/§33.1**). Wired into the real `/ask` path (`asgi.py::ask_async`): serves a cached answer when available, else the calm paused-state payload, and makes the previously-dead `ASK_RESPONSE_CACHE` mechanism live for the first time. Frontend banner (`templates/index.html`'s `renderDisclaimerBanner()` + new `.ai-disclaimer-banner--breaker-paused` CSS, reusing `--ai-warning-*` tokens) verified live in-browser, both themes. `docs/SECURITY.md` §7 has the full WAF (L1) dashboard spec — **written, not yet entered in the Vercel dashboard, operator action.** Full write-up: `docs/VERCEL_COST_OPTIMIZATION.md`, `docs/SECURITY.md` §7, `plan.md` §16.10. A real circular import (`cost_meter → rate_limit → helpers → search_provider → claude → cost_meter`) was found and fixed during implementation — see Prompt 45/§33.7. **DAILY_BUDGET_USD is still empty in `.env.example`/production (§24.4) — the breaker is live code with an inert threshold until configured (reconfirmed 2026-08-23, still unresolved).** Also newly unblocked Prompt 33c's (§20c) decision — **decided 2026-08-23, Option A, see Prompt 45/§33.5.** |
| **29c** | **§16 Phase 9c — identity quotas, Turnstile, logging** | ✅ **Done 2026-08-24** | Identity-aware keying shipped in `backend/rate_limit.py`: Clerk `sub` (via `backend/auth.py`'s existing `extract_user_id_from_bearer_value`, not re-implemented) when authenticated, IP fallback otherwise; authenticated callers get a higher per-minute allowance (`authenticated_max_requests=40` vs. `max_requests=20`) plus an independent `daily_max_requests=200` cap anonymous traffic never sees. New `backend/turnstile.py` gates anonymous `/ask` past a per-IP hourly threshold (`TURNSTILE_ANON_HOURLY_THRESHOLD`, default 5) behind Cloudflare Turnstile — chosen over Vercel BotID because BotID's server-side verification SDK is JavaScript-only and `/ask` is a Python FastAPI route at every plan tier (documented in the module docstring and `docs/SECURITY.md` §8); `siteverify` uses async httpx, feature-flagged `TURNSTILE_ENABLED` (default off, true no-op — reuses `backend.rate_limit.get_shared_store()` per the §3 reuse rule, no second store). Mitigation observability: `backend/logging_setup.py::log_mitigation()` emits one structured line + a Sentry breadcrumb (not an event) per rejection, hashing the key (never a raw IP/`sub`) per §8.D. `docs/API.md` corrected (the pre-existing false 429 claim, plus the real limits/body shape/`Retry-After`/env matrix), `.agents/ENGINEERING_RULES.md` gained the "every new route must be classified in the policy table" rule. **A severe, pre-existing, unrelated production bug was found while writing this pass's own authenticated-`/ask` tests — every authenticated call to the real `/ask` endpoint currently 500s — filed as Prompt 47/`plan.md` §35, not fixed here (out of this prompt's scope).** Full suite green (pytest -q, 91.17% coverage), `graphify update .` run. |
| **30** | **backend/ask_pipeline.py decide-or-delete** | ⏸ **Decision made (via Prompt 35), re-scoped 2026-08-21, still NOT executed as of 2026-08-26 — open across five passes.** | 2026-08-01 Phase A pass found `run_ask_pipeline()` never calls `classify_safety()` and that the module is 91%-covered-by-direct-tests but 0%-production-reachable. **➡️ Superseded 2026-08-19 by Prompt 35 / `plan.md` §22**, which recommended deleting the whole file + a parity suite. **That recommendation is itself now stale**: Prompt 20 landed 2026-08-21 and made `run_agentic_ask()` (the module's other export) a real production importer — see Prompt 40 STEP 3 / `plan.md` §27.3, which re-scopes the decision to a split: delete only the still-dead `run_ask_pipeline`/`AskPipelineResult` + their two dedicated test files, keep the module and `run_agentic_ask()`, still build `tests/test_ask_transport_parity.py`. **Verified 2026-08-26: none of it has happened** — `backend/ask_pipeline.py` still defines both `run_ask_pipeline` and `run_agentic_ask`, `tests/test_ask_pipeline.py`/`tests/test_ask_pipeline_smoke.py` still exist, no `tests/test_ask_transport_parity.py` exists. This is the correct next prompt to run for this track — the decision is settled, only execution remains. |
| **31** | **CSP nonce hardening: onclick/inline-style refactor decision** | ✅ **Decided 2026-08-15 (final) — Option 2 (defer, keep `'unsafe-inline'`). Nothing left to execute.** | 2026-08-01 Phase G pass found that removing `'unsafe-inline'` from `script-src`/`style-src` is blocked on 112 inline `onclick=` handlers + 43 inline `style=` attributes in `templates/index.html`, not the (already-resolved) Tailwind CDN JIT compiler the old code comment cited. **Confirmed directly with the user 2026-08-15: Option 2.** CSP stays as-is — no nonce, `'unsafe-inline'` retained on both directives; this is the accepted final state, not a placeholder pending a future revisit. Recorded in `plan.md` §18.3 and Prompt 11's own status row. Option 1's refactor remains available as future work only if `index.html`'s markup is refactored for other reasons first — see Prompt 32, which now inherits that same 112-`onclick=` surface for its own, unrelated extraction work. |
| **32** | **§19 — inline-block / ES-module duplication (frontend §2 instance)** | ⏸ **Not started, plan written 2026-08-17 — unblocked and ready.** No longer gated on anything: Prompt 31's CSP decision (the one prior condition on this row's Phase 4+) is now final (Option 2, 2026-08-15), so there is no pending onclick-refactor to collide with. Verified 2026-08-26: zero of Phase 0-3 has landed — `grep` finds no `__shelahLegacyBookmarkBound` guard anywhere, `static/js/ai-service.js` still has no retry/timeout logic, `static/js/zmanim.js` still has no `installZmanim()` export and the inline `initZmanim()` (`templates/index.html`) is still there, and `tests_js/` still contains only `sentry_init.test.js`. | New finding, never tracked before (`grep` for `ShelahModules`/`askAi`/`fetch('/ask'` across `plan.md` + this file returned zero hits pre-2026-08-17). `templates/index.html`'s 10,004-line inline block (`:1568-11571`, 314 top-level functions) runs its own `POST /ask` (`:9365-9408`) and its own semantic-bookmark save (`:5303-5356`) beside the `static/js/` modules that already own both, and never uses the `window.ShelahModules` bridge (`main.js:18-26`) — **zero consumers repo-wide**. Direction of drift is counter-intuitive: the inline `/ask` copy is the *stronger* one (60 s timeout + 3-attempt 502/503/504 retry) and `ai-service.js::askAi` has none of it, so the naive swap is a regression. The semantic-bookmark double-bind is currently prevented only by `reader-ui.js:175`'s implicit-global guard, which **any** scope conversion silently breaks. `docs/FRONTEND.md` carries 12 verified-false status claims (`plan.md` §19.5). Test floor is one file (`tests_js/sentry_init.test.js`). Verbatim bulk-move to a single `static/js/app.js` explicitly rejected — see this prompt. Phased plan in `plan.md` §19.8; none executed. Recommend Phase 0 next — small, and disarms the live semantic-bookmark double-POST tripwire. |
| **33a** | **§20a — cost-ledger integrity (price table + observability)** | ✅ **Done 2026-08-19** | `_PRICE_PER_M` now prices `gemini-3.5-flash-lite` (`$0.30`/`$2.50` per 1M, verified against `ai.google.dev` pricing 2026-08-19) via a CI-guarded, single-source-of-truth check (`backend.claude.get_dispatchable_models()` + `tests/test_cost_meter_pricing.py`); unpriced-model lookups now warn once per process (log + Sentry breadcrumb) instead of failing silently; the misleading `"provider"`-key-holds-a-model-name booby trap is renamed to `"model"` across all 12 sites in `claude.py`; `_insert_usage_row`'s swallow now reaches `_capture_backend_error` instead of `logger.debug` (C3b). Full pytest green (1391 passed). **Backfill not executed** — `scripts/recompute_ai_usage_log_cost.py` is written (dry-run by default) but deliberately not run against the single production-looking Supabase project in an unattended pass; pre-2026-08-19 `ai_usage_log` rows should be treated as under-reported for Gemini-primary calls until an operator reviews and runs it. **Unblocks Prompt 29b.** |
| **33b** | **§20b — atomic per-user budget ceiling** | ✅ **Done 2026-08-20** | `check_and_reserve_user_budget()` (`scripts/sql/check_and_reserve_user_budget.sql`) does the check-and-reserve in one `pg_advisory_xact_lock`-serialized transaction; `check_user_budget_and_enforce()` calls it via RPC and a returned `allowed=false` now blocks. Reservations settle in place via a new `bind_budget_reservation`/`get_budget_reservation` contextvar so `record_llm_call()` never double-inserts; abandoned reservations expire off the existing `retention_enforce()` cron. Per-request ceiling added (`_max_single_ask_reservation_usd()`). Flask's `/ask` unreachability is now a pinned regression test, not an undocumented asymmetry. 20-concurrent-caller test: 1 of 20 allowed (was 20 of 20 pre-fix). `pytest -q` green (1408 passed), SQL file committed. **One test-infrastructure bug found and fixed in the same pass, and specced separately for follow-up as Prompt 38 / `plan.md` §25** — the shared Supabase mock fixture's response shape doesn't match real PostgREST, which briefly caused 13 unrelated `/ask` tests to 402. |
| **33c** | **§20c — multi-account budget bypass: decide** | ✅ **Decided 2026-08-23 (Prompt 45/§33.5) — Option A chosen**, recorded in `plan.md` §20.2 Phase 20c, `.env.example`, `docs/SECURITY.md` §5 | The ceiling keys on Clerk `user_id` and Clerk signup is frictionless — hit the cap, make another account. Three options in `plan.md` §20.2; **recommend A (accept + document) conditional on Prompt 29b shipping**, since A's honesty depends on a global breaker existing. If 29b is not going to be built, A is unavailable and B becomes the default. Must not be left unresolved — "we have a \$2/day cap" is currently a claim this project cannot fully defend. |
| **34** | **§21 — is Postgres RLS actually functioning?** | ❌ **Not started; gated on an operator action.** 2026-08-24: client→Supabase JWT-forwarding path (part A) confirmed correct by code read; the live claims/RLS checks (parts B/C) still need the operator — see Prompt 34's update below. | Not a pure code task. `auth.uid()` resolves a Clerk JWT only if Supabase Third-Party Auth for Clerk is enabled — a dashboard setting versioned nowhere. Four production call sites depend on it (`routes_user.py:227,299`, `rag.py:308,405`), two more than prior audits recorded. All 10 existing RLS tests monkeypatch the client (`tests/test_routes_user.py:138-295`) and therefore cannot answer the question. Needs the dashboard check **plus** a live acceptance test whose **negative** (cross-user) assertion is what gives it teeth. |
| **35** | **§22 — `ask_pipeline.py`: delete + pin parity (recommended)** | ✅ **Done 2026-08-26, sixth pass. Supersedes Prompt 30.** | Executed the re-scoped Option B: deleted `run_ask_pipeline`/`AskPipelineResult` (774→271 lines, -503) plus their two dead-code test files (-350); kept the module and `run_agentic_ask()`. `tests/test_ask_transport_parity.py` (20 tests) pins all six invariants. Two real transport-parity bugs found and fixed along the way (Flask missing `ai_cited_sources` on the security-blocked path; asgi.py had no security-blocked branch at all). STEP 3 (duplication cost) and STEP 4 (`backend/customs.py` decide-or-delete — decided keep, documented in place) both done. Full evidence in the Prompt 35 section below. |
| **36** | **§23 — schema & documentation provenance** | ⚠️ **Partial, landed 2026-08-21** | `ai_usage_log`'s base `CREATE TABLE` reconstructed+committed in an earlier pass (2026-08-20). This pass: provenance headers added to all 5 previously-untracked migration SQL files (honest "provenance unknown as of 2026-08-21", not committed to git yet — working-tree edit only); closed the exact swallow-bug *shape* §23.1's motivating bug is about in three more places (`routes_user.py::accept_legal()` itself, plus `rag.py::_store_ask_history()`/`_store_user_memory_summary()`), each now routing through `_capture_backend_error()` with a regression test proving it fires; repointed all 11 external dangling `plan.md §7.13`/`§7.14`/`§7.10.2`/`§3` citations to `§23.4`/`§8.C`/`§8.D` and added `tests/test_config_lint.py::TestPlanMdCitationsResolve`, a top-level-section lint that is the actual "make this the last time" control (verified it has teeth: a synthetic `§99` fails it). **Built but not fully executable here:** `scripts/generate_database_doc.py` + `scripts/sql/introspect_schema.sql` (a `get_schema_snapshot()` RPC, matching `check_and_reserve_user_budget.sql`'s existing pattern rather than adding a new direct-Postgres-connection credential type) — confirmed reachable against the real live `SUPABASE_URL` (fails with the correct "RPC not found, has introspect_schema.sql been applied?" error), but actually applying that SQL to the live project is a live-infrastructure change deliberately left to the operator, so `docs/DATABASE.md` is still an **interim hand-reconstruction** (rewritten from the real `scripts/sql/*.sql`/`scripts/migrate_*.sql` files, clearly banner-labeled as interim, not machine-generated) rather than the real generated-and-verified output. The shared-scheduled-workflow-with-§21.2.2 deliverable is not done since §21 (Prompt 34) is itself still not started. Full `pytest -q` green (1595 passed, 91.58% coverage). `graphify update .` run. |
| **37** | **§24 — sweep: quick wins + config defaults** | ❌ **Not started, specced 2026-08-19** | Sefaria disk cache almost certainly a silent no-op on Vercel's read-only FS (`sefaria_library.py:108`, bare `except: pass` at `:139-140`); three health endpoints that cannot go red (`asgi.py:282`, `supabase.ready`, `external_apis`); uncommitted `vercel.json` region/cron config plus the §14.6.1 "no cron jobs" rule it contradicts; `.env.example` shipping `CLERK_ENFORCE_AUTH=false` (`:67`), empty `DAILY_BUDGET_USD` (`:98`) and empty `CRON_SECRET` (`:102`). §24.7 lists what was deliberately **not** planned, with reasons. |
| **38** | **§25 — test fixture: Supabase mock doesn't match real PostgREST shape** | ❌ **Not started, specced 2026-08-20** | Found while implementing Prompt 33b, fixed at the call-site level in the same pass but not at the source: `tests/conftest.py`'s autouse Supabase POST mock (`:237-241`) returns `{"data": [], "error": None}` for every `.table()`/`.rpc()` call — a shape no real PostgREST response ever has. This briefly caused `_reserve_budget_or_deny()` to read `bool(None)` as a deny, 402-ing 13 unrelated `/ask` tests. The narrow parsing fix already landed; the fixture itself is still wrong and will silently miscategorize the next `.rpc()` consumer that parses its result differently. |
| **39** | **§26 — primary-`/ask` circuit breakers, dark-theme a11y gate, ruff cleanup** | ✅ **Done 2026-08-20 — all three sub-items closed; two scoped caveats, see evidence** | **§26.1** (the gap Prompt 17 left open): `health` is now imported in `backend/claude.py:27` and consulted at all three `/ask` provider entry points — `_call_gemini_model()` sync primary (`:563`), `_call_anthropic_httpx_model()` Claude fallback (`:1754`, gate placed ahead of `_get_async_client()`), `_call_gemini_httpx_model()` async primary (`:1828`) — which covers both transports without touching a route handler or response builder. Both async gates go through `asyncio.to_thread` because `is_healthy()` performs an inline blocking `requests.get` re-probe once `RECOVERY_INTERVAL` elapses, so a bare call would block the event loop (`.agents/ENGINEERING_RULES.md`). `record_success`/`record_failure` wired **one per attempt**, not one per branch (`:618`/`:631`/`:650`, `:1799`/`:1808`, `:1892`/`:1901`); the empty-response branch records a *failure* (`:631`) — treating "HTTP returned" as success would let endless empty completions reset the counter and keep the circuit permanently closed. Circuit-open returns reuse the existing `{answer, confidence, error, is_fallback, model}` shape with `error="gemini_circuit_open"`/`"anthropic_circuit_open"`, which is not `security_blocked*`, so the route's existing fall-through logic is unchanged; `_call_anthropic_httpx_model()` was restructured around an `_error_result()` closure to preserve the `gemini_error: <upstream>; <ours>` prefix. 8 new tests in `tests/test_ask.py::TestAskPrimaryAiCircuitBreaker` (`:723-948`) — the two route-level ones (Flask `:779`, FastAPI `:814`) prove the negative with **spies that record rather than raise** (a raising spy is swallowed by `/ask`'s own `except` and lands on the identical fallback payload, i.e. passes for the wrong reason), asserting `attempts == []` and that a *wrapped* `get_halakhic_sources` was reached exactly once; the Flask one needs `environ_base={"REMOTE_ADDR": "10.39.26.1"}` because the shared 127.0.0.1 limiter bucket is already spent by earlier tests in the file. **Caveat:** `summarize_with_gemini()` (`/api/bookmarks/semantic`) is a real Gemini call site and is deliberately **not** gated — it is not on `/ask`, which is this item's stated scope. **§26.2** (the hole Prompt 18 left): the deliverable's proposed approach does not work — pa11y 9.1.1 has **no JS-evaluate action**, so `actions: [evaluate: () => localStorage.setItem(...)]` is not expressible; and browser-level `prefers-color-scheme` emulation can't reach dark theme in this app either (probe showed `prefersDark: true` with `data-theme` still `"light"`, because `templates/index.html`'s FOUC script normalizes an absent preference to `'light'`). Implemented instead via pa11y's documented `browser`/`page`/`ignoreUrl` options in new `scripts/a11y_dark_scan.js` (153 lines): seeds the real key — `Sh'elahPrefs`, a JSON prefs blob whose `theme` field it read-merge-writes, **not** a bare `theme` key (`:55`, `:72-81`) — with `evaluateOnNewDocument` before first paint, re-uses `.pa11yci.json`'s own `urls`/`defaults` so there is one URL list, and hard-fails at `:90` if `data-theme !== 'dark'` after navigation (drift guard verified by temporarily renaming the key and confirming it fires; without it a renamed key silently degrades to a second always-passing light scan). Wired at `package.json:13-15` (`test:a11y` = light `&&` dark) and kept inside the single existing `.github/workflows/ci.yml:85` step so a red run is one signal. **This was not theoretical — the first dark run failed 16 elements on `/`**: 15 at 4.34:1 (`--ink-secondary` `#888077` on `--surface-2`) and 1 at 4.10:1 (`--ctrl-text` `#888070` on `--surface-pill`), all normal-weight sub-14px. Third time `--ink-secondary` has been lightened; each prior pass fixed only the surface it measured, so this one computed against **every** dark surface incl. hover (6.30/6.02/5.43/5.92/5.17/4.54) → `#999188` and `#999181`, updated in all three duplicated dark-palette blocks (`static/css/tokens.css:310`,`:336`,`:528`,`:546`,`:594`,`:601`) with the full ratio table written into the token comment at `:297`. `docs/ACCESSIBILITY_AUDIT.md` gained an "Automated coverage (CI)" subsection (`:21`), corrected finding 3's stale "dark already passes" claim, and added finding 4 (`:123-145`); `templates/accessibility.html:100,102,118` updated in `data-en`/`data-he`/fallback. **Caveat:** the muted/primary separation narrows 2.02:1 → 1.61:1 as the price of real margin; and local runs require `PUPPETEER_EXECUTABLE_PATH` pointed at a system Chrome (no machine-specific path was committed — CI installs its own). Verified live against a running instance, **8/8 light + 8/8 dark, 0 errors**. **§26.3**: `ruff check .` → `All checks passed!` — though the premise was off, the errors were **not** concentrated in `tests/` (largest clusters were `app.py` 19 E402 and `asgi.py` 19 E402; `tests/` cleared with four `FAIL_THRESHOLD` import hoists). Real fixes: `backend/routes_prayers.py:71,73` E741 `l`→`line`; unused `Optional` dropped from `scripts/crawl_library_leaves.py`; a stray `_THREAD_POOL = ThreadPoolExecutor(...)` moved out of the middle of `app.py`'s import block (19→5); `_RateLimitStore` and `logger` moved below `asgi.py`'s import block, clearing all 19 with **zero** `# noqa` while leaving `asgi.py`'s load-bearing Sentry INIT-ORDERING import and comment untouched (smoke-import verified). **Caveat:** 5 deliberate `# noqa: E402` remain, all in `app.py` (`:911`, `:921`, `:1106`, `:2097`, `:2116`), each per-line with a one-line reason — back-compat re-export shims, the `__main__`→`app` alias, and blueprint registration, all of which app.py's own comments already justify. No file-level or rule-level suppression added anywhere; CI's `continue-on-error` on the ruff step left as-is per §26.3's exit criteria. **VERIFY:** full suite **1431 passed**, coverage **91.53%** (gate 85%); `ruff check .` clean; pa11y 16/16 checks green; no `/ask` response-shape change and no pre-existing fallback assertion edited. |
| **40** | **§27 — findings from implementing 20 (agentic tools)** | ❌ **Not started — 0 of 4 steps executed, verified 2026-08-26.** | Four independent small findings; none require a decision, all are ready to run. **STEP 1** (Wikipedia connector `User-Agent`): confirmed still missing — `backend/search.py`'s `_get_async_client()` (`:36-43`) constructs `httpx.AsyncClient(timeout=10.0)` with no headers, and `search_wikipedia()`'s sync path (`:46-57`) calls the module-level `_HTTP = requests.Session()` (`:25`) with no `User-Agent` override either; the `ShelahBot` header cited as precedent (`:215`,`:349`) belongs to a different (Sefaria) call site. **STEP 2** (`get_monthly_events` `None`-timezone bug): confirmed still present — `backend/zmanim_engine.py`'s Hebcal URL builder still reads the raw `timezone_str` parameter (`&tzid={timezone_str}`) instead of the already-resolved `tz_name` local, so a `None` timezone still produces a literal `tzid=None` query param. **STEP 3**: re-scoped by its own §27.3 into the Prompt 30/35 re-scope described in those rows above — not executed, see there. **STEP 4** (plan.md §9.2b table accuracy): docs-only, not checked this pass, no evidence it was done. |
| **41** | **§28 — findings from implementing 36 (schema/doc provenance)** | 🔴 **STEP 1 (uncommitted-tree judgment call) still open — human decision, not autonomous.** | Flagged an ~150-path uncommitted working tree as of 2026-08-20, needing human judgment rather than an autonomous commit. **Current count, re-checked 2026-08-26: 147 uncommitted paths** (`git status --porcelain \| wc -l`) — six commits landed this session (Prompts 48/49) paid down a real slice of it (`rate_limit.py`, `cost_meter.py`/`logging_setup.py`/`health_check.py`, four legal/pages/privacy/feedback blueprints + templates, `zmanim_engine.py`'s pytz migration, `tests/conftest.py`'s `RATE_LIMIT_REDIS_URL` fix), but the backlog has also grown from continued working-tree drift, netting out close to where it started. Still requires the operator decision this row's STEP 1 originally asked for — how to break the remainder into reviewable commits — not something to resolve unilaterally. The small, safe `rag_identity_cache_setup.sql` filename rename (the row's second item) not re-checked this pass. |
| **42** | **§29 — findings from implementing 25 (product surface)** | 🔴 **STEP 6 is a live production bug (`/api/user/preferences` UUID-type mismatch) — do first, independent of everything else in this row.** | STEP 7 (apply the `answer_feedback` migration) is a hard prerequisite for the feedback loop to function at all in production — not confirmed applied this pass. STEP 2 (vercel.json rewrites contradiction) was unblocked once Prompt 24's live preview deploy landed (2026-08-21) but not re-verified since. STEPS 1/3/4/5 are independent small items, not checked this pass. |
| **43** | **§31 — findings from implementing 29a (rate limiter)** | 🔴 **STEP 1 (fail-open/fail-closed posture has zero dedicated test coverage) is the one with real risk.** | An untested security-critical branch in `backend/rate_limit.py` — still true as of 2026-08-26's re-verification of Prompt 29a (see that row); no evidence a dedicated test for this posture was added since. STEP 2 (confirm `RATE_LIMIT_REDIS_URL` is actually set in production) is an operator action, unanswered. STEP 3 (DECISIONS.md currency) is optional, not checked. |
| **44** | **§32 — findings from implementing 26/27 (Sonar debt, Vercel quick wins)** | 🔴 **STEP 1 (commit the `templates/index.html` dialog double-handling fix) still sitting only in a local working tree.** | `templates/index.html` remains uncommitted (see Prompt 41/§28.1's tracked backlog, still 147 paths). STEP 2 (continue the 48-function Sonar complexity backlog) and STEP 3 (defer `app.py`'s `supabase` import) are real, no-urgency backlog, not checked this pass. STEP 4 (`calendarModal` backdrop-click parity) needs an operator decision before any code change. STEPS 5–6 are documentation/awareness only. |
| **45** | **§33 — findings from implementing 28/29b (cache tiers, WAF + cost breaker)** | ✅ **Done as scoped 2026-08-23 — STEPS 1/2/3/4/6/7 closed, STEP 5 operator-only.** | Full detail already in this table's Prompt 45 lineage via Prompts 29b/33c above; STEP 5 (WAF dashboard entry) remains blocked on dashboard access this session doesn't have. New findings from this pass (the then-180-file uncommitted backlog, now Prompt 41 above) filed as Prompt 46/§34. |
| **46** | **§34 — findings from implementing 45 (cost-breaker sign-off, §20c decision, cache-purge verification)** | 🔴 **STEP 1 is the same uncommitted-tree finding as Prompt 41/§28.1 — see that row for the current count (147 as of 2026-08-26).** | STEP 2 (refresh `docs/SECURITY.md` §5's stale pre-unification rate-limiter description) is small and independent — worth re-checking now that `backend/rate_limit.py` is finally committed (Prompt 49/`a519a21`), since that landing is exactly the kind of drift this step exists to catch. Not re-verified this pass. |
| **47** | **§35 — findings from implementing 29c (identity quotas, Turnstile)** | ✅ **Done 2026-08-25.** | Every authenticated `/ask` call previously returned 500; fixed by threading an explicit `bearer_token`/`authorization` parameter down the full chain instead of reading Flask's global `request` unconditionally. Full suite 1642 passed, 0 failed. See `plan.md` §35.1/§35.2. One test-infrastructure finding this pass's verification surfaced, filed as Prompt 48/§36 below. |
| **48** | **§36 — findings from implementing 47 (`RATE_LIMIT_REDIS_URL` test leak)** | ✅ **Done 2026-08-26, commit `9a7c159`.** | `tests/conftest.py` now blanks `RATE_LIMIT_REDIS_URL` before any app-level import, exactly as specified, closing a real leak of a developer's local `.env` credential into every test run. Verified: full suite green with a real `RATE_LIMIT_REDIS_URL` present in the shell, no more `RuntimeError: Event loop is closed`. |
| **49** | **§37 — the import-chain repair session (`main` did not import)** | ✅ **Done 2026-08-26.** | Five stacked `ImportError`s found on the committed tree; all five now fixed and committed (`5a018aa`, `e7d2064`, `871a100`, `7a809b9`, `a519a21`), plus three further, previously-undocumented blockers this session's own verification pass surfaced and closed (`7cf767c` zoneinfo migration, `f81ace6` cost-breaker cron function, `9a7c159` four untracked blueprints). `python -c "import app"`/`"import asgi"` both exit 0 against a clean-room export of current `main` for the first time. Two follow-up findings filed as Prompt 50/§38. |
| **50** | **§38 — findings from closing 49/§37.2 (legal-page content, test-isolation bug)** | 🟡 **Not started, filed 2026-08-26.** | Two low-severity, non-blocking items: STEP 1 — the working tree's `terms.html`/`privacy.html`/`accessibility.html`/`index.html`/shared-component diffs add real cross-links between all 7 legal pages (large, content-sensitive, possibly bundled with an unrelated topbar refresh); 3 of 7 `TestLegalPagesCrossLinking` cases fail without it. STEP 2 — `tests/test_cost_meter_call_sites.py` (untracked) has an order-dependent test-isolation bug: passes in the full suite, fails run alone. Neither blocks anything else. |

**2026-07-31 UI/UX pass** (ad hoc bug reports, not numbered prompts — see `plan.md`'s changelog note): full-article source-attribution parity with the AI answer box, dark-mode color audit (language toggle, calendar event colors, settings-panel buttons, global text-selection highlight), mobile header/logo layout, mobile calendar legibility, library-sidebar accordion arrows, and a first pass on library-loading reliability (loading-overlay race condition, chapter-grid-modal/AI-modal stacking, pre-flight-fetch-before-loading-feedback, stale-response race guard, prayer-load fatal-error fallback). Shulchan Arukh siman-grid section-grouping and the backend prayer/siddur-loading parallelization were in progress as of this pass — check `plan.md` for current status before assuming done.

**2026-07-31 Prompts 1–11 verification + dead-code audit** (see `plan.md` §18 for full evidence). Re-verified every Prompt 1–11 status row in the table above against the running codebase rather than trusting the prior snapshot; found and fixed one live bug (the dark-mode no-FOUC script forcing dark mode for every user via a dead `localStorage` key — Prompt 9, `plan.md` §18.2). Dead/unused/redundant code and docs registry found in that pass — **all items below were resolved in the 2026-08-01 pass** (Phases A–G, see the status table above for per-prompt detail):
- ~~`app.py`'s Phase-1/2 compatibility shims are now fully dead~~ — **resolved**: every genuinely-unused re-export deleted after cross-checking each blueprint's `from app import (...)` block and every lazy `import app as _app` attribute-access site (2 false positives caught this way and correctly kept — `WEB_LAST_RESORT_WARNING`/`_extract_query_keywords`, consumed dynamically by `backend/rag.py`). `ruff --select F401` is clean across `backend/`/`app.py`/`asgi.py`.
- ~~`backend/ask_pipeline.py`~~ — **✅ resolved 2026-08-26 (Prompt 35)**: see the Prompt 35 row/section below for what shipped.
- ~~Dead CSS: `.ai-source-card`, duplicated stagger-delay rule, `ai.css` pre-token-migration leftovers~~ — **resolved**: `.ai-source-card` deleted, stagger-delay consolidated into `loading.css`'s canonical rule, `--ai-loading-color` and the orphaned `#aiSynthesis p.animate-pulse` rule deleted from both `ai.css` and `tokens.css`.
- ~~Stale docs describing removed functionality~~ — **resolved**: all 6 archived to `docs/archive/` with redirect stubs.
- Investigated and ruled out as false positives (still true, unchanged): `static/js/{ai-service,reader-ui,state,zmanim}.js` looked orphaned (zero direct `<script>` references in any template) but are all reachable via `main.js`'s ES-module `import` chain plus `service-worker.js`'s precache list — not dead

---

## Prompt 1 — §4 Phase 1: extract text/formatting → `backend/utils/text_engine.py`

```
Read plan.md §2, §3, §4 PHASE 1, §5, and §6 in full before writing any code. Execute PHASE 1 exactly as specified.

Scope: move from app.py into a new backend/utils/text_engine.py the constants HEBREW_DIACRITICS_RE, CLOCK_TIME_LATEX_RE, HALAKHIC_VERDICT_RE, UI_SECTION_KEYS and the functions _strip_model_web_warning_prefix, _normalize_ai_answer, _bold_halakhic_verdicts, _collapse_markdown_spacing, _format_ui_answer, plus any private helpers in their call-closure (verify with `graphify query`/`graphify path`).

Procedure (strict order):
1. Read-only: build the call-closure; grep each target body for Flask globals (g, session, request, current_app) — any function using them is out of scope, flag it and skip.
2. Write tests/test_text_engine.py FIRST as golden-master characterization tests against current app.py behavior (halakhic verdict line, clock-time LaTeX artifact, multi-blank-line markdown, web-warning prefix, Hebrew diacritics string, and edge cases: string cut mid-**bold**, mid-<tag> — no dangling markdown/HTML ever).
3. Create backend/utils/__init__.py and backend/utils/text_engine.py; COPY the symbols verbatim (no rewrites); text_engine must import nothing from app.
4. Import the moved names back into app.py as back-compat shims so blueprints importing from app keep working; only then delete the originals from app.py.
5. Reconcile the HEBREW_DIACRITICS_RE duplicate: canonical copy in text_engine.py, helpers.py re-imports it, app.py copy deleted. First verify semantic equivalence of the two regexes with a small check.
6. Verify: python -m pytest -q green; python -c "import app, asgi, backend.utils.text_engine" clean; run graphify update . and confirm no new import cycle.

Do NOT touch retrieval/search functions (that is Phase 2). Stop when Phase 1 exit criteria pass and report.
```

## Prompt 2 — §4 Phase 2: extract retrieval/corpus matching → `backend/utils/search_provider.py`

```
Read plan.md §2, §3, §4 PHASE 2, §5, §6. Phase 1 (text_engine) is already merged. Execute PHASE 2 exactly as specified.

Scope: move from app.py into new backend/utils/search_provider.py: HEBREW_WORD_GLOSSARY, HALAKHIC_CORPUS_ALIASES, QUERY_STOPWORDS, HEBREW_PREFIXES; _strip_common_hebrew_prefixes, _expand_hebrew_keyword_forms, _extract_query_keywords; _query_search_wrapper, _collect_global_sefaria_sources, _iter_local_json_matches, _find_local_custom_matches, get_halakhic_sources, plus their call-closure helpers (_match_corpus, _extract_hit_snippet, _is_sefaria_hit_relevant, _build_discovery_queries, _collect_external_global_sources, _dedupe_ordered_text, etc. — determine the exact set via graphify closure).

Constraints:
- search_provider.py imports backend.* modules (sefaria, sefaria_library, search, data_service) directly, NEVER app. If a function uses _THREAD_POOL, take it as a parameter or import lazily inside the function.
- Reconcile the HEBREW_WORD_GLOSSARY duplicate (app.py L174 vs helpers.py L139) and the DIVERGED _translate_text_google/_translate_text_mymemory pair per plan.md §2: diff, pick the test-anchored correct behavior, keep ONE canonical implementation, delete both old copies, repoint all callers.
- Golden-master tests first: tests/test_search_provider.py covering Hebrew prefix stripping, stopword removal, glossary expansion, local-JSON-match correctness against a fixture corpus, and get_halakhic_sources happy-path + empty-result fallback shape — all offline via conftest mock_outbound_http.
- Copy verbatim → shims in app.py (blueprints and ask_pipeline.py's flask_app_module.get_halakhic_sources keep working) → delete originals.

Verify: full pytest green (esp. test_ask.py, test_ask_pipeline_smoke.py); no import edge search_provider→app after graphify update .; python -c "import app, asgi, backend.utils.search_provider" clean. Stop and report. Do not start Phase 3.
```

## Prompt 3 — §4 Phase 3: circuit-breaker hardening on `search_provider` network calls

```
Read plan.md §3, §4 PHASE 3, §5, §6. Phases 1–2 are merged. Execute PHASE 3 exactly as specified.

Reuse the EXISTING breaker in backend/health_check.py (APIHealth, FAIL_THRESHOLD=3, RECOVERY_INTERVAL=120) — do not build a new mechanism. Wrap all external calls now in backend/utils/search_provider.py (Sefaria via sefaria_library, _collect_external_global_sources, and the reconciled _translate_text_google/_translate_text_mymemory) with per-service keys: 'sefaria', 'hebcal', 'translate_google', 'translate_mymemory', 'web' (extend the service registry additively if needed).

Pattern per call: check health.is_healthy(service) before → skip to fallback if open; record_success on success; on timeout/connection error record_failure then FAIL OPEN to local corpus (_find_local_custom_matches / _iter_local_json_matches).

Defensive rules: every network call gets an explicit timeout= (audit calls relying on session defaults); catch narrowly (requests.RequestException, httpx.HTTPError, TimeoutError) — never bare except Exception; log warnings via logging_setup. get_halakhic_sources must return a well-formed local-corpus payload even when ALL providers are circuit-open — never raise to the route.

Tests: extend test_health_check.py patterns — N consecutive failures opens the circuit; get_halakhic_sources transparently serves local corpus while open; half-open re-probe after RECOVERY_INTERVAL; use the suite's monkeypatched-clock approach. Full pytest green, graphify update ., stop and report. Do not start Phase 4.
```

## Prompt 4 — §4 Phase 4: streamline `app.py` & finalize wiring

```
Read plan.md §4 PHASE 4, §5, §6. Phases 1–3 are merged. Execute PHASE 4.

1. Confirm every migrated block (formatting regex/constants, glossary tables, formatting fns, retrieval fns) is DELETED from app.py; only re-export shims remain, each commented as "compatibility shim — remove in cleanup ticket".
2. Verify app.py retains exactly: Flask app construction, env bootstrapping, global middleware + security headers, _THREAD_POOL, user-scoped Supabase auth glue (reconcile any auth duplication with backend/auth.py the same reconcile-then-consolidate way as plan.md §2), blueprint registration, and the thin /ask route delegating to ask_pipeline.
3. Re-point internal callers to canonical import paths where low-risk; keep shims only where removal touches many blueprint files (defer those, list them in the report).
4. Final: full pytest green; graphify update . and confirm zero cycles + reduced app.py density; report the new app.py line count (target: meaningfully below 3,560).

Stop and produce a summary report of everything moved, every shim retained, and every duplicate reconciled.
```

## Prompt 4b — §4 Phase 5: concurrent fault-tolerance & thread isolation

```
Read plan.md §4 PHASE 5 in full (5.1, 5.2, 5.3), plus §3 and §5. Phases 1–4 are merged. Execute PHASE 5 exactly as specified.

5.1 — In backend/claude.py, guard _call_primary_model_sync's asyncio.run() with a loop-state probe (try asyncio.get_running_loop() / except RuntimeError → fast path unchanged). If a running loop IS found, submit asyncio.run(coro) to a dedicated lazily-created module-level ThreadPoolExecutor(max_workers=2, thread_name_prefix="claude-loop-bridge") and block on future.result(timeout=AI_TOTAL_BUDGET_SECONDS); log a warning via logging_setup on that path. Never import app._THREAD_POOL. Tests per plan §5.1 in tests/test_loop_bridge.py (fast path, bridge path, no thread growth, no ResourceWarning, timeout propagation).

5.2 — In backend/sefaria_library.py: migrate the plain TTL dicts (_cache, _search_query_cache, _resolved_title_ref_cache, _resolved_query_ref_cache) to backend.cache.TTLCache, FIRST verifying TTLCache is thread-safe (add an internal threading.Lock to TTLCache if not); preserve the disk write-through. Convert the multi-key caches (_title_catalog_cache, _library_index_view_cache, _library_index_adjustments_cache) to build-new-dict + single atomic reference swap (writer-side lock only; readers lock-free). Lock discipline: never hold a lock across network/disk/JSON work — release, fetch, re-acquire, re-check, write. Treat cached values as immutable after insert; do NOT deepcopy inside critical sections (plan explicitly rejects it) — audit callers and add caller-boundary shallow copies only where a caller mutates. Tests per plan §5.2 in tests/test_sefaria_library_concurrency.py (16-thread hammer, torn-read regression, immutability guard, hit-never-blocks-behind-miss).

5.3 — Exit: full pytest green (including Phase 1–2 golden masters unchanged), no import from app, graphify update . Stop and report.
```

---

## Prompt 5 — §7a: AI source box fix (frontend)

```
Read plan.md §7 bullet "AI source box fix" and .agents/ENGINEERING_RULES.md (mandatory). Fix the AI source-box rendering bugs in the frontend:

- Replace innerHTML += accumulation in the source-card render loop (it destroys earlier nodes' click handlers) with single-insert rendering (build all markup, insert once) and DELEGATED event listeners on the container.
- Make escapeHtml usage consistent across every interpolation point.
- Deduplicate the .ai-source-card CSS (there are duplicate style blocks); one canonical definition.
- Replace the capped nth-child stagger with a per-box CSS custom property (--i) driven stagger so any number of cards animates.
- Add skeleton loading states for the source-box region per ENGINEERING_RULES (zero layout shift, prefers-reduced-motion honored, dark+light themes, WCAG AA contrast).

No backend changes. Verify manually in both themes and confirm existing behavior (links, expansion, citation clicks) is preserved. Run graphify update . after.
```

## Prompt 6 — §7b: observability completion

```
Read plan.md §7 "Observability" and §8.E.1. Complete the observability layer (backend/logging_setup.py, cost_meter.py, SENTRY_DSN already exist — finish, don't rebuild):

- Structured JSON logging with a request ID generated per request and propagated across Flask, thread-pool work (_THREAD_POOL), and asyncio tasks (contextvars).
- Confirm Sentry initializes in prod and _capture_backend_error routes to it with request-ID context.
- Audit that cost_meter.py covers EVERY model call site (Gemini + Claude, sync and async paths) and logs token/USD to Supabase; add any missing call sites; add a daily budget-alert hook.
- No blocking I/O on the FastAPI event loop (asyncio.to_thread or async httpx only, per .agents/ENGINEERING_RULES.md).

Tests: request-ID present in log records across a threaded and an async path; cost meter invoked from each model call site (mocked). Full pytest green; graphify update .
```

## Prompt 6b — §7b-frontend: browser Sentry SDK + retire `/api/client-errors` as a proxy

Run **after Prompt 6 merges.** Written 2026-07-31. Depends on the Sentry dashboard setup checklist in `docs/OBSERVABILITY.md` (§ "Sentry project setup") — the two DSNs must exist before this can be verified end-to-end, but the code can be written and tested without them because both paths are no-ops when unset.

```
Read plan.md §17 IN FULL FIRST (Sentry vendor snippets + the 7 binding deviations from them — the vendor defaults are the wrong spec and deviation 1 is a security defect), then §16.2, §16.4, §8.D, §7g, and .agents/ENGINEERING_RULES.md. Prompt 6 (§7b observability) has just merged — do NOT assume what it did.

Additional hard requirements from §17, on top of everything below:
- NEVER hardcode a DSN. Both come from env (SENTRY_DSN, SENTRY_DSN_BROWSER) and both paths are true no-ops when unset (§17.1, §17.3 deviation 2).
- send_default_pii MUST be False on the Python init, overriding Sentry's suggested True (§17.3 deviation 1).
- Do NOT add Sentry's suggested /sentry-debug route to production (§17.3 deviation 3).
- Set the browser SDK's dataCollection block ACTIVE — userInfo: false, httpBodies: [] — not commented out (§17.3 deviation 5). Omit the Replay integration entirely rather than zeroing its sample rates (deviation 6).
- Replace the flat traces_sample_rate=0.1 at backend/logging_setup.py:41 with a traces_sampler: meaningful sampling on /ask and the fan-out routes, 0 on /api/health and statics (§17.3 deviation 7, §17.5).
- Add the §17.4 init-ordering comment at BOTH backend/logging_setup.py:37 and asgi.py:36 — sentry_sdk.init() currently runs before FastAPI() only by accident of import order, and a future reshuffle would disable instrumentation with no visible symptom.
- Enable only the §17.5 products. Do not enable Profiling, Sentry Logs, Session Replay, User Feedback, or Crons.

STEP 0 — reconcile with Prompt 6 before writing anything:
a) Read backend/logging_setup.py and backend/cost_meter.py as they stand now and report to me what Prompt 6 actually changed.
b) If Prompt 6 added a daily budget-alert hook to cost_meter.py: STOP and report it. plan.md §16.3 L3 / Prompt 29b builds a daily USD *ceiling* on the same module. Two independent daily-budget mechanisms is the §2 divergent-duplication anti-pattern in the layer that protects the spend. Do not delete either yet — tell me what exists so I can decide which one 29b subsumes.
c) Confirm whether Prompt 6 instrumented the two SYNC model call sites at backend/claude.py:509 and backend/claude.py:1862 (only the async paths at :1556 and :1642 had record_llm_call before). If it did not, add them now — an incomplete token ledger makes the §16 cost breaker under-count real spend.

CONTEXT THAT DRIVES EVERY DECISION BELOW: Sentry's free Developer plan is 5,000 events/month. That is a small number for a public site. It means (i) filtering is a prerequisite, not polish, and (ii) POST /api/client-errors — currently unauthenticated, unrate-limited, accepting an 8000-char stack and forwarding it to Sentry (backend/routes_devtools.py:135) — is not just a cost vector, it is a way to BLIND your error monitoring for a whole month with ~5,000 requests. Treat it as a security defect.

STEP 1 — browser SDK, loaded defensively:
- Load the Sentry Browser SDK from Sentry's CDN with an SRI integrity hash (§7g requires SRI on all CDN assets). Mirror the graceful-degradation pattern already used in static/js/motion.js: if the CDN fails, the site must work normally and nothing may throw.
- New env var SENTRY_DSN_BROWSER, rendered into the template. It is public by design (that is what a browser DSN is) — but it must be a TRUE no-op when unset: no script tag emitted, no network call, no console warning. Mirror the discipline in backend/logging_setup.py:33-47.
- environment and release MUST match the Python side exactly: VERCEL_ENV and VERCEL_GIT_COMMIT_SHA. If frontend and backend disagree on release, you cannot correlate a browser error to the deploy that caused it, which is the main reason to have both.
- tracesSampleRate: 0 initially (errors only). Session Replay: OFF. Both are quota and §8.D privacy decisions, not preferences — document why in a comment.

STEP 2 — privacy, and this one is specific to Sh'elah (§8.D + §8.B):
- sendDefaultPii: false on the browser SDK; confirm the same on the Python init.
- beforeSend / beforeBreadcrumb MUST scrub the user's question text and any /ask request or response body from breadcrumbs, XHR breadcrumbs, and event payloads. Halachic questions are frequently deeply personal — medical, marital, mental-health, abuse-adjacent (this is exactly the material §8.B's safety classifier exists to route). That text must never reach a third-party error tracker. Write a test that submits a question through the instrumented path and asserts the question string appears nowhere in the captured event.
- Scrub URL query strings and any Clerk token from breadcrumbs.

STEP 3 — noise filtering (protects the 5K/month quota):
- beforeSend drops: ResizeObserver loop errors, browser-extension-origin stack frames, network aborts from user navigation, and errors from non-shelah.org origins.
- Client-side dedupe/throttle so one looping component cannot emit unbounded events from a single session.

STEP 4 — demote /api/client-errors to a fallback (do NOT delete it):
- Its job becomes: catch errors from sessions where SENTRY_DSN_BROWSER is unset or the CDN script failed. Deleting it outright loses coverage in exactly the case you most want it.
- Harden it per §16.4: require same-origin, cap `stack` far below the current 8000 chars, drop the _extract_client_ip() value from the Sentry context (that value is forgeable until §16 D2 lands — see plan.md §16.1 D2), and put it in the tightest rate-limit class. If Prompt 29a has already merged, use its policy table; if not, add a TODO referencing the class name so 29a wires it up rather than inventing a second mechanism.

STEP 5 — CSP (this will silently break everything if skipped):
app.py:1175's Content-Security-Policy currently has no Sentry host in either directive. Add the Sentry CDN host to script-src and the ingest host to connect-src. Verify in a real browser that no CSP violation is logged and that a deliberately-thrown test error actually arrives — a CSP-blocked SDK fails quietly, which is the worst possible failure mode for error tracking. Coordinate with Prompt 11 (§7g) so the two CSP edits do not conflict; if Prompt 11 has not run, leave a clear comment marking this block as owned by §7g.

STEP 6 — docs:
Add a "Sentry project setup" section to docs/OBSERVABILITY.md covering: the two projects (Python/FastAPI + Browser JavaScript) and why FastAPI rather than Flask (asgi.py:559 is the entrypoint; Flask is mounted inside it at :556), the two env vars and which Vercel environments they belong to, the 5K/month quota math, the required dashboard settings (per-key rate limits, spike protection, inbound filters, IP scrubbing), and the fact that source maps are deferred until Prompt 11 gives the frontend a build step.

VERIFY: pytest green; the question-scrubbing test passes; SENTRY_DSN_BROWSER unset is a byte-identical no-op page; a forced error reaches Sentry in a preview deploy with correct release + environment; no CSP violations in console; graphify update .. Report what STEP 0 found before doing anything else.
```

## Prompt 7 — §7c: docs pass

```
Read plan.md §7 "Docs pass", §8 "Where this is written down", and §10.1. Write/refresh documentation only (no code changes):

- Update docs/SERVICE_ARCHITECTURE.md to the post-refactor module layout (text_engine, search_provider, ask_pipeline, blueprints).
- Create docs/API.md (every route: method, path, params, auth, response shape — derive from the blueprints and asgi.py), docs/OBSERVABILITY.md (logging/request IDs/Sentry/cost meter/circuit breakers), docs/FRONTEND.md (template/static structure, theming, motion rules).
- A complete environment-variable table (scan the code for every os.environ/os.getenv read; mark required vs optional; never include real values).

Cross-link from README. Keep the style of existing docs/.
```

## Prompt 8 — §7d: motion overhaul

```
Read plan.md §7 "Motion overhaul" and .agents/ENGINEERING_RULES.md (Framer Motion + UI/UX sections are mandatory). Implement the motion system:

- Vanilla `motion` (motion.dev) on the current non-React surfaces; define a motion token layer (durations 150–250ms ease-out, spring configs) in CSS custom properties.
- Zero new @keyframes outside the token layer; migrate existing ad-hoc animations into it.
- Honor prefers-reduced-motion everywhere (guard every animation).
- Micro-interactions per ENGINEERING_RULES: hover/focus-visible/active/disabled feedback on interactive elements; animate only transform/opacity.
- If any React surface exists, use Framer Motion with AnimatePresence + variants + layoutId per the rules; otherwise document the pattern for future React islands.

Verify no CLS regressions and both themes. No backend changes.
```

## Prompt 9 — §7e: dark mode overhaul

```
Read plan.md §7 "Dark mode overhaul" and .agents/ENGINEERING_RULES.md. Replace the ~570 scattered `body.theme-dark` overrides with a semantic token layer:

- Define semantic CSS custom properties (--surface, --surface-raised, --text-primary, --text-muted, --border, --accent, states, etc.) with light values on :root and dark values under the theme class AND @media (prefers-color-scheme: dark) for first paint.
- Add a no-FOUC head script that applies the stored/system theme before first paint.
- Migrate components to tokens incrementally, deleting theme-dark overrides as each component converts; finish with zero scattered overrides (grep-verified).
- AA contrast audit (≥4.5:1 text, ≥3:1 UI) in BOTH themes; never color-only state.
- Use 21st.dev component patterns as the QA baseline for the converted components.

Work in reviewable chunks by template/component group. Verify visually in both themes; no functional regressions.
```

## Prompt 10 — §7f: loading-states overhaul

```
Read plan.md §7 "Loading-states overhaul" and .agents/ENGINEERING_RULES.md. Build the token-driven loading design system (light + dark):

- Skeleton components (shimmer via transform/opacity only) for every region currently showing "Loading..." text; zero layout shift (reserve space).
- Rebuild the AI answer loading animation off hardcoded hex onto the theme tokens (Prompt 9's layer) with dark variants; fix any timer/interval cleanup leaks on unmount/navigation.
- role="status" + aria-live="polite" on loading regions; reduced-motion guards on all shimmer/pulse effects.

Verify in both themes, at 320px and desktop widths, with reduced-motion enabled.
```

## Prompt 11 — §7g: frontend platform fixes (Tailwind build, CSP, SRI, SEO, RTL)

```
Read plan.md §7 "Frontend platform fixes" and §8.C.3–C.4. Implement:

1. Replace the production Tailwind CDN JIT compiler with a proper build step (tailwind.config.js exists): compiled CSS committed or built in CI, CDN script removed from templates, then remove 'unsafe-eval' from CSP.
2. Harden CSP: nonce-based script-src, add Strict-Transport-Security and Permissions-Policy, drop deprecated X-XSS-Protection (headers live in app.py middleware).
3. SRI hashes + version pins on every remaining CDN script/style.
4. Fonts: preconnect + woff2, confirm SILEOT loads efficiently.
5. SEO/OG metadata on public pages; dynamic lang/dir attributes for Hebrew (RTL) pages.

Verify every page still renders (no CSP console violations), scripts load with SRI, and the app works with the compiled Tailwind. This is zero-breakage work: test each template group before moving on.
```

---

## Prompt 12 — §8.A: legal documents & disclosures

```
Read plan.md §8 preamble and §8.A in full. Draft ALL legal documents as attorney-review scaffolding (mark each "DRAFT — pending attorney review", versioned + dated). These are templates rendered via the existing legal-page pattern (components/legal_topbar.html, legal_scripts.html), bilingual en/he like the current pages:

1. Rewrite templates/terms.html complete per §8.A.1 (fill missing §6/§10/§12; all listed clauses: eligibility/age 13+, AI-output disclaimer, not-advice clause, IP/attribution, indemnification, liability cap + AS IS, assumption of risk, dispute resolution placeholder for counsel, DMCA, termination, severability, contact; Last-updated + version).
2. Expand templates/privacy.html per §8.A.2 (data inventory from the actual code: Clerk identity, Supabase bookmarks/preferences/memory summaries, IP for rate limiting, error logs, AI cost logs; purposes/legal bases; processor list; transfers; retention; rights + mechanism; DNS/GPC; children 13+; localStorage disclosure; breach commitment).
3. New templates/ai-disclosure.html per §8.A.3 (LLMs used, RAG at high level, hallucination risk, not-p'sak, consult-your-rabbi, EU AI Act interaction disclosure).
4. New templates/acceptable-use.html, templates/dmca.html, templates/accessibility.html, templates/licenses.html per §8.A.5–A.8.
5. THIRD_PARTY_LICENSES.md at repo root (Sefaria per-text caveat, Hebcal, Wikipedia CC-BY-SA, SILEOT font, MIT JS/CSS libs).
6. Wire versioned click-through consent through the existing /api/accept-legal + accept_legal route: store ToS+Privacy version + timestamp per user; re-prompt on version bump.

Add routes/links for the new pages following the existing legal-page routing. Tests for the consent-version storage. Full pytest green.
```

## Prompt 13 — §8.B: AI liability & religious-guidance safety

```
Read plan.md §8.B in full. Implement the religious-guidance safety layer:

1. Persistent, unavoidable disclaimer in the answer UI: every AI answer renders a visible "Educational information, not a halachic ruling — consult your rabbi" banner (the backend RABBI_FINAL_RULING_FOOTER already exists — surface it prominently; non-dismissible on first use). Bilingual en/he, both themes, token-styled.
2. Extend the scope guardrails in backend/claude.py (_detect_out_of_scope_subject, is_prohibited): medical, legal, mental-health, and dangerous-practice questions dressed as halacha get a referral response (professional + rabbi), never a "ruling".
3. No-impersonation audit: review all system prompts for first-person rabbinic-authority phrasing; add an explicit "never claim to be a rabbi or issue p'sak" rule.
4. Sensitive-topic escalation pattern (minors, abuse, self-harm, medical emergencies): documented referral template, never procedural instruction.
5. Tighten provenance labeling: "internal AI knowledge" answers clearly marked lower-confidence via _build_source_attribution_note.
6. Defensibility logging: request/response/prompt-version metadata retained per the §8.A.2 retention schedule.

Tests: each guarded category returns referral-not-ruling; ordinary halachic Q&A unchanged (no over-refusal regression). Full pytest green.
```

## Prompt 14 — §8.B-AGE: 13+ age gate & age-appropriate output layer

```
Read plan.md §8.B-AGE in full and implement it exactly:

1. AGE_APPROPRIATE_DIRECTIVE constant (single source of truth) appended to both CORE_SYSTEM_PROMPT and SIMPLE_SYSTEM_PROMPT in backend/claude.py, with the content rules in §B-AGE.1 (13+ reader assumption; clinical educational language for sensitive halacha; principles + sources + "learn practical details with a rabbi/teacher/parent"; no explicit detail; scholarly depth preserved).
2. classify_safety(query) step (extending _detect_out_of_scope_subject) run before synthesis, returning ok | sensitive_intimate | medical | mental_health_or_self_harm | abuse_or_minor_safety | dangerous_or_illegal, with the routing behavior in §B-AGE.2.
3. Structured-output fields age_safe + safety_class added backward-compatibly (defaults true/"ok" when absent); route/UI renders referral template when safety_class != ok.
4. Post-generation output check extending validate_model_output: scan for explicit-content markers; on hit, replace body with principles-plus-referral template. Never rely on the prompt alone.
5. Applies across all modes (balanced/practical/sources/strict); bilingual en/he templates matching render_structured_markdown.
6. Age gate at sign-up: 13+ attestation (EU 16+ default) tied to Clerk, stored with the consent record; under-age attempts blocked.
7. docs/AGE_AND_SAFETY_POLICY.md + add the standing rule to .agents/ENGINEERING_RULES.md.

Tests per §B-AGE: tests/test_safety_classifier.py (routing per class; medical/self-harm/abuse never get a "ruling", always a referral; dangerous refuses); intimate-topic fixtures return principles+citations+referral; regression that ordinary Shabbat/kashrut/brachot questions keep full depth; Hebrew parity. Full pytest green.
```

## Prompt 15 — §8.C: security hardening

```
Read plan.md §8.C in full. Execute the security hardening pass:

1. Secrets: scan git history for committed secrets (gitleaks/detect-secrets); confirm .env never tracked; report any key that ever touched a commit (flag for rotation — do not rotate yourself); verify FLASK_SECRET_KEY handling requires a strong value in prod; confirm anon vs service-role Supabase key separation (service-role never reaches the client).
2. Supabase RLS: make STRICT_SUPABASE_RLS the enforced default in prod; add the /api/devtools/rls-audit check to CI; verify every user-data table (bookmarks, preferences, memory summaries) denies cross-user access using scripts/sql/SUPABASE_RLS_POLICIES.sql.
3. CSP/headers: (coordinate with Prompt 11) remove 'unsafe-eval', nonce-based script-src, HSTS, Permissions-Policy, drop X-XSS-Protection.
4. Supply chain: SRI/pin CDN scripts; add pip-audit to CI; extend .pre-commit-config.yaml with secret scanning.
5. Input validation & abuse: audit all route inputs (coordinate bounds, ref decoding, payload caps); confirm both rate limiters active in prod; document the serverless per-instance-limit weakness and the shared-store option.
6. AuthN/Z review: _verify_clerk_token path, CLERK_ENFORCE_AUTH on for protected routes in prod, session cookie flags (Secure/HttpOnly/SameSite) in apply_session_cookie_policy.
7. Finish with a written findings report in docs/SECURITY.md including a vulnerability-report contact.

Zero-breakage: full pytest green after each change; nothing behavior-changing ships without a test.
```

## Prompt 16 — §8.D: privacy operations

```
Read plan.md §8.D in full. Implement privacy operations:

1. DSR flow: self-serve "download my data" (JSON export of the user's Supabase rows) and "delete my account + data" in settings — deletion cascades through all user Supabase tables AND Clerk (use Clerk's deletion API); plus a documented email-intake procedure in docs/PRIVACY_OPERATIONS.md.
2. Consent records: (with Prompt 12) version + timestamp stored at acceptance via /api/accept-legal; re-prompt on material version change.
3. docs/PRIVACY_OPERATIONS.md: DPA checklist (Anthropic, Google, Clerk, Supabase, Vercel, Sentry — link each click-through DPA), Records of Processing (GDPR Art. 30) table, and a basic DPIA in docs/DPIA.md covering automated religious guidance.
4. Retention enforcement: a scheduled job (Vercel cron or Supabase scheduled function) that actually deletes data past the retention windows defined in the Privacy Policy.
5. Breach response plan in docs/PRIVACY_OPERATIONS.md: roles, GDPR 72h timeline, notification templates.

Tests: export returns exactly the user's own data (RLS respected); deletion removes all rows + is idempotent; retention job deletes only past-window rows. Full pytest green.
```

## Prompt 17 — §8.E: reliability, observability & operations

```
Read plan.md §8.E in full (overlaps Prompt 6 — skip anything already done). Implement:

1. Verify Sentry-in-prod, request-ID logging, cost metering on every model call, and circuit breakers on all external calls (Phases 3 / Prompt 6 outputs) — close any gaps.
2. Uptime: document external monitor setup for /api/health and /api/stack/health with alerting (provider-agnostic instructions in docs/RUNBOOKS.md).
3. Graceful degradation: end-to-end verify the fail-open local-corpus path; audit user-facing error states — friendly messages, no stack traces or internals leaked on any 4xx/5xx.
4. Backups: document Supabase backup cadence + restore procedure + customs/config data export in docs/RUNBOOKS.md.
5. Runbooks: incident response, rollback (Vercel preview → promote), deploy checklist — all in docs/RUNBOOKS.md.
6. Cost ceiling: rate limits verified + document hard monthly spend caps on Anthropic/Google dashboards; wire the cost_meter daily budget alert.

Tests for the degradation path (all providers circuit-open still returns a well-formed answer payload). Full pytest green.
```

## Prompt 18 — §8.F: quality, accessibility & content integrity

```
Read plan.md §8.F in full. Implement:

1. CI coverage gate: GitHub Actions workflow running the offline pytest suite with a coverage threshold (start at current coverage, ratchet up); include the RLS audit (Prompt 15) and pip-audit.
2. Accessibility: add axe-core or pa11y automated checks to CI for key pages; fix violations to WCAG 2.1 AA; publish templates/accessibility.html (from Prompt 12) with real audit results.
3. Content integrity: write docs/CONTENT_QA.md stating plainly the app is not rabbinically supervised (or documenting the advisor's scope if one exists) and the review process for corpus/customs data.
4. Localization: verify dynamic lang/dir for Hebrew pages (with Prompt 11), and that machine-translated content is labeled as such, never presented as authoritative.

Full pytest green; CI workflow passes on a test branch.
```

## Prompt 19 — §8.G + §8.H: business scaffolding & launch gate

```
Read plan.md §8.G and §8.H. This is documentation only (the G items are attorney/accountant decisions — document, don't decide):

1. docs/LAUNCH_CHECKLIST.md: reproduce the §8.H checklist verbatim as a living tracked checklist, each line linked to the doc/PR/test that proves it, with current status honestly marked.
2. Add a "Business & compliance — needs human counsel" section listing §8.G items (entity + E&O/cyber insurance, trademark clearance for "Sh'elah", EU AI Act / EAA posture, future-monetization triggers) with plain-language explanations of why each matters.
3. Cross-link from README and docs/SECURITY.md / PRIVACY_OPERATIONS.md.

No code changes.
```

---

## Prompt 20 — §9: agentic AI tool-use layer

```
Read plan.md §9 in full (9.1–9.7) before any code. Implement the agentic tool-use layer behind the AI_AGENTIC_TOOLS flag (env-default OFF; when off, behavior is byte-for-byte today's pre-fetch RAG path).

1. backend/ai_tools.py — pure-function tool registry with JSON schemas (typed params with bounds: lat/lon ranges, date formats, community enum; descriptions encode the hierarchy "texts first, web last"). Wrap ONLY existing backend functions per the §9.2 and §9.2b tables (search_judaic_texts, get_text_by_ref, search_responsa_external, get_zmanim, get_hebrew_date, get_parasha, get_omer, get_holidays, get_daily_study, web_search, lookup_word_meaning, translate_text, get_commentaries, search_community_customs, get_community_profile, browse_library, search_library, get_prayer_text, get_daily_zmanim_summary, convert_measurements [new small deterministic shiurim table — Chazon Ish vs R' Chaim Naeh], calculate_hebrew_date_math, format_source_citation). Imports backend.* only, never app. Do NOT expose export_answer/save_bookmark as model tools (UI actions only — agent loop stays read-only).
2. web_search scope LOCKED: async_search_wikipedia + the existing allowlist in search.py only. No new provider.
3. Orchestrator-level last-resort gate (§9.3.2): web_search is not in the tool list offered to the model until a texts/library search returned insufficient results this turn. Answers that used web_search get include_web_warning=True and tier-4 attribution; never sole basis for a ruling.
4. Agent loop in backend/ask_pipeline.py (§9.4): tool_use → execute (parallel via asyncio.gather + to_thread, reusing _THREAD_POOL) → tool_result → repeat; hard cap 4 rounds; per-tool timeouts; location from stored user preference else "location required" result (never guess).
5. Integration (§9.5): each tool call is timed out, narrowly caught, health.record_success/failure'd, and fail-open; circuit-open providers hidden from the model that turn; cost_meter records every round; the §B-AGE safety classifier runs BEFORE the loop; tool results (esp. web_search) sanitized as untrusted before re-injection.
6. Tests (§9.6): tests/test_ai_tools.py (shape + param validation per tool, offline) and tests/test_agent_loop.py covering scenarios (a)–(g) in the plan, including flag-off reproducing today's behavior exactly.
7. docs/AI_TOOLS.md + the standing rule added to .agents/ENGINEERING_RULES.md (§9.7).

Full pytest green with the flag both on and off. graphify update . Stop and report; do not enable the flag by default.
```

---

## Prompt 21 — §10.1: README full update

```
Read plan.md §10.1 and the current README.md. Update README.md per the 10-point list:

badges row (MIT, Python version, Vercel, tests/CI once present, "not legal/halachic advice"); screenshots/demo-GIF placeholders with TODO markers + live demo link; feature list refresh (community-lens answers, bilingual EN/HE + RTL, PWA/offline, dark mode; agentic tools only if §9 is merged); new "AI & safety" section (RAG overview, source hierarchy texts→poskim→whitelisted→web-last-resort, educational-only disclaimer, 13+ posture, link to AI Disclosure); keep the architecture diagram current + link docs/SERVICE_ARCHITECTURE.md; RECONCILE the env-var table against the actual code (fix wrong names like RATE_LIMIT_PER_MIN/RATELIMIT_STORAGE_URI to the real RATE_LIMIT_ASK/RATE_LIMIT_DEFAULT etc.; required vs optional; no real values); expanded Testing section (offline pytest, coverage gate, golden-master approach); CONTRIBUTING.md + CODE_OF_CONDUCT.md (create both, standard templates) with links; Credits & licenses section pointing to THIRD_PARTY_LICENSES.md; SECURITY.md link with vulnerability reporting.

Docs only, no code changes. Verify every env var name in the table against an actual os.environ/os.getenv read in the code.
```

## Prompt 22 — §10.2: MIT LICENSE + NOTICES

```
Read plan.md §10.2. Add licensing files:

1. LICENSE at repo root: the exact MIT text in plan.md §10.2 (Copyright (c) 2026 Akiva Yevdayev).
2. THIRD_PARTY_LICENSES.md (if not created by the §8.A prompt — otherwise verify/extend): every third-party source with license + attribution — Sefaria (verify per-text licensing caveat noted), Hebcal, Wikipedia (CC-BY-SA share-alike note), SILEOT.woff font license, DaisyUI/Tailwind/marked/DOMPurify (MIT notices). State explicitly that content retrieved from Sefaria/Hebcal/Wikipedia remains under its own license and is NOT relicensed by this project's MIT license.
3. Set the license field in package.json (and any Python packaging metadata) and add the MIT badge to README.

No code changes.
```

## Prompt 23 — §10.3: GitHub profile update — ✅ DONE 2026-08-21 (item 1 only; items 2-4 closed as descoped)

```
PREREQUISITES (confirm with me before starting): my GitHub username, and either the gh CLI authenticated or the Claude-in-Chrome connector. This task involves PUBLIC writes — show me each change and get explicit approval before publishing anything.

Read plan.md §10.3. Then:
1. Create/update the profile README repo (<username>/<username>, README.md) using the draft in plan.md §10.3 with <username> filled in and tone-checked with me first. ✅ DONE 2026-08-21 — username confirmed (akivayevdayev-debug), content approved verbatim by the operator (differs from plan.md §10.3's placeholder draft — the operator's own copy is what's live), repo created via `gh repo create ... --public --push` (didn't exist before), pushed and confirmed rendering via `gh repo view`. See plan.md §10.3 for details.
2. Pin the Sh'elah repo (+ up to 5 others I choose). Closed, not done — the operator only requested item 1 and marked this prompt complete without asking for this. Reopen explicitly if wanted later.
3. Polish the Sh'elah repo: description, topics (judaism, halacha, torah, flask, fastapi, ai, rag, sefaria, vercel), website link; verify README + LICENSE render. Closed, not done — same as above.
4. Optional bio/link update — propose, don't apply without approval. Closed, not done — same as above.
```

## Prompt 24 — §11: Vercel deploy fix verification (fix already implemented) — ✅ DONE 2026-08-21 (operator-verified live preview deploy)

```
Context: plan.md §11 Option A is already implemented — api/index.py exists (re-exporting the ASGI app) and vercel.json uses modern functions + rewrites + headers. Your job is the verification checklist ONLY (plan.md §11 "Verification"):

1. Validate vercel.json parses and matches the Option A spec. ✅ DONE 2026-08-21 — vercel.json parses; note it has no `rewrites` key today (only functions/regions/crons/headers, re-confirmed live during this pass) — see the still-open STEP 2 discrepancy in Prompt 42/§29.
2. Deploy to a PREVIEW URL (never straight to prod): vercel deploy from the repo root. ✅ DONE 2026-08-21 — operator ran a real `vercel deploy` from their own terminal (the Vercel CLI in this sandbox was previously blocked by an unrelated PATH collision with a same-named pip package, fixed the same session — see plan.md §11's note) and confirmed the preview deploy live.
3. On the preview: confirm / renders; /api/async/health returns ok; /static/css/ai.css serves from CDN (response headers show it was NOT function-served) with the Cache-Control header; /ask works end-to-end; cold start succeeds (the import chain api/index.py → asgi.py → app.py → backend/* resolves — if a root-relative import fails, the sys.path.insert in api/index.py is the fix, keep it). ✅ Operator confirmed the live preview works ("live preview is verified"); no per-item breakdown was captured in this session, so if any individual sub-check (CDN headers specifically, cold-start import path specifically) later turns out to have been missed, re-open just that sub-item rather than assuming full parity was independently re-verified line-by-line.
4. Report results. Only promote to production after I confirm. Operator confirmed the preview and asked to close this prompt; production-promotion is a separate, not-yet-requested action.
```

---

## Prompt 25 — §12 Phase 6: product surface, content & growth layer — ✅ DONE 2026-08-21 (search filter UI closed as descoped; remaining gaps tracked in Prompt 42/§29)

```
Read plan.md §12 in full, including the §12.0 claim-triage table (items marked wrong/already-shipped are NOT in scope — do not build user accounts, answer modes, bookmarks, or export; they exist). Prerequisites: §7 frontend overhauls (tokens, skeletons, source-box fix) and §8.A legal pages merged. All new routes go in backend/ blueprints, never app.py; all UI follows .agents/ENGINEERING_RULES.md.

1. §12.1 About page: templates/about.html on the legal-page pattern (legal_topbar, bilingual en/he, both themes, tokens); honest solo-developer content per the plan; links to (never restates) ToS/Privacy/AI-Disclosure; nav/footer link added.
2. §12.2 Help + glossary: /help user guide (asking a good sh'elah, answer modes, reader features incl. existing Shul Mode, community lens, zmanim tour); glossary page statically generated from a build-time JSON seeded via the existing lexicon engine — no per-view API fan-out. Learning paths are deferred — skip.
3. §12.3 Discoverability: reader breadcrumbs from the get_library_index hierarchy (nav[aria-label="breadcrumb"]); expose search_library's existing category/corpus filters in the search UI; citation deep-links from ai_cited_sources into the reader at the ref (build on the §7 delegated-listener source-box container); verify the customs data path has Phase-3 circuit-breaker fail-open end-to-end and skeleton loading. Semantic search is deferred — skip.
4. §12.4 Feedback loop: per-answer helpful/not-helpful + optional ≤500-char comment; new blueprint endpoint; Supabase answer_feedback table (nullable user_id, question hash, mode/lens/language/fallback/safety_class metadata, verdict, comment, ts) with RLS; rate-limited + sanitized; "no personal details" microcopy; devtools feedback-digest view.
5. §12.5 SEO/analytics: robots.txt (disallow /api/, devtools) + generated sitemap.xml of stable public routes, served static — verify they bypass the §11 rewrite (extend the negative-lookahead if needed); JSON-LD WebSite+Organization on home (no schema spam); consent-gated cookieless analytics per §8.A.4 (Vercel Web Analytics or Plausible — NOT Google Analytics by default), documented in the Privacy Policy; light keyword/copy pass.
6. Respect the §12.6 deferred registry — build none of it.

Tests per §12.7: route tests (200s, content-type, bilingual), feedback endpoint (happy/rate-limit/injection/RLS), citation deep-link href, sitemap schema + JSON-LD validity. Full pytest green; graphify update . Stop and report.
```

---

## Prompt 26 — §13 Phase 7: SonarCloud static-analysis debt — ✅ DONE 2026-08-22 (3 of 51 hotspots refactored; dialog double-handling fix applied but not committed; remaining gaps tracked in Prompt 44/§32)

```
Read plan.md §13 in full. Prerequisite: §4 Phases 1–4 are merged (they are — app.py is down to ~1,910 lines).

1. §13.1 Re-run the SonarCloud scan and diff the python:S3776 (cognitive complexity) list against the 67 pre-refactor instances. Report which hits disappeared as a side effect of the text_engine/search_provider extraction.
2. For whatever remains (expected: sefaria_library.py catalog/index builders, a few routes_library.py handlers), refactor them ONE AT A TIME, each as its own reviewed change: guard clauses first, then flatten nested conditionals, then early returns. Test-anchor every extraction — suite green after each. Do NOT do a bulk 67-site pass.
3. §13.2 Convert #calendarModal, #chapterGridModal, #legalModal (templates/index.html) to native <dialog> with .showModal()/.close(). Audit for double-handling: native <dialog> already closes on Escape and provides ::backdrop — remove duplicate custom handlers. Ship as THREE separate commits, one per modal.
4. Interaction-test each modal in-browser (not just visually): open trigger, close button, backdrop click, Escape, focus returns to trigger on close, screen-reader announcement on open.

Full pytest green; graphify update . Stop and report.
```

---

## Prompt 27 — §14 Phase 8a: Vercel config corrections + startup-cost quick wins — ✅ DONE 2026-08-22 (found and fixed a reintroduced routing regression in the uncommitted vercel.json; remaining gaps tracked in Prompt 44/§32)

```
Read plan.md §14 in full (especially 14.1 for the billing model, 14.2, 14.4.1) and the §11 correction note. This is the low-risk quick-win pass. ZERO user-visible behavior change is the hard requirement.

1. §14.2.1 vercel.json: REMOVE the "memory": 1024 key — it is invalid under Fluid compute (build-time warning, ignored; Hobby memory is Vercel-managed). KEEP maxDuration but justify its value against the observed p99 of /ask (AI_TOTAL_BUDGET_SECONDS + fallback headroom); do not loosen it — a long ceiling means a hung request bills Provisioned Memory for its whole lifetime. Add "regions": ["iad1"] (or pdx1/cle1 — the cheapest at $0.128/CPU-hr, $0.0106/GB-hr) after confirming latency for the actual audience is acceptable. Do NOT add "fluid": true without first reporting the project's current dashboard Fluid state to me.
2. §14.4.1 Startup cost: app.py calls validate_all_customs_at_startup() at module import — this bills Active CPU on every cold start and delays the first response. Make it lazy or dev/CI-only (env-gated), skipped in production imports. Then audit remaining module-level work: convert large JSON corpus loads to lazy singletons (loaded on first use, cached for the instance lifetime — Fluid keeps instances alive so this is effective); keep hoisted regex compilation; defer heavy optional imports into the functions that need them.
3. §14.2.2 Report (do not perform) the dashboard actions I must take: Spend Management cap + Usage Notification thresholds; record the recommended values in docs/RUNBOOKS.md.
4. §14.2.3 Optional: an ignoreCommand that skips builds for commits touching only *.md/docs/**. State the trade-off in the commit message. Do NOT change anything about the production Git integration — newest-commit auto-deploy must keep working identically.

Verify: vercel.json parses and matches the Option-A shape minus memory; full pytest green; cold-start import chain still resolves (python -c "import app, asgi"); graphify update . Report before/after import-time work. Stop — do not start the cache-tier work (Prompt 28).
```

## Prompt 28 — §14 Phase 8b: CDN cache tiers, FOT & invocation reduction (the main win)

```
Read plan.md §14 in full, especially 14.3, 14.5, 14.6, 14.7, 14.8. Prerequisites: Prompt 27 merged, §4 Phase 4 complete (route ownership settled). This is the largest cost lever AND the one with real security risk — a mis-tiered route can leak one user's data to another via the CDN. Treat 14.3.1's Vary/auth rules as a security requirement, not a perf tweak.

1. BASELINE FIRST (§14.7.1): tell me to record 30 days of Vercel Usage figures per meter (Active CPU, Provisioned Memory, Invocations, Fast Origin Transfer) plus the cached-vs-uncached Requests ratio, before any change lands. Do not proceed past step 2 until I confirm the baseline is captured.
2. §14.3.1 Replace the blanket `/api/` → no-store branch in app.py::apply_response_cache_policy with an explicit cache-tier table. Classify EVERY route into exactly one tier per the plan's table (Immutable / Deterministic-by-date / Corpus-derived / Private-never-cache). Unclassified routes default to no-store (fail-safe). Use s-maxage (CDN) with short/zero browser max-age, plus stale-while-revalidate. Include DEPLOY_HASH in the cache key story so redeploys bust corpus-derived entries. Anything varying by user, auth state, or language gets the correct Vary header or the variant in the URL — and cacheable routes must be auth-independent by construction. /ask stays no-store permanently (§14.8).
3. §14.3.2 Verify static assets never reach the Python function on a preview deploy (headers show edge-served); extend the §11 rewrite negative-lookahead if §12.5's robots.txt/sitemap.xml have landed.
4. §14.3.3 Client-side invocation audit: find and fix polling loops, interval re-fetches, duplicate page-load fetches, search-as-you-type without debounce, and service-worker revalidation storms.
5. §14.5 Trim Fast Origin Transfer: verify _compact_ai_sources is applied on EVERY /ask return path including both asgi.py fallback branches; drop fields the UI never renders; confirm no debug/echo fields ship in prod; confirm no double-compression.
6. §14.6 Invocation/"stay inactive" rules: confirm the repo contains NO cron jobs, warmers, or keepalive pings. Reconcile the §8.E.2 uptime monitor conflict — recommend a static-asset primary check plus a low-frequency (5-minute, not 30-second) deep check on /api/health, and make /api/health cheap by default (no upstream fan-out or DB round-trip unless an explicit deep-check flag is passed). Document intervals in docs/RUNBOOKS.md. Audit that _store_user_memory_summary/_store_ask_history stay in-request — do NOT convert them to fire-and-forget background work (it extends Provisioned Memory billing).
7. §14.7.3 Tests (the regression net): assert the EXACT Cache-Control value per route tier, and assert no authenticated/user-scoped route is ever `public`. Add cross-user leakage checks for every newly cached route.
8. §14.7.5–6 Add the cache-tier + no-warmers rules to .agents/ENGINEERING_RULES.md; write docs/VERCEL_COST_OPTIMIZATION.md (meter model, tier table, baseline + post-change numbers, monitoring intervals, spend-cap values).

Verify on a PREVIEW deploy before any promotion: cacheable routes return expected headers and show a CDN HIT on second request; /ask and all authed routes show no-store; statics edge-served; fetch-as-user-A-then-user-B shows no cross-user leakage; cold start succeeds. Full pytest green; graphify update . Re-measure usage at 7 and 30 days post-merge and report the delta per meter.
```

## Prompt 29a — §16 Phase 9a: fix the rate limiter (stop the bleeding) — ✅ DONE 2026-08-22

> **✅ Done 2026-08-22.** The reduced scope §16.8.2 identified (STEP 1 unified middleware, STEP 3 Upstash store, STEP 5 remove the dead limiter) shipped: `backend/rate_limit.py` is the single enforcement point for both transports, `RATE_LIMIT_REDIS_URL`-backed with a loud-warning in-process fallback, Flask-Limiter and asgi.py's independent limiter both deleted. Full `pytest -q` green (91% coverage), clean `import app, asgi, backend.rate_limit`, `graphify update .` shows no new cycle. See `plan.md` §16.9 for the full reconciliation and **Prompt 43 / `plan.md` §31** for what this pass found but left for follow-up (most notably: the fail-open/fail-closed posture has zero test coverage — read §31.1 before touching `backend/rate_limit.py` again). The banner below is kept for lineage; do not re-run the prompt body.
>
> **⚠️ Correction 2026-08-19 — do not run this prompt as written.** Re-verified against the working tree; `plan.md` §16.8 has the full table. Three of the four defects this prompt targets have changed state since it was authored on 2026-07-31:
>
> - **STEP 0(b) and STEP 2 (D2 — forgeable key) are DONE.** There is now one canonical `_resolve_client_ip()` in `backend/helpers.py`, shared by both transports (`asgi.py:35,73-76`), with zero `CF-Connecting-IP` reads anywhere in application code and a regression test carrying an explicit "must never regress" comment (`tests/test_helpers.py:37-64`). **Running STEP 2 would mean editing code that is already correct.** Delete these steps; do not "fix" them again.
> - **STEP 4 (D4 — unlisted routes unlimited) is DONE** for the Flask surface: `RATE_LIMIT_DEFAULT = ["60 per minute"]` (`app.py:805`), applied at `:835-836`. STEP 4's *enumeration test* is still worth building — it is the part that stops future routes shipping unlimited — but the defect itself is closed.
> - **STEP 0(a) and D1 no longer reproduce.** `/ask` does return 429 today, because `asgi.py` grew its own in-process limiter (`asgi.py:62-63,86-106`). It was closed **by the exact route §16.3 rejected** — two independent limiters rather than one middleware. If STEP 0(a) is run as written it will fail to reproduce, and per the prompt's own instruction that means *stop and correct the plan*, which §16.8 has now done.
>
> **What is actually left:** STEP 1 (single ASGI middleware), STEP 3 (Upstash shared store — D3, still fully open; `app.py:820` still defaults to `memory://` and `app.py:810` carries its own "KNOWN GAP" marker), STEP 5 (remove the second limiter), STEP 6 (body caps — **also already shipped**, verify rather than rebuild), plus one new step §16.8.2 adds: **a test asserting the two limiters' effective policy is identical, so `app.py:809`'s `RATE_LIMIT_ASK = "20 per minute"` and `asgi.py:62-63`'s `_RATE_LIMIT_WINDOW_SECONDS`/`_RATE_LIMIT_MAX_REQUESTS` cannot drift apart while they both exist.** Land that test even if the middleware unification does not happen in the same pass — it is cheap and it is the guard against the failure mode that produced this situation.
>
> The prompt body below is kept unedited as the historical spec for STEP 1/3/5. Read §16.8 first, then run only the steps above.

```
Read plan.md §16 in full — especially 16.1, 16.3 (L2), 16.4 and 16.6 — plus §2, §3, §5, §6. This is a SECURITY fix, not a perf tweak. The /ask endpoint currently has zero enforced rate limiting in production and a rate-limit key the client can forge. Do not soften or partially apply any step.

STEP 0 — PROVE THE BUGS BEFORE FIXING THEM (golden-master rule, non-negotiable):
Write tests/test_rate_limiting.py with characterization tests against the REAL asgi.app (not the bare Flask app), asserting current broken behavior:
  a) N+1 rapid POST /ask calls all return 200, never 429 — proving the FastAPI route at asgi.py:187 shadows Flask's @maybe_limit'd ask_question at app.py:1372-1375 because the Mount at asgi.py:556 is registered after it.
  b) Sending a different CF-Connecting-IP header per request yields a fresh bucket — proving _rate_limit_key (app.py:816-820) trusts a client-supplied header.
Run them, confirm they pass against the CURRENT code, and report both results to me before writing any fix. If (a) does NOT reproduce, STOP and tell me — the §16 D1 diagnosis would be wrong and the plan needs correcting, not the code.

STEP 1 — one limiter, in the right place:
Create backend/rate_limit.py containing (i) the trusted-client-key function, (ii) the route-class policy table from §16.3, (iii) a pure ASGI middleware class. backend/rate_limit.py must NOT import app (§2 rule). Install it in asgi.py so it wraps BOTH the FastAPI routes and the Flask WSGIMiddleware mount — above everything, seeing 100% of traffic exactly once.

STEP 2 — trusted key (fixes D2):
Delete the CF-Connecting-IP branch from BOTH _rate_limit_key (app.py:816) and _extract_client_ip (app.py:1228). Trust order: x-vercel-forwarded-for -> x-forwarded-for -> x-real-ip -> peer address. Nothing else, ever. Add a comment citing Vercel's request-headers doc (Vercel overwrites x-forwarded-for and does not forward external IPs; CF-Connecting-IP is NOT platform-controlled on Vercel).

STEP 3 — shared store (fixes D3):
Storage via Upstash Redis over rediss:// from RATE_LIMIT_REDIS_URL. Failure posture is deliberately ASYMMETRIC and must be implemented exactly as §16.3 states: fail OPEN for library/text/calendar classes, fail CLOSED for the llm class, alarm to Sentry in both cases. Add a test for each posture with the store stubbed unreachable. Local dev with no env var falls back to in-memory with a loud startup warning.

STEP 4 — no unlisted routes (fixes D4):
Every route in app.py and backend/routes_*.py plus both asgi.py routes maps to exactly one class in the policy table. Unmapped -> the tightest anonymous default, never unlimited. Add a test that enumerates the live URL map and fails if any rule is unclassified — so future routes cannot silently ship unlimited.

STEP 5 — remove the dead limiter:
Delete Flask-Limiter usage from app.py (RATE_LIMIT_* constants, the Limiter construction at app.py:823-832, maybe_limit, the @maybe_limit decorator). Keep ONE 429 handler shape: JSON body with a stable machine-readable code plus a Retry-After header, served identically whether the request was going to hit FastAPI or Flask. Do not leave a second limiter behind as a fallback — that is the divergent-duplication anti-pattern from §2.

STEP 6 — body caps (§16.4):
Set MAX_CONTENT_LENGTH on the Flask app AND an independent byte cap in the ASGI middleware (the Flask setting does not protect asgi.py:187 — that is the whole D1 lesson generalized). Add an explicit character cap on the /ask question before tokenization. Tighten /api/client-errors: same-origin required, stack capped well below its current 8000 chars, telemetry class.

VERIFY: the STEP 0 tests now assert 429 / stable bucket and pass. Full `pytest -q` green. `python -c "import app, asgi, backend.rate_limit"` clean. `graphify update .` shows no new import cycle and no backend/* -> app edge. Confirm rate-limit keys are hashed before any logging (no raw IPs into Sentry, §8.D).

Do NOT touch the Vercel WAF or cost_meter.py — that is Prompt 29b. Stop and report.
```

## Prompt 29b — §16 Phase 9b: edge WAF rules + LLM cost circuit breaker — ✅ UNBLOCKED 2026-08-19 (Prompt 33a shipped)

> **Unblocked 2026-08-19.** The cost circuit breaker this prompt builds reads `ai_usage_log.cost_usd`. Prompt 33a fixed that column's price table (it now prices the production model `gemini-3.5-flash-lite`) and added a CI guard so a future model rename can't silently re-zero it. A breaker built on this ledger will now actually fire in production. Historical rows from before 2026-08-19 are still under-reported unless the operator runs `scripts/recompute_ai_usage_log_cost.py` — keep that in mind if this prompt's breaker logic looks at multi-day history, not just today's total. Clear to implement.

```
Read plan.md §16, especially 16.2, 16.3 (L1 and L3), 16.5, 16.6. Prerequisite: Prompt 29a merged. Two deliverables: (1) push mitigation to the edge where it is free, (2) guarantee the failure mode is "the chatbot is paused," never "the budget is drained" and never "the site is down."

PART A — Vercel WAF (I will click these; you produce the exact spec, since the CLI is not authenticated here):
Write docs/SECURITY.md with a WAF section giving me, verbatim and ready to enter in the dashboard, the rules from §16.3 L1. Hobby budget is 1 rate-limit rule + 3 total custom rules — spend it exactly as the plan allocates and explain why slot 4 stays empty.
  - Rate-limit rule: POST /ask, keys IP + JA4 digest, fixed window, action = rate limit with a persistent block on repeat offenders.
  - Custom rule 1: deny scanner paths (/.env, /.git/*, /wp-admin/*, /vendor/*, /phpmyadmin/*).
  - Custom rule 2: deny methods outside GET/POST/HEAD/OPTIONS.
  - Custom rule 3: reserved, empty, incident-response only.
Two things you must state explicitly in the doc: (a) deploy the rate-limit rule in LOG mode first, read one week of real Firewall traffic, then set the threshold from data — do not invent a number; (b) WAF counters are per-region, and this project's `"regions": ["iad1"]` pin in vercel.json is what makes a single counter correct — so if regions ever change, this rule must be revisited. Note that WAF-mitigated traffic incurs no CDN request, no Fast Data Transfer and no function invocation, and cross-reference §14.

PART B — cost circuit breaker in backend/cost_meter.py:
cost_meter.py today only records (estimate_cost_usd:40, record_llm_call:58) and enforces nothing. Add a global daily USD ceiling checked BEFORE the provider call, reusing the existing circuit-breaker primitive from backend/utils/search_provider.py — do NOT write a second breaker (§3 reuse rule). Ceiling from env with a conservative default. Ledger must be shared across Fluid instances (same store as Prompt 29a), not in-process.
When tripped: /ask serves a cached answer when one exists, otherwise a calm degraded payload. The Torah library, siddur, calendar, zmanim and search MUST remain fully functional — verify this with a test that trips the breaker and then asserts library/text/calendar routes still return 200.

PART C — frontend degraded state:
An honest, calm /ask-paused UI ("AI answers are paused for today — the full library below is unaffected"), plus a friendly 429 state with Retry-After countdown. Skeleton, zero CLS, WCAG 2.1 AA in BOTH themes, 44x44px targets, prefers-reduced-motion honored (.agents/ENGINEERING_RULES.md). Not a wall, not a raw JSON error.

VERIFY: pytest green including the new breaker + degraded-availability tests; graphify update .; docs/SECURITY.md complete enough that I can enter the WAF rules without asking a follow-up question. Report the recommended starting thresholds AND the reasoning, flagged as provisional pending the log-mode week.
```

## Prompt 29c — §16 Phase 9c: identity-aware quotas, Turnstile, mitigation observability — ✅ DONE 2026-08-24

```
Read plan.md §16.3 (L2 identity keys), 16.4, 16.6 Phase 9c. Prerequisites: Prompts 29a and 29b merged.

1. Identity-aware keys: key on the Clerk `sub` when authenticated, falling back to the trusted IP key when not. Rationale to preserve in a comment: IP-only buckets punish yeshivas, day schools and shuls behind a single CGNAT egress, which is a real and common shape of Sh'elah's audience. Signed-in users get a higher per-minute allowance plus an explicit daily /ask quota; anonymous traffic keeps the tighter IP bucket. Reuse backend/auth.py's existing Clerk verification — do not re-implement token parsing.

2. Turnstile (NOT Vercel BotID): gate anonymous /ask above a per-hour threshold behind Cloudflare Turnstile. State in the code comment and in docs/SECURITY.md WHY BotID was rejected — its server-side verification SDK is JavaScript-only and this is a Python function, so it cannot be wired into /ask at any plan tier. Turnstile's siteverify call must not block the event loop: async httpx or asyncio.to_thread (project rule). Feature-flag it (TURNSTILE_ENABLED) and make the whole path a no-op when unset so local dev and tests are unaffected.

3. Mitigation observability: one structured log line per mitigation with {tier: waf|middleware|breaker, class, key_hash, route, request_id}. Hash the key — never log a raw IP or a Clerk sub (§8.D privacy). Add a Sentry breadcrumb, not a Sentry event, for routine 429s so an actual attack is visible as a rate spike without drowning the error signal.

4. Docs correction: docs/API.md currently documents a 429 on /ask that did not exist before Prompt 29a — correct the historical claim and document the real limits, the 429 body shape, Retry-After semantics, and the full RATE_LIMIT_* / TURNSTILE_* / cost-ceiling env matrix. Add the "every new route must be classified in the policy table" rule to .agents/ENGINEERING_RULES.md.

VERIFY: pytest green; a test proving an authenticated user and an anonymous user on the same IP get separate buckets; a test proving Turnstile-disabled is a true no-op; graphify update .. Stop and report.
```

## Prompt 30 — backend/ask_pipeline.py: decide-or-delete (deferred product-risk decision, 2026-08-01) — ➡️ SUPERSEDED by Prompt 35

> **➡️ Superseded 2026-08-19 by Prompt 35 / `plan.md` §22.** This prompt deliberately withheld a recommendation because the 2026-08-01 pass was told to write up options rather than decide unilaterally. That was correct then; it has now been left open across **three** passes (2026-08-01, 2026-08-17, 2026-08-19), which is itself the outcome the original instruction was trying to avoid. **Prompt 35 recommends Option 1 (delete) and states the tradeoff explicitly**, on the strength of new evidence this prompt did not have: the two live pipelines have since diverged on a *cost-control* boundary as well as a response-shape one (`check_user_budget_and_enforce` is called from `asgi.py:668` and nowhere else — the Flask `/ask` at `app.py:1943` has no per-user spend check at all). Read Prompt 35, not this. The option text below remains accurate and is worth reading as background for Option 2.
>
> **⚠️ Prompt 35 itself was then re-scoped 2026-08-21 (Prompt 40 STEP 3 / `plan.md` §27.3) — read that, not Prompt 35's original text, before doing anything.** Prompt 20 landed between Prompt 35 being written and anyone executing it, and made `run_agentic_ask()` (one of this module's two exports) a genuine production importer. "Delete `ask_pipeline.py`" no longer means what it did on 2026-08-19 — it now means "delete only `run_ask_pipeline`/`AskPipelineResult`, the still-dead half, plus their two dedicated test files; keep the module and `run_agentic_ask()`; still build the parity suite."
>
> **✅ Executed 2026-08-26, re-scoped form, sixth pass (Prompt 35).** `backend/ask_pipeline.py` now defines only `run_agentic_ask` — `run_ask_pipeline`/`AskPipelineResult` are gone, along with `tests/test_ask_pipeline.py`/`tests/test_ask_pipeline_smoke.py`; `tests/test_ask_transport_parity.py` exists (20 tests, all six invariants). See the Prompt 35 row and section below for full evidence.

Written during the 2026-08-01 Phase A cleanup pass (closing out Prompts 1–11's remaining gaps). That pass's instructions explicitly required *either* wiring this module in with a full parity pass *or* deleting it outright — "do not leave it in limbo a third time" — but also said to stop and ask if genuinely unsure, since this is a product-risk call, not mechanical cleanup. The user asked for the decision to be written up as a prompt instead of made unilaterally mid-cleanup. Read this whole section before touching the file.

**What's actually true about the module (verified 2026-08-01, don't trust older claims):**
- `backend/ask_pipeline.py` (432 lines) is a from-scratch async reimplementation of the `/ask` orchestration flow — prayer early-return, parallel source/knowledge collection, strict-mode guard, AI synthesis, and a fallback-to-halakhic-discovery path on synthesis failure.
- It is **not imported by app.py or asgi.py anywhere** — confirmed by grep, zero production reachability. Its own module docstring says so explicitly and warns: *"Do not treat this module as drop-in-safe or wire it into app.py/asgi.py without a dedicated correctness review and test pass first."*
- Earlier audits (plan.md §18.3, this file's own 2026-07-31 note) described it as "0% coverage." **That is now stale/wrong** — the current `pytest --cov` run shows `backend/ask_pipeline.py` at **91% line coverage** (117 stmts, 11 missed: lines 215-217, 274, 292, 299, 325, 335-338). Tests exercise the module directly (unit-level), they just don't run through any live route — production-path coverage is 0%, line coverage is not. Re-verify this yourself with `pytest --cov=backend.ask_pipeline -q` before assuming either number.
- **New finding, not in any prior audit**: `run_ask_pipeline()` never calls `classify_safety()` before invoking `claude_module.ask_ai_async(...)`. Per `.agents/ENGINEERING_RULES.md`'s "Age-appropriate output & safety routing" section, `classify_safety()` is mandatory before *every* synthesis call — `medical`, `mental_health_or_self_harm`, and `abuse_or_minor_safety` questions must route to a referral and never reach the model. Confirm this is still true of the live app.py/asgi.py `/ask` handlers (it was as of 2026-08-01) and confirm `run_ask_pipeline()` still lacks the call before doing anything else here — if either has changed, this whole analysis needs redoing, not just step 1 below.

**Do exactly one of the following. Pick based on whether you have room in this session for a genuinely safety-critical parity pass — if you're already deep into other work, prefer delete-and-revisit over a rushed wire-in.**

### Option 1 — Delete it (lowest risk, recommended default if time/context is tight)
1. `git rm backend/ask_pipeline.py` and any test file that exists solely to exercise it (check `tests/` for an `ask_pipeline`-specific test module first — read it before deleting to confirm it doesn't also cover something else reused elsewhere).
2. Grep repo-wide for `ask_pipeline` / `run_ask_pipeline` / `AskPipelineResult` to confirm nothing else references it (plan.md and this file's own prose mentions don't count — code only).
3. Remove the plan.md §9 / Prompt 20 language that names `backend/ask_pipeline.py` specifically as the wire-in target, OR leave §9/Prompt 20 as the place a *future* reimplementation would live — your call, but say which you did.
4. `pytest -q` green, `graphify update .`, report the coverage/LOC delta.

### Option 2 — Wire it in properly (only if you can commit real time to it)
1. Add a `classify_safety()` call inside `run_ask_pipeline()` in the same position/semantics as app.py's and asgi.py's live `/ask` handlers use it — before `claude_module.ask_ai_async(...)`, with the same referral-routing behavior for the three restricted classes. This is not optional groundwork, it's the blocking prerequisite — do not skip to parity testing without it.
2. Read both live implementations in full — app.py's Flask `/ask` route and asgi.py's FastAPI `/ask` route — and diff their behavior against `run_ask_pipeline()` line by line: prayer early-return wording/conditions, strict-mode guard, source attribution note construction, fallback-on-failure path, `DEVTOOLS_STATS` bookkeeping, response `meta` key set (must match `.agents/ENGINEERING_RULES.md`'s "same JSON key set on every path" rule). List every divergence found before fixing any of them.
3. Write golden-master parity tests: same question/mode/language inputs fed to the current app.py handler and to `run_ask_pipeline()`, asserting byte-identical (or intentionally-documented-different) output shape and content for the happy path, the strict-block path, and the AI-failure fallback path.
4. Only after parity tests pass, switch one transport (recommend asgi.py's FastAPI path first, since it's already async-native) to delegate to `run_ask_pipeline()`. Do not switch both transports in the same change — land one, verify in production behavior/logs, then migrate the other as a separate follow-up.
5. Full pytest green including the new parity suite, manual verification that safety-classifier routing still fires (test with a real restricted-class question, not just a mock), `graphify update .`. Report exactly what changed, what stayed independently implemented in the other transport, and what the follow-up migration still needs.

### Option 3 — Explicitly park it (only if neither of the above fits right now)
Not preferred — the 2026-08-01 pass was told not to leave this in limbo a third time. Only choose this if you have a concrete reason Options 1 and 2 both don't apply yet (e.g., Prompt 20's agentic tool-use layer is about to subsume this file's whole purpose and rewriting it twice would be wasted work). If you do: update the module docstring's date-stamped warning, add a code comment linking back to this prompt, and say explicitly in your report why delete-or-wire-in was deferred again and what condition would trigger revisiting it.

**Whichever option you take, update this file's status table (this prompt's row) and plan.md §18.3's ask_pipeline.py bullet in place to reflect the outcome — don't leave both describing a decision that was actually made.**

---

## Prompt 31 — CSP nonce hardening: onclick/inline-style refactor decision — ✅ DECIDED 2026-08-15 (Option 2)

**Decision: Option 2 (defer, keep `'unsafe-inline'`).** Confirmed directly with the user 2026-08-15. CSP stays as-is — no nonce, `'unsafe-inline'` retained on both `script-src`/`style-src`. This is the current, working, non-regressive state; Option 1's refactor (112 `onclick=` + 43 `style=` sites) remains available as future work if `templates/index.html`'s markup is refactored for other reasons first, at which point removing `'unsafe-inline'` becomes a much smaller incremental step rather than a dedicated large effort. Prompt 11's status row updated to reflect this as the final state of its CSP item, not a pending question.

Written during the 2026-08-01 Phase G pass (closing out Prompt 11's remaining CSP item). The original ask was "implement nonce-based CSP script-src/style-src, thread the nonce into every inline script/style, remove `'unsafe-inline'`" — framed as a mechanical threading task. It isn't. Read this whole section before touching `app.py`'s CSP header or any inline `onclick=`/`style=` attribute.

**What's actually true (verified 2026-08-01, don't trust the old code comment):**
- The CSP header (`app.py`'s `apply_response_cache_policy`) currently ships `script-src 'self' 'unsafe-inline' ...` and `style-src 'self' 'unsafe-inline' ...`. The removed comment blamed this on "the Tailwind CDN JIT compiler" not yet being replaced with a build step — **that's stale**. Tailwind has already been a prebuilt `static/css/tailwind.css` (via `package.json`'s `build:css` script) for some time; that blocker is gone.
- The **real** blocker: `templates/index.html` has **112 inline `onclick=` handlers** (plus 1 `onkeydown=`) and **43 inline `style="..."` attributes** (grep-counted 2026-08-01). CSP's script-src/style-src directives cover both `<script>`/`<style>` *elements* and inline event-handler/style *attributes* — a nonce only lets you allowlist the former. Per the CSP spec, once any `'nonce-...'` (or hash/`'strict-dynamic'`) source is present in a directive, CSP2+ browsers **ignore `'unsafe-inline'` entirely** for that directive, even if it's still listed alongside the nonce for older-browser fallback. That means: add a nonce to `script-src` today, and every one of the 112 `onclick=` handlers stops firing in every modern browser. Same for `style-src` and the 43 inline `style=` attributes.
- Actual inline `<script>`/`<style>` **blocks** needing a nonce are a small, already-enumerated list: `templates/index.html` (script tags at lines ~21, 43, 53, 96; a `<script type="application/ld+json">` too, which technically doesn't execute but is still a script element), `templates/components/legal_scripts.html` (2 inline blocks, shared by all 3 legal pages), `templates/{terms,privacy,accessibility}.html` (1 `<script>` + 1 `<style>` each). This part genuinely is a simple threading task — it's just not the blocking part.
- Google Fonts' `https://fonts.googleapis.com/css2?...` stylesheet has no `integrity` and **cannot get one**: Google serves different `@font-face`/`src` payloads per requesting User-Agent (different browsers get different woff2/woff/ttf subsets), so there is no single byte-stable response to hash. This is a genuine, permanent SRI inapplicability, not an oversight — the only ways around it are self-hosting the font files (a real asset-pipeline change) or accepting the gap. Not attempted here.

**Do exactly one of the following.**

### Option 1 — Refactor onclick/style to unlock the nonce (the complete, spec-correct fix; large and unverifiable headlessly)
1. Convert all 112 `onclick=`/`onkeydown=` attributes in `templates/index.html` to `addEventListener` bindings, wired up in the existing bootstrap/init code rather than inline in markup. Each site currently passes literal arguments (e.g. `onclick="displayCommunity('Ashkenaz')"`) — these need `data-*` attributes + delegated listeners, or per-element listener registration after render, preserving exact existing behavior.
2. Convert all 43 inline `style=` attributes to either existing utility classes or new single-purpose CSS classes (several are already `sk-*` skeleton width/height overrides from this session's Phase F work — those specifically are legitimate one-off sizing and might be better served by CSS custom properties set via `.style.setProperty(...)` in JS than by a blanket class-ification; judgment call per site).
3. Add per-request nonce generation (`secrets.token_urlsafe(16)` or similar) in a `before_request` hook, expose it to templates via `g.csp_nonce` + a Jinja context processor, and add `nonce="{{ csp_nonce }}"` to every inline `<script>`/`<style>` block enumerated above.
4. Update the CSP header to `script-src 'self' 'nonce-{nonce}' https://... ; style-src 'self' 'nonce-{nonce}' https://...` — drop `'unsafe-inline'` entirely (it would be spec-ignored anyway once the nonce is present, so leaving it in is just misleading, not harmful — but drop it for clarity).
5. This needs live-browser verification (click every interactive control, confirm no CSP console violations) that is not possible in this headless environment — do not mark this complete without it. Budget this as genuinely large, multi-session work, not a quick follow-up.

### Option 2 — Defer, keep `'unsafe-inline'` (recommended default given the headless-verification constraint)
1. Leave the CSP as-is (`'unsafe-inline'` on both directives, no nonce). This is the current, working, non-regressive state.
2. Optionally add nonces to the ~8 inline `<script>`/`<style>` *blocks* only (the ones enumerated above) as defense-in-depth groundwork, **but only if you also keep `'unsafe-inline'` and verify in a real CSP-2+ browser that this doesn't silently break the onclick handlers** — per the spec behavior above, this is risky to do partially; if in doubt, skip it entirely rather than guess.
3. Update this file's status table (this prompt's row) and `plan.md`'s CSP note to say explicitly why nonce-based CSP is deferred and what unblocks it (Option 1's refactor).

### Option 3 — Scoped partial: nonce only the legal pages (terms/privacy/accessibility)
Not preferred over Option 2 unless there's a specific reason to prioritize CSP hardening on just the legal pages. Those 3 pages + `legal_scripts.html` have **zero** inline `onclick=`/`style=` attributes today (confirm this is still true before relying on it) — meaning a nonce-based CSP *could* be safely rolled out to `/terms`, `/privacy`, `/accessibility` independently of `index.html`, which would need its own separate (looser) CSP. This requires per-route CSP headers instead of one blanket header in `apply_response_cache_policy` — a real but smaller change. Only do this if the legal pages specifically need hardening sooner than the main app.

**Whichever option you take, update this file's status table (this prompt's row) and Prompt 11's status-table row to reflect the outcome — don't leave both describing a decision that was actually made.**

---

## Prompt 32 — §19: inline-block / ES-module duplication (frontend instance of the §2 failure mode) — ⏸ NOT STARTED, plan written 2026-08-17

Written 2026-08-17. `plan.md` §19 is the full finding; this prompt is the executable version. Read §19 **and** this whole section before touching `templates/index.html`, `static/js/*`, or `docs/FRONTEND.md`. Every line number below was re-verified 2026-08-17 against the working tree — **re-verify them yourself before editing**, `index.html` drifts by hundreds of lines between sessions (the prior session's notes had `/ask` at `:7815` and geolocation at `:8932`; they are now `:9382` and `:10499`).

**Update (2026-08-21):** Duplication B below is now partly stale — `navigator.geolocation.getCurrentPosition`, `resolveSavedLocationLabel`, and `hasMeaningfulLocationDelta` were deleted (operator request: stop prompting for GPS permission, rely on the session cookie + IP-geolocation fallback `get_engine()` already does server-side). See `plan.md` §19.3's own update note for the full replacement behavior and the corrected Phase 2 exit criterion (`navigator.geolocation` should end up appearing **zero** times repo-wide, not once inside `zmanim.js`).

**What's actually true (verified 2026-08-17, don't trust older notes or `docs/FRONTEND.md`):**

- `templates/index.html` is **11,661 lines** (`wc -l` says 11,660 — no trailing newline; all citations below use editor/`Read` numbering). Lines **1568–11571** are one classic inline `<script>` block — **10,004 lines, 314 top-level `function` declarations**. `motion.js` (`:11578`) and `main.js` (`:11579`) load as `type="module"` *after* it.
- `main.js:18-26` builds `window.ShelahModules = { askAi, loadSemanticBookmarks, saveSemanticBookmark, prewarmDailyStudy, getState, setState, subscribe }`. **Nothing consumes it.** `grep -rn ShelahModules` repo-wide returns `main.js:18` plus 6 prose hits in `docs/FRONTEND.md`, and nothing else. `templates/index.html` never mentions it. The bridge is write-only in production.
- **Duplication A — `/ask`, and the module is the WEAKER copy.** Inline `askWithRetry` (`index.html:9365-9408`) + `handleAiSearch` (`:9410-9542`) have: 60 s per-attempt `AbortController` (`:9357`, `:9379-9380`), 3 attempts with `1200ms × attempt` backoff (`:9358`, `:9367-9377`), `[502,503,504]` retry (`:9388`), `AbortError`/`TypeError` retry (`:9396`), `error.attempts` telemetry (`:9400`, `:9406`, logged `:9532`), and live retry phase-text UX (`:9369-9375`). `static/js/ai-service.js::askAi()` (`:14-74`) has **none of it** — no timeout, no retry, no status branching. It also returns a parsed payload and throws on `!ok` (`:49-53`) where the inline caller wants a raw `Response` (`:9503-9522`), and writes `setState({ai:{…}})` (`:25-31`, `:55-62`) where the inline caller writes `appState.lastAiQuestion`/`.lastAiResponse` (`:9506-9507`) — same object (`window.appState`, set `index.html:2647`), two key schemes.
- **Duplication B — geolocation is a DOC duplication, not a code one.** Inline `initZmanim()` (`:10475-10543`) owns `navigator.geolocation.getCurrentPosition` (`:10499`), the localStorage location cache, the manual-override guard (`:10501-10505`), `POST /set_location` (`:10517`), and `fetchZmanimAPI` (`:10545`). `static/js/zmanim.js` (139 L) exports **only** `prewarmDailyStudy` + `installDailyPrewarm` and contains **zero geolocation code**. `docs/FRONTEND.md:78-88` claims it exports `installZmanim()` and owns geolocation/IP-fallback/`/api/calendar/today`/60 s polling. All false.
- **Duplication C — semantic bookmarks, NOT flagged in any prior audit, and the most dangerous.** Inline `saveSemanticBookmark()` (`:5303-5356`) bound to `#semanticBookmarkBtn` at `:7196`; `reader-ui.js::saveSemanticBookmark()` (`:130-167`) installed unconditionally by `main.js:13`. Both POST `/api/bookmarks/semantic`. There is no double-POST **today** only because `reader-ui.js:175` guards on `typeof window.saveSemanticBookmark === "function"` — which passes solely because the inline copy is a top-level `function` declaration in a *classic* script (contrast `const appState` at `:2610`, which needed an explicit `window.appState = appState` at `:2647` to be visible). **Wrapping the inline block in an IIFE or converting it to a module — the exact step every extraction phase performs — silently breaks that guard and double-binds the button: two POSTs, two billed AI summaries, two alerts, no test catching it.**
- Separately: `main.js:16` fires `void loadSemanticBookmarks()` (authenticated `GET /api/bookmarks/semantic`) on **every page load**, writing `appState.semanticBookmarks`. `grep -c semanticBookmarks templates/index.html` = **0**. Nothing reads it.
- **Test floor is essentially zero.** `tests_js/` contains exactly one file: `sentry_init.test.js`. No `static/js/` module other than `sentry-init.js` has any test coverage.
- **`docs/FRONTEND.md` is substantially divergent** — 12 verified false status claims catalogued in `plan.md` §19.5 (line count, `askAi`'s Clerk/429/503/`setState` behavior, `reader-ui.js`'s and `zmanim.js`'s export names, `main.js`'s claimed responsibilities, the bridge-usage claim, the state-shape table). Its **module map, bridge pattern, and "Adding a New ES Module" recipe (`:239-278`) are still the correct target architecture** — follow those. Distrust only its status claims.
- **Do not port a 429 handler on the doc's authority.** `FRONTEND.md:61` claims `askAi` handles 429; `grep -c 429 templates/index.html` = **0**, and neither copy handles it. Prompt 29c's row already records that `docs/API.md` documents a 429 that doesn't exist. A 429 path is net-new behavior belonging to Prompt 29a/29c, not to this extraction.
- Stale cross-reference to fix in passing: `index.html:9364` cites "plan.md §7.13", which does not exist (current §7 has no numbered children). Same dead numbering as the surviving `§7.10` references at `plan.md:309`/`:310`/`:338`/`:632`/`:640`/`:666`/`:702`. Re-point it at §19.

**Explicitly rejected — do not propose, plan, or start this:**

**Dumping the ~10,000-line inline block verbatim into one new `static/js/app.js`.** Suggested by an external review, rejected 2026-08-16, re-rejected 2026-08-17 on the evidence above. Five reasons, any one sufficient: (1) it resolves nothing — all three duplications survive the move intact, relocated rather than reconciled; (2) it creates a sixth competing convention beside `state.js`'s store, the `install*()` recipe, the bridge, and the per-feature module map; (3) it inverts `FRONTEND.md`'s own documented incremental, feature-boundary migration; (4) 314 functions with near-zero test coverage moved in one change-set is unverifiable headlessly — a *smaller* frontend refactor (Prompt 31's 155 inline sites) was already deferred on exactly this reasoning, and this is ~2× larger; (5) it converts declaration scope in one shot, which fires the Duplication-C tripwire and breaks all 112 inline `onclick=` handlers catalogued in Prompt 31 / `plan.md` §18.3 simultaneously.

**Also rejected: "delete the inline `/ask` copy and call `ShelahModules.askAi`."** The module is the weaker implementation — this is a functional regression wearing a cleanup's clothes. §2's reconcile-then-consolidate mandate applies: diff, decide which behavior is correct (it's the inline one), make *that* canonical in the module, delete both originals, repoint callers.

**Execution — one phase per change-set, stop and report between phases. Do not run two phases in one session.**

### Phase 0 — Disarm the tripwire + establish a test floor (mandatory prerequisite)
1. Replace `reader-ui.js:175`'s implicit-global guard with an explicit, scope-independent flag: have the inline block set something deliberate near `:7196` (e.g. `window.__shelahLegacyBookmarkBound = true`) and have `installSemanticBookmarking()` test that instead of `typeof window.saveSemanticBookmark`.
2. Gate or delete `main.js:16`'s `void loadSemanticBookmarks()`. Re-confirm `grep -c semanticBookmarks templates/index.html` is still `0` before choosing delete over gate.
3. Add `tests_js/` coverage: a jsdom test asserting exactly **one** `click` listener on `#semanticBookmarkBtn` under both scope models (inline global present and absent), and a `fetch`-mocked test asserting exactly **one** `POST /api/bookmarks/semantic` per click.
4. Exit: `npm test` green, **and** the new test must **fail** when step 1 is temporarily reverted — prove it has teeth. A guard test that passes both ways is worthless.

### Phase 1 — Reconcile `/ask` into `ai-service.js` (first real duplication deleted)
1. Port the inline retry/timeout logic **up into** `askAi()` verbatim — constants, backoff, `[502,503,504]` list, `AbortError`/`TypeError` test, `error.attempts`. No "improvements" in the same change.
2. Abstract the DOM coupling out: the inline copy writes `#aiLoadingPhaseText` (`:9369-9375`). Add an optional `onRetry(attempt)` callback in `options`; the call site passes the phase-text updater and owns `t()`.
3. Pick the return contract explicitly and document it in the module docstring — recommend keeping the module's parsed-payload-plus-throw shape and adapting the caller, since it's ES-module-native and the only shape the bridge expresses cleanly.
4. Keep **both** state writes (`state.ai.*` and the top-level `lastAiQuestion`/`lastAiResponse`) this phase; add `// TODO(§19)` on the top-level keys. Collapsing the two schemes is a separate change with its own blast radius.
5. **Then** delete `askWithRetry` (`:9365-9408`) and rewrite `handleAiSearch`'s try-block (`:9491-9522`) to call `window.ShelahModules.askAi(...)` — the first actual use of the bridge. Modules are deferred, so `ShelahModules` doesn't exist until after the classic block runs; `handleAiSearch` is interaction-triggered so this is safe, but **assert it** (`if (!window.ShelahModules?.askAi)`) rather than assume it.
6. Re-point the `§7.13` comment at `:9364` to `§19`; correct `docs/FRONTEND.md:61` to describe real behavior, deleting the false 429 claim.
7. Exit: `grep -rn "/ask" templates/ static/js/` shows exactly one `fetch` call site (in `ai-service.js`); new `tests_js/ai_service.test.js` covers happy path, 502-then-success, 3-attempt exhaustion, timeout/abort, and 4xx-no-retry; `npm test` + `pytest -q` green; `graphify update .`.

### Phase 2 — Real `installZmanim()` in `zmanim.js`
Extract per `FRONTEND.md:239-278`: `initZmanim` (`:10475`), `fetchZmanimAPI` (`:10545`), `hasRealZman` (`:10452`), `setZmanRowVisibility` (`:10457`), `applyOptionalZmanRows` (`:10463`), `hasMeaningfulLocationDelta` (`:10469`), `formatZmanClockDisplay` (`:10690`), `formatCountdownDuration` (`:10673`), `startCountdown` (`:10706`), `setZmanimLocationLabel` (`:1717`), `resolveSavedLocationLabel` (`:1733`). Build the call-closure **first** and list, per symbol, how each inline global it touches (`t()` `:2672`, `isHebrewMode()` `:2664`, `zmanimData`, `currentZmanimLocationLabel`, `zmanimLocationManuallySet`, `ZMANIM_LOCATION_*_KEY`) becomes a parameter, an import, or a documented `window.*` contract — do **not** add a third undeclared-global reach like `buildAuthHeaders`'s `window.authHeaders`. `main.js` calls `installZmanim()`; the inline `initZmanim` and its call site are deleted, not left dormant. Update `FRONTEND.md:78-88`. Exit: `navigator.geolocation` appears once repo-wide; `tests_js/zmanim.test.js` covers cached-location-first, GPS-success, GPS-denied fallback, and the currently-untested manual-override guard (`:10501-10505`).

### Phase 3 — Consolidate semantic bookmarks into `reader-ui.js`
Third, not first, because Phase 0 already removed the acute risk and this needs a real product decision: the copies differ in **user-visible** behavior (inline = `window.alert` + 401-retry + sign-in routing; module = throws + `postClientError`). Recommend keeping the inline 401 retry and sign-in routing plus the module's `postClientError`, and replacing `window.alert` with non-blocking in-page status. Then delete the inline copy, drop the Phase-0 flag and both guards, bind unconditionally.

### Phase 4+ — Everything else, one feature boundary per phase, never as a unit
See `plan.md` §19.8's candidate table (`i18n.js`, `theme.js`, `auth.js`, `prefs.js`, `library.js`, `prayers.js`, `sidebar.js`/`settings.js`, and extending `reader-ui.js` last). **Sequencing rule: `auth.js` before any further extraction** — `ai-service.js:3-12` and `reader-ui.js:6-14` both already reach for `window.authHeaders` as an inline global; extracting it converts two hidden couplings into real imports instead of adding a third.

**Constraints on every phase:** reconcile-then-consolidate per §2 (no phase is done while two copies survive); move verbatim and change behavior in *separate* commits; no module reads an undeclared inline global; enumerate what moved code exposed implicitly on `window` before moving it (scope conversion is the hazard); no extraction lands without a `tests_js/` test that fails when the extraction is reverted; and per the Prompt 11 / Prompt 31 precedent, anything needing a real browser to confirm is **not** marked done in a headless environment — say so explicitly.

**Whichever phase you complete, update this file's status table (this prompt's row) and `plan.md` §19's corresponding phase in place — don't leave both describing work as un-started once it has begun. If Phase 1 lands, also update `plan.md` §7's new bullet and §19.6's bridge-caller-count claim, both of which assert zero bridge consumers.**

---

## Prompt 33a — §20a: fix the cost ledger (it currently reads $0.00 for most production calls) — ✅ DONE 2026-08-19, see `plan.md` §20 PHASE 20a

**Run this before Prompt 29b and before Prompt 20.** It is small, self-contained, and every cost claim in this project is false until it lands.

```
Read plan.md §20 in full — especially §20.0 (the scope boundary against §16), §20.1 C1/C3b, and §20.2 Phase 20a — plus §2, §6. This is a correctness fix to a measurement layer that two enforcement mechanisms already depend on. Do NOT build a global cost breaker in this pass; that is §16.3-L3 / Prompt 29b, and §20.0 forbids putting it here.

STEP 0 — GOLDEN MASTER FIRST (project rule, §6.2, non-negotiable):
Add tests/test_cost_meter_pricing.py asserting the CURRENT broken behavior: estimate_cost_usd("gemini-3.5-flash-lite", 1000, 1000) == 0.0. Run it, confirm it passes against current code, report to me. If it does NOT pass, STOP — the §20.1-C1 diagnosis is wrong and the plan needs correcting, not the code.

STEP 1 — close the drift at its source, not by adding a row:
Do NOT simply add "gemini-3.5-flash-lite" to _PRICE_PER_M. That fixes today and guarantees the identical bug on the next model bump (§2 forbids this shape of fix). Instead: export from backend/claude.py a single module-level constant naming every model the code can actually dispatch — _DEFAULT_GEMINI_MODEL (claude.py:62), the GEMINI_MODEL env override, and the hardcoded Claude fallback "claude-haiku-4-5" (claude.py:1690). backend/cost_meter.py reads model identity from that one source instead of a hand-maintained parallel list. Then add the real prices for every name in it.

STEP 2 — the CI guard (this is the deliverable that matters):
Extend tests/test_cost_meter_pricing.py: for every name in the exported dispatchable-model set, assert _PRICE_PER_M has an entry AND both input/output prices are > 0. Prove it has teeth — temporarily rename _DEFAULT_GEMINI_MODEL, confirm the test goes red, revert. Report that you did this. A test that cannot fail is worse than no test.

STEP 3 — make a runtime miss audible:
estimate_cost_usd() on an unpriced model must emit a WARNING-level structured log (and a Sentry breadcrumb), once per process per model name — not silence. Keep returning 0.0; do not guess a price. The bug here was never the arithmetic, it was that the gap was invisible.

STEP 4 — fix the booby trap:
backend/claude.py:1435 passes model=str(primary_result.get("provider") or _DEFAULT_GEMINI_MODEL). The key is named "provider" but holds a MODEL name ("provider": model_name at claude.py:580,595,610,1701,1741,1752,1775,1784,1825,1833; "provider": _PRIMARY_MODEL at :541,550). It currently passes the right string by accident of naming. Rename the key to "model" throughout backend/claude.py so a future edit making it hold "gemini" cannot silently re-break pricing. Mechanical, ~13 sites.

STEP 5 — cover ALL FOUR record_llm_call() sites, not one:
claude.py:1433, 1717, 1806, 2000. Note :2000 is route="/api/bookmarks/semantic", not /ask. Tests must assert across all four.

STEP 6 — stop the ledger going silently dark (C3b):
_insert_usage_row (cost_meter.py:50-59) swallows Supabase insert failure at logger.debug — invisible at default LOG_LEVEL=INFO. Route it through the project's existing _capture_backend_error() funnel instead. Keep the write non-fatal to the request; change visibility, not control flow. This is the same failure signature as the accept_legal/clerk_id bug (§23.1): broad except + sub-INFO logging hiding a schema problem.

STEP 7 — backfill decision, state it either way:
Historical ai_usage_log rows are wrong. They carry input_tokens/output_tokens, so a recompute is arithmetically possible. RECOMMENDED: recompute in place via a one-off script and record pre/post totals in the commit message. If you instead accept the loss, write a documented cutoff timestamp so nobody later reads pre-fix history as real spend. Do not leave this unaddressed.

VERIFY: STEP 0's golden master is now inverted, with a comment saying why. The STEP 2 guard demonstrably fails on a renamed model. All four record_llm_call sites covered. A forced _insert_usage_row failure reaches _capture_backend_error. `pytest -q` green. `graphify update .` shows no new cycle.

Do NOT touch the per-user budget race (that is Prompt 33b), the multi-account question (33c), or anything in §16. Update plan.md §20's Phase 20a status and this file's Prompt 33a row when done. Stop and report.
```

## Prompt 33b — §20b: make the per-user budget ceiling atomic — ✅ DONE 2026-08-20, see `plan.md` §20 PHASE 20b

**Requires Prompt 33a first** — making a race-free check over a ledger that reads \$0.00 buys nothing.

```
Read plan.md §20.1-C2 and §20.2 Phase 20b, plus §2, §6. The per-user daily spend ceiling is a check-then-act race.

STEP 0 — CONCURRENCY TEST FIRST, and it is the deliverable:
Write a test that fires N concurrent check_user_budget_and_enforce() calls against a real or faithfully-faked Postgres, for a key already just under threshold, and asserts at most one is allowed. Confirm it FAILS against current code (cost_meter.py:285-312 reads at :307, decides at :309, never writes). A sequential-only test proves nothing about the defect being fixed — if your test passes against current code, it is testing the wrong thing.

STEP 1 — atomic check-and-reserve in Postgres:
New scripts/sql/check_and_reserve_user_budget.sql defining a plpgsql function that, in ONE transaction, sums today's cost_usd for the key, compares to threshold, and if under, inserts a RESERVATION row before returning allowed. The reservation is what closes the race — concurrent callers must see each other's reservations because the read and write are the same statement. COMMIT THIS FILE (five of its siblings currently are not — §23.2.1).

STEP 2 — spec the reservation lifecycle BEFORE writing code:
Write out reserve -> settle (and reserve -> expire) explicitly, including what happens if the process dies between reserve and settle. RECOMMENDED: a TTL sweep folded into the existing retention-enforce cron (routes_privacy.py:272), not a new job. record_llm_call() must SETTLE the reservation with real token counts, not insert a second row — otherwise the ledger double-counts and you have re-broken what Prompt 33a just fixed. Show me the lifecycle spec before implementing.

STEP 3 — wire it in:
check_user_budget_and_enforce() calls the function via RPC instead of _fetch_today_usage_cost_for_key. Keep fail-open on TRANSPORT errors (deliberate, documented at cost_meter.py:293-294), but a returned allowed=false must BLOCK.

STEP 4 — per-request ceiling, not just cumulative:
C2's second property: a caller starting the day at $0 always gets one unbounded call through. Cap a single call's cost. Cheapest sound form is capping max_output_tokens at dispatch — bounding at the provider is stronger than bounding in the ledger.

STEP 5 — the transport asymmetry:
check_user_budget_and_enforce has exactly one non-test call site: asgi.py:668. Flask's /ask (app.py:1943) has NO budget check. Either wire it into both, or add a test ASSERTING the Flask route is unreachable in production — do not leave the asymmetry undocumented. This is the same discipline Prompt 35 applies to the pipelines.

VERIFY: STEP 0's concurrency test fails on old code, passes on new. Ledger totals after a settled reservation match a non-reserved control. `pytest -q` green. The SQL file is committed.

Do NOT touch the multi-account bypass (Prompt 33c) or §16. Update plan.md §20 Phase 20b and this file's row. Stop and report.
```

## Prompt 33c — §20c: the multi-account budget bypass — a decision, not a task

**✅ Decided 2026-08-23 (Prompt 45/§33.5) — Option A chosen.** Recommendation was written 2026-08-19, conditional on Prompt 29b (§16 Phase 9b) shipping; 29b shipped 2026-08-22 (§16.10), satisfying the precondition. Recorded in `plan.md` §20.2 PHASE 20c, `.env.example:50-61`, and `docs/SECURITY.md` §5. Read `plan.md` §20.2 Phase 20c for the full text of the decision.

**The problem, stated plainly:** the per-user ceiling keys on Clerk `user_id`. Clerk signup is frictionless by design. A caller who hits the \$2/day cap creates a second account and continues. Nothing limits accounts-per-person or links accounts by device/IP for budget purposes.

**This is a product/abuse decision, not a bug with an obvious fix**, and it needs to stop being an unspoken known-bad — because "we have a \$2/day per-user cap" is currently a claim this project cannot fully defend in a technical conversation.

| Option | Cost | Benefit |
|---|---|---|
| **A — Accept and document.** Per-user is a good-faith guardrail against accidental single-user runaway, not an anti-abuse control. The real controls are Prompt 29b's edge WAF (IP+JA4, survives account rotation) and its global breaker (caps total spend regardless of account count). | Nothing to build. | An honest, defensible answer: *"account-level is a guardrail; the global breaker and edge WAF are the actual ceiling."* **Only true once Prompt 29b ships.** |
| **B — Secondary IP/fingerprint budget key**; a caller must be under both. | Punishes shared egress — a yeshiva or day school behind one NAT would budget-limit each other. Exactly the failure §16.3-L2 already calls out for rate-limit keys; needs the same identity-aware treatment. | Meaningfully raises the cost of the bypass, no new infrastructure. |
| **C — Friction at signup** (Turnstile / verified email, coupled to §8.B-AGE's age gate). | Conversion cost on a product whose point is accessibility to learners. | Attacks the root — makes accounts non-free. |

> **Recommend A, conditional on Prompt 29b shipping.** B's collateral damage lands squarely on Sh'elah's actual audience (institutional shared egress), and C taxes every legitimate learner to stop an attacker who can still buy email addresses. **If Prompt 29b is not going to be built, A is not available and B becomes the default** — that conditional is the whole point of writing this down.

**Whichever is chosen:** record it with a date in `plan.md` §20.2 Phase 20c, update `.env.example` and `docs/SECURITY.md` to describe the *actual* guarantee rather than the intended one, and — if A — add an explicit dependency note to `plan.md` §16.6 Phase 9b. Then update this file's Prompt 33c row.

## Prompt 34 — §21: is Postgres RLS actually functioning? (operator action + acceptance test) — 🟡 UNBLOCKED 2026-08-24

**✅ Operator action answered 2026-08-24 — Clerk confirmed as a trusted Third-Party Auth issuer.** Recorded with a date in `docs/SECURITY.md` §2 and `plan.md` §21.2.1. This was the one finding in the roadmap that no amount of code reading could resolve — it is now answered, but that only means `auth.uid()` is *configured* to resolve; STEPs 1-8 below (the live acceptance test, the negative cross-user assertion, the two secondary idiom fixes) are the part that proves it's *actually enforcing*, and none of that has been built yet. Do not mark this prompt done on the dashboard confirmation alone.

**OPERATOR ACTION (Akiva — not an engineering pass): ✅ DONE 2026-08-24.** ~~In the Supabase dashboard, **Authentication → Sign In / Providers → Third-Party Auth**, confirm whether Clerk is registered as a trusted JWT issuer for this project, and enable it if not.~~ Confirmed enabled. The token-template half of this action is also resolved — see the 2026-08-24 update below: the live path uses Clerk's plain default session token, not the named `"supabase"` template `scripts/clerk_supabase_rls.py` expects, so that script is confirmed stale/unused (still needs deleting or explicitly relabeled — not done yet).

```
Read plan.md §21 in full, plus §8.C.2 (whose premise this prompt tests). Do not start until the §21.2.1 operator action has an answer — the fix differs by answer, and building the test first against an unconfigured project just documents that it is broken.

CONTEXT YOU MUST NOT GET WRONG: if Supabase is not configured to trust Clerk, auth.uid() returns NULL, every RLS policy evaluates false, and the affected routes SILENTLY RETURN ZERO ROWS. Not an error. Not a 403. Empty results that look like "no data yet."

STEP 0 — establish the blast radius yourself:
grep for _get_user_scoped_supabase_client. Expect FOUR production call sites: backend/routes_user.py:227 (/api/user/preferences), :299 (/api/bookmarks/semantic), backend/rag.py:308 and :405 (user-memory read/write). Prior audits recorded only two. The rag.py pair is the dangerous one — unlike preferences or bookmarks, a user CANNOT SEE that personalization silently stopped working. If your grep finds a different set, report it before proceeding.

STEP 1 — do NOT write another mocked unit test:
All 10 existing RLS-adjacent tests monkeypatch the client (tests/test_routes_user.py:138-295). A mock cannot tell you whether auth.uid() resolves in a real project, which is the only question being asked. Adding more of these actively makes things worse by looking like coverage. Leave them; they test orchestration, which is a different question.

STEP 2 — the live acceptance check (committed, not ad hoc):
Write scripts/verify_rls.py that mints a real Clerk session token for a dedicated test user, PUTs a sentinel to /api/user/preferences against the deployed URL, GETs it back, asserts the sentinel round-trips. Repeat for /api/bookmarks/semantic and — critically — the rag.py user-memory path, which has no user-visible symptom.

STEP 3 — THE NEGATIVE ASSERTION IS WHAT GIVES IT TEETH:
With a SECOND test user's token, assert user A's row is NOT returned. A test that only proves "my own data comes back" passes identically whether RLS is enforcing or whether the service-role client is quietly serving everything to everyone. Without this, the whole script is decorative. Demonstrate the negative assertion can fail before trusting the positive one, and report that you did.

STEP 4 — wire it as a scheduled/manual job, NOT a per-PR gate:
It needs live credentials and a deployed target. Blocking every PR on a network round trip to Supabase is the wrong trade. Use the same GitHub Actions workflow Prompt 36 creates for the schema-doc check — both need live Supabase credentials and both answer "does the repo still describe reality." One credential path, not two.

STEP 5 — extend /api/devtools/rls-audit to report the OBSERVED result of a real user-scoped query, not merely policy presence, so the answer is visible from the running app rather than only from a CI log.

STEP 6 — fix the two secondary inconsistencies:
(a) ask_history's RLS uses current_setting('request.jwt.claims',...)->>'sub' (scripts/migrate_ask_history.sql:34-36) while the other three use auth.uid(). It has never been exercised — it is never queried through a user-scoped client. Migrate it to the same idiom, OR delete it and state in the SQL file that the table is service-role-only by design. Either is defensible; drifting between two idioms is not.
(b) ai_usage_log has no RLS at all and holds raw client_key (IP strings) and per-caller cost. Add RLS, or add a comment in its migration file stating explicitly that it is service-role-only and why — so the absence is a recorded decision, not an oversight.

STEP 7 — then, and only then, correct the claims:
If STEP 0's answer is "not configured," plan.md §8.C.2 and docs/SECURITY.md are FALSE and must be rewritten to describe what actually protects the data: a hand-written .eq("user_id", ...) filter in application code, with no database backstop and no test catching that filter being dropped from a new route. ADD THAT TEST TOO — cross-user isolation asserted at the application layer on every Supabase-backed route, independent of whether RLS is live. It is cheap and it is the control that actually exists today.

STEP 8 — while here, resolve one dead branch: app.py:1008-1038 reads sb-access-token / supabase-access-token / chunked cookies that Clerk never sets — likely leftover from a pre-Clerk native-Supabase-Auth flow. Confirm dead, then delete. It is an auth-path branch; it does not get to stay on a guess.

VERIFY: a dated line in docs/SECURITY.md naming who verified the dashboard setting; scripts/verify_rls.py committed and demonstrated to fail on the cross-user case; ask_history on one idiom; ai_usage_log's status recorded; §8.C.2 describing verified reality.

Update plan.md §21 and this file's Prompt 34 row. Stop and report.
```

**Update 2026-08-24 — partial, code-only evidence gathered; the operator gate is still unanswered.** Checked against a 3-part client→Postgres JWT checklist (A: does the client send Clerk's JWT to Supabase; B: does Postgres see the JWT claims; C: does an RLS policy keyed on those claims actually gate access):

- **A — confirmed by code read.** This app has no `supabase-js` `createClient(..., { auth: { accessToken } })` call to find — it's Flask/FastAPI, not a Next.js client. The equivalent path: `templates/index.html`'s `authHeaders()` (`templates/index.html:4074-4091`) calls `window.Clerk.session.getToken()` with **no template argument** — the plain default Clerk session token, exactly as this prompt's own text already said — and attaches it as `Authorization: Bearer <token>` on fetch calls (used at 11 call sites, including the preferences/bookmarks routes). The backend forwards that same header as-is: `app.py::_extract_supabase_access_token()` → `_get_request_supabase_client()` (`app.py:1017-1034`) sets `Authorization: Bearer <token>` on a `ClientOptions(headers=...)`-configured Supabase client — the Python equivalent of the JS `accessToken` callback. So the token genuinely reaches Supabase's PostgREST layer on every request that has one. **Not verified live** — this session did not sign in through a browser, so the checklist's own STEP 3 (`!!token` / token-length logging) was not captured; that still needs a real signed-in session, which only the operator has.
- **B and C — still blocked, unchanged from 21.2.1/21.2.2.** This session's Supabase MCP connection exposes only `query_logs` (read-only log search over `edge_logs`/`postgres_logs`) — no `execute_sql`, `list_tables`, or dashboard access. Running `select auth.jwt()` in the SQL Editor, or the temporary `create policy ... using (auth.jwt() ->> 'org_id' = ...)` end-to-end test, cannot be done from here. These remain exactly the operator action 21.2.1 already specifies (Supabase dashboard → Authentication → Third-Party Auth) plus the live acceptance script 21.2.2 calls for — `scripts/verify_rls.py` still does not exist.

Net: this narrows the RLS question to B/C only (A is no longer a candidate explanation if RLS is silently failing), but does not answer the underlying "is it enabled" question. Prompt 34 stays **Not started**.

## Prompt 35 — §22: `backend/ask_pipeline.py` — delete it, and pin the two live pipelines instead

**✅ Done 2026-08-26, sixth pass, Option B (re-scoped form) in full.** Recommendation made 2026-08-19, superseded Prompt 30, re-scoped 2026-08-21, executed 2026-08-26. See "Execution evidence" below the STEP-by-STEP block for what actually shipped, including two transport-drift bugs found and fixed along the way and the STEP 4 `backend/customs.py` decision.

> **⚠️ Re-scoped 2026-08-21 (Prompt 40 STEP 3 / `plan.md` §27.3) — this is the form that was actually executed.** This prompt's title and STEP 1 say "delete `backend/ask_pipeline.py`," written when the file was verified 100% dead. Prompt 20 landed after this prompt was written and made `run_agentic_ask()` — one of this file's two exports — a real production importer: `app.py` and `asgi.py` both call it directly behind `AI_AGENTIC_TOOLS`. The module's *other* export, `run_ask_pipeline()`/`AskPipelineResult`, remains exactly as dead as this prompt found it. **STEP 1 meant:** delete only `run_ask_pipeline`/`AskPipelineResult` and their two dedicated test files (`tests/test_ask_pipeline.py`, `tests/test_ask_pipeline_smoke.py`) — **not** `git rm backend/ask_pipeline.py` as literally written. Keep the module file and `run_agentic_ask()`. STEP 2 (the parity suite) was the actual deliverable and is done.

**Verified 2026-08-19:** `backend/ask_pipeline.py` is 515 lines with **zero production importers** (repo-wide grep returns only its own two test files). There are three implementations of `/ask` orchestration — `app.py:1490-1975` (sync), `asgi.py:141-680` (async), and the dead module. **Both test files exist only to keep dead code from rotting:** `tests/test_ask_pipeline.py:44` asserts `app.py` still exposes every attribute the dead module expects (so it "doesn't rot silently since nothing else imports it" — its own docstring), and `tests/test_ask_pipeline_smoke.py` (41 lines) asserts the dead module still imports and still defines `run_ask_pipeline`/`AskPipelineResult`.

**The two live pipelines have already drifted, with a shipped bug to show for it** (`.agents/ENGINEERING_RULES.md:54`: the ASGI handler silently dropped `ai_cited_sources`), and they have since drifted again on a boundary no prior audit caught: `check_user_budget_and_enforce` is called at `asgi.py:668` and **nowhere else**, so Flask's `/ask` has no per-user spend check at all.

| | **A — Finish the migration** | **B — Delete, pin parity instead** |
|---|---|---|
| Work | Diff the dead module against *both* live pipelines (which have moved since it was written), update, cut over two business-critical routes. Multi-session. | One deletion commit, then a focused parity suite. |
| Risk | Cutting a money-spending, safety-classified, user-facing route to a 0%-production-exercised implementation. The riskiest change available. | Deletion is provably zero-risk. Parity tests are additive. |
| Fixes | The duplication itself — genuinely the better end state. | Not the duplication. Only the **drift**, which is the part that has actually caused bugs. |
| Verifiable here? | Needs a live environment. Per the Prompt 11 / Prompt 31 precedent, this project does not mark deploy-dependent work done in a headless pass. | Fully verifiable offline. |
| If half-done | A **fourth** state: a partially-cut-over pipeline. This is the third pass to consider finishing it. | None — it is one commit. |

> **Recommend B.** The honest framing: **A is the better architecture and B is the better decision given who is executing it and how.** A solo developer cutting two live revenue-and-safety-critical routes to an unexercised implementation, with no staging environment and no in-browser verification, is how the *first* drift bug shipped. **B is only correct if the parity suite is actually built** — deleting the module and not building the tests is strictly worse than doing nothing, because it removes the aspirational fix and adds nothing. Treat STEP 2 as non-optional.

```
Read plan.md §22 in full. Execute Option B unless explicitly told otherwise.

STEP 1 — delete:
Read tests/test_ask_pipeline.py and tests/test_ask_pipeline_smoke.py FIRST and confirm neither also covers something reused elsewhere — test_ask_pipeline.py exercises _flatten_sources_for_ai (:52-66); confirm that function exists ONLY in the dead module before its tests go with it. Then git rm backend/ask_pipeline.py and both test files. Grep repo-wide for ask_pipeline / run_ask_pipeline / AskPipelineResult and confirm zero remaining CODE references (prose in plan.md, DECISIONS.md and this file does not count). Leave §9 / Prompt 20 as the place a future shared pipeline would live and say so in the commit message, so this reads as a deliberate close rather than an abandonment. Report the LOC/coverage delta.

STEP 2 — THE ACTUAL DELIVERABLE: tests/test_ask_transport_parity.py
Pin the Flask and ASGI handlers to each other on the SPECIFIC things that have already drifted or now differ. Not a vague "outputs match":
  a) Identical top-level response key set on EVERY path — success, strict-block, AI-failure fallback. Partially covered today by tests/test_ask.py:366 — EXTEND that, do not duplicate it.
  b) Identical retry/timeout behavior (named in ENGINEERING_RULES.md:54 as one of the two confirmed production bugs).
  c) Identical prompt-selection thresholds (an already-observed drift).
  d) classify_safety() invoked before EVERY synthesis call, on both. This was the gap that made the dead module dangerous to wire in.
  e) Per-user budget check present on both, OR an explicit test asserting the Flask route is unreachable in production (see Prompt 33b STEP 5 — coordinate, do not implement it twice).
  f) Identical meta key set (ENGINEERING_RULES.md's "same JSON key set on every path" rule).

STEP 3 — make the accepted duplication a NAMED cost:
Add to .agents/ENGINEERING_RULES.md: "/ask is implemented twice (Flask sync, FastAPI async) by deliberate decision (plan.md §22). Any change to one MUST be mirrored in the other in the same commit, and the parity suite must be extended if the change adds a new observable behavior." An accepted cost that is not written where the next editor will see it is just an unacknowledged bug waiting to recur.

STEP 4 — while here, same discipline on the quiet sibling:
backend/customs.py's local-JSON fuzzy-match path is a second, undocumented dead path of the same shape — but unlike ask_pipeline.py, nothing flags it. Determine whether the JSON is still authoritative for anything after the Supabase migration, then decide-or-delete it in this same pass. Report which and why.

IF OPTION A IS CHOSEN INSTEAD: build STEP 2's parity suite FIRST (it becomes the specification to migrate toward); rewrite ask_pipeline.py against the CURRENT shape of both live pipelines — it predates the safety-classifier wiring, check_user_budget_and_enforce, and the ai_cited_sources fix, so this is a rewrite not a merge; cut over ONE transport (ASGI first — async-native and the only one live) and verify in production logs before touching the other. Never both in one change-set.

VERIFY: `pytest -q` green; `graphify update .`; the parity suite covers all six invariants.

Update plan.md §18.3's ask_pipeline bullet, plan.md §22, AND Prompt 30's status row in this file in the same commit — this is the fourth document to describe this as undecided, and that pattern is itself the §2 failure mode applied to planning artifacts. Stop and report.
```

### Execution evidence (2026-08-26)

- **STEP 1:** `backend/ask_pipeline.py` 774→271 lines (-503). `tests/test_ask_pipeline.py` (-303) and `tests/test_ask_pipeline_smoke.py` (-47) deleted in full — `_flatten_sources_for_ai` confirmed used only in the deleted path first. Repo-wide grep for `run_ask_pipeline`/`AskPipelineResult` returns zero code references. §9/Prompt 20 remains the home for `run_agentic_ask()`.
- **STEP 2:** `tests/test_ask_transport_parity.py` (new, 20 tests) covers all six invariants (a)-(f); invariant (e) is referenced, not duplicated, per the coordination note pointing at Prompt 33b STEP 5 (`tests/test_cost_meter_budget_atomicity.py::test_flask_ask_route_is_unreachable_behind_the_asgi_mount`). Building the suite surfaced two real drift bugs, fixed in the same pass:
  - Flask's `_security_blocked_ask_payload` was missing the top-level `ai_cited_sources` key.
  - `asgi.py` had **no dedicated `security_blocked` branch at all** — a referral/domain-block result fell through into success-shaped rendering, so the block message got formatted as if it were a real halachic answer and was fed into `_store_user_memory_summary()`. Fixed with `asgi.py::_security_blocked_ask_async_payload`, mirroring `app.py::_security_blocked_ask_payload`.
  - A third drift matching invariant (c): `asgi.py`'s `render_structured_markdown` call site was silently dropping `is_simple`, so a short question could render in full "Deeper Reasoning" format on the ASGI transport (the one actually reachable in production) while Flask rendered it compact. Fixed by passing `is_simple=bool(result.get("is_simple", False))`, matching app.py's existing call.
- **STEP 3:** exact required text added to `.agents/ENGINEERING_RULES.md` rule 7.
- **STEP 4:** decided keep, not delete, for `backend/customs.py`. It's live — the sole implementation backing `backend/ai_tools.py`'s `search_community_customs` agentic tool — but it is *not* authoritative for the main `/ask` pre-fetch pipeline any more (that pipeline uses the Supabase `community_knowledge` table via `backend/rag.py::_retrieve_community_knowledge`). The agentic tool can't call that function directly because `backend/rag.py` does a lazy `import app as _app` and `backend/ai_tools.py` must stay backend-only — a real, pre-existing constraint. Documented in place in `backend/customs.py`'s module docstring, including the accepted Supabase-drift trade-off and the scoped-out follow-up fix. Full writeup: `plan.md` §22.6.
- **Verify:** full suite green, `.venv/bin/python3 -m pytest -q`, exit 0, 91.18% coverage (85% floor). `graphify update .` run.
- Docs updated in the same commit: this file (Prompt 30's banner, this section) and `plan.md` (§18.3, §22, the customs.py row at §24.6).

## Prompt 36 — §23: schema & documentation provenance (the drift that already shipped a bug)

```
Read plan.md §23 in full. This exists because documentation drift here is not a tidiness problem — it caused a silent production data-loss bug, and every condition that produced it is still in place.

THE BUG, so you understand the stakes: backend/routes_user.py:76-82 documents in-code that a previous accept_legal() used on_conflict="clerk_id" — matching docs/DATABASE.md, not the schema. PostgREST rejected every write, a broad except swallowed it, and LEGAL-CONSENT RECORDS WERE NEVER PERSISTED until someone noticed. Wrong doc -> code written against the doc -> broad except + sub-INFO logging hides it.

STEP 1 — DO THIS FIRST, TODAY, INDEPENDENT OF EVERYTHING ELSE:
git add and commit the five untracked schema-defining SQL files:
  scripts/migrate_ai_usage_log_add_user_columns.sql
  scripts/migrate_ask_history_safety_metadata.sql
  scripts/migrate_user_preferences_legal_consent.sql
  scripts/migrate_user_preferences_legal_version.sql
  scripts/migrate_user_preferences_user_id_to_text.sql
These back currently-used, load-bearing columns (safety_class/prompt_version on every completed answer; the per-user budget columns Prompt 33b depends on; legal-consent versioning). Re-clone this repo today and there is no record they were ever written. Add a header comment to each recording whether/when it was applied to production — an honest "applied 2026-XX, approximate" beats no record. If provenance is genuinely unknown, write "provenance unknown as of 2026-08-19" rather than guessing a date.

STEP 2 — reconstruct the missing base table:
ai_usage_log's base CREATE TABLE is in the repo NOWHERE — not tracked, not untracked. Derive it from the live schema (STEP 3's introspection gives you this) and commit as scripts/sql/ai_usage_log_setup.sql. Prompt 33a/33b depend on this table heavily and it is currently unreproducible in a new environment.

STEP 3 — make docs/DATABASE.md GENERATED, not hand-written:
Write scripts/generate_database_doc.py — connects with the service-role key, introspects information_schema.columns / pg_policies / pg_indexes, emits docs/DATABASE.md in a stable deterministic format (sorted, no timestamps in the body, so diffs are meaningful). Add a --check mode that regenerates into memory, diffs against the committed file, exits non-zero on any difference. Give the generated file a header: "Generated by scripts/generate_database_doc.py on YYYY-MM-DD against project <ref>. Do not hand-edit."

DO NOT just hand-correct the prose and move on. That is what produced the current state — the doc has been wrong long enough to ship a bug; the failure is the ABSENCE OF A CHECK, not the presence of typos. Correcting the content is still required, but as OUTPUT of the generator.

DO NOT adopt the Supabase CLI migration workflow (supabase/migrations/, supabase db push, shadow-DB diffing) in this pass. It is the better long-term answer and deserves its own scoped section, but it is a workflow migration against a live production project holding real user data, and it does not fix the doc — a correct migration history and a wrong DATABASE.md coexist fine. Sequencing it ahead of STEP 1 would leave five files untracked while a larger project gets scoped.

For reference, what the doc currently gets wrong (verify each yourself, do not trust this list):
  - tenant key documented as clerk_id (:3,:17,:25-28,:42,:61,:71,:86,:109,:113) — real: user_id everywhere, including in the RLS policy examples
  - rag_identity_cache table (:36-50) — does not exist; the linked SQL creates community_knowledge + user_memories
  - bookmarks table (:54-75) — real table is study_bookmarks
  - queries analytics table (:79-97) — exists in no SQL file and no Python file
  - supabase db push workflow (:120-127) — there is no supabase/ directory in this repo
  - retention table (:133-140) — names two tables that do not exist, omits both tables the real job (routes_privacy.py:272-307) actually sweeps

STEP 4 — share ONE credential path with Prompt 34:
Wire --check into the same manually-triggered / scheduled GitHub Actions workflow as Prompt 34's scripts/verify_rls.py. Both need live Supabase credentials and a deployed target; both answer "does the repo still describe reality." NOT a per-PR gate — a schema change legitimately lands before the doc regen.

STEP 5 — close the swallow that hid the bug:
Audit Supabase write paths for the broad-except + sub-INFO-log shape. cost_meter.py:50-59 is a confirmed instance (Prompt 33a STEP 6 owns it — coordinate, do not fix twice). routes_privacy.py:301 uses _capture_backend_error correctly and is the model to copy. A PostgREST schema error must never be indistinguishable from success. Where a write is genuinely best-effort, keep it non-fatal but route it through _capture_backend_error().

STEP 6 — the 17 dangling plan.md citations (§23.3):
A restructure removed §7's numbered subsections; seventeen citations across code, tests, ENGINEERING_RULES.md and plan.md itself still point at them. This is NOT a find-and-replace — §7.13/§7.14 documented invariants that TESTS STILL ENFORCE, so their content was lost while the enforcement survived. plan.md §23.4 now restores those two invariants as their permanent home. Do this:
  a) Repoint these at plan.md §23.4: backend/claude.py:94, backend/helpers.py:949, app.py:1758, tests/test_ask.py:302/:366/:429, tests/test_config_lint.py:2, .agents/ENGINEERING_RULES.md:54, templates/index.html (grep for "§7.13" — it was :9364, now :9763, it moves).
  b) backend/helpers.py:94's §7.10.2 is a CSP/header item — point it at §11's frontend-platform lineage, not §23.4.
  c) backend/routes_privacy.py:60 cites "plan.md §3 retention windows" — §3 is "Existing infrastructure this refactor must reuse." Wrong target, not merely stale. Point it at §8.D.5.
  d) plan.md's own internal §7.10 (x6), §7.3, §7.2, §3.15, §1.4, §2.2-style refs point at a PRIOR ROADMAP REVISION whose content is not recoverable from the working tree. Do NOT guess at what they said. Either recover the old revision from git history, or delete the citation and say so. Report which you did for each.
  e) THE CONTROL THAT MAKES THIS THE LAST TIME: add to tests/test_config_lint.py (which already exists for exactly this class of problem) a check asserting every "plan.md §N" citation in the repo resolves to a heading that actually exists in plan.md. One regex over the repo, one over plan.md's headings. Prove it has teeth by pointing a citation at a fake section and confirming red.

STEP 7 — §23.4's invariants have real content to fix, not just anchors:
Invariant A records that MODEL_REQUEST_TIMEOUT_SECONDS (50) can NEVER fire because AI_TOTAL_BUDGET_SECONDS (45) always wins first — dead configuration that looks intentional. And nothing asserts at startup that the budget stays under vercel.json's maxDuration: 90. Make the per-model timeout a deliberate fraction of the total budget, and add the startup assertion to tests/test_config_lint.py.

VERIFY: five SQL files committed; ai_usage_log's CREATE TABLE reconstructed; generate_database_doc.py committed with working --check; docs/DATABASE.md regenerated and matching live schema; zero dangling plan.md citations, enforced by a lint that fails when one exists.

Update plan.md §23 and this file's Prompt 36 row. Stop and report.
```

## Prompt 37 — §24: sweep — quick wins, health-check honesty, and config defaults

**§24.3's `vercel.json` commit is a two-minute action and should happen immediately** — three `plan.md` sections currently rest on config that is not in git.

```
Read plan.md §24 in full, including §24.7 (what was deliberately NOT planned, and why — do not re-open those).

STEP 1 — COMMIT vercel.json (do this first, it takes a minute):
Verified 2026-08-19: `git show HEAD:vercel.json` has keys ["$schema","functions","headers"]; the working tree has ["$schema","crons","functions","headers","regions"]. The iad1 region pin and BOTH cron jobs (/api/devtools/budget-check 13:00 UTC, /api/devtools/retention-enforce 14:00 UTC) exist only in the working tree. Until committed, three plan.md sections rest on config not in git: §14.2's region pin, §16.3-L1's "exactly one WAF counter pool" synergy, and §20/§8.D's cron-driven jobs.

STEP 2 — reconcile the rule the crons contradict, do not just commit past it:
plan.md §14.6.1 stated as a hard rule: "No cron jobs, no keepalive pings, no warmers." There are now two crons. plan.md §14.6.1 has been AMENDED (2026-08-19) to permit scheduled jobs doing real non-request-path work while still forbidding warmers. MIRROR THAT EXACT AMENDED WORDING into .agents/ENGINEERING_RULES.md, which still carries the un-amended version (plan.md §14.7.5 put it there). An unamended rule with two live violations trains the next reader to ignore the rule.

STEP 3 — .env.example defaults that would ship as live defects if copied verbatim:
  CLERK_ENFORCE_AUTH=false (:67) -> flip to true. Fail-safe default; a developer who wants it off sets it off deliberately. As shipped it reopens anonymous /ask AND degrades the per-user budget key to a shared ip: bucket for everyone behind one NAT.
  DAILY_BUDGET_USD= (:98) -> add "# REQUIRED IN PRODUCTION" marker. Empty makes the only whole-app spend signal a no-op.
  CRON_SECRET= (:102) -> same marker. It gates BOTH cron routes.
  CLERK_AUDIENCE -> unset by default with NO runtime warning anywhere, not even on /api/stack/health (which reports clerk.configured/clerk.enforced but not whether audience verification is active). Add it to the same list.
Then add a startup check logging a WARNING naming each production-critical var that is unset.
OPERATOR ACTION (Akiva, separate from the code): confirm the LIVE Vercel values for all four and record them in docs/RUNBOOKS.md. Note budget-check is currently a NO-OP in production because DAILY_BUDGET_USD is unset — it fires daily, costs an invocation, does nothing. Configure it or disable it.

STEP 4 — health endpoints that cannot go red:
  a) asgi.py:282 "flask_mounted": True is a hardcoded literal, never verified. Make it a real probe or delete the field.
  b) /api/stack/health's supabase.ready is a config-presence check — constructing a Supabase client makes no network call, so a wrong key or paused project still reports ready:true. Make it a trivial round trip, or rename it supabase.configured and say so.
  c) external_apis reports Gemini/Claude "up" forever. Verify yourself: is_healthy / record_success / record_failure are called ONLY from backend/utils/search_provider.py (sefaria :237,247,250,253; web :459,465,470,633,638,642) — never from backend/claude.py, app.py or asgi.py, whose provider call functions catch and swallow their own exceptions without ever reporting into the breaker. EITHER wire record_success/record_failure into the four backend/claude.py provider call paths — which ALSO delivers the cost win plan.md §14.4.4 wrongly claimed was already shipped (corrected 2026-08-19; see §24.5) — OR remove Gemini/Claude from health_check.py's _circuits entirely. A health field that cannot go red is worse than a missing one: it suppresses the question.

STEP 5 — the Sefaria disk cache: VERIFY before assuming, then decide:
backend/sefaria_library.py:108 writes to _PROJECT_ROOT / ".sefaria_cache" — inside the deployed function bundle, on a filesystem Vercel makes read-only outside /tmp. _disk_cache_set wraps mkdir+write_text in a bare `except Exception: pass` (:139-140), so in production it fails SILENTLY every time. plan.md says "almost certainly" — make it certain first: add a bounded (once-per-process, not per-call) INFO log on write failure and read production logs. Then:
  RECOMMENDED: delete the disk tier entirely and be honest that the cache is two-tier in production, and fold a SHARED Sefaria cache into Prompt 29b's Upstash work as a follow-on once that connection exists. Repointing at /tmp buys almost nothing over the in-memory tier already there while preserving a "three-tier cache" story that is not true where it matters.
  EITHER WAY: remove the silent except. A cache tier that cannot report its own failure is how this went unnoticed. If the tier survives in any form, bound its size — 27MB/185 files locally already, eviction only by passive TTL-staleness on read.

STEP 6 — observability gaps that make incidents invisible (fold into §8.E):
  a) No global exception handler on either framework. The structured-error funnel is only called from /ask, user, privacy and devtools routes. The calendar, community, library and prayers blueprints log locally and NEVER reach Sentry or the webhook.
  b) A fully silent exception swallow in the calendar blueprint's parasha lookup — no log line at any level.
  c) Bearer-token verification failures logged at debug — invisible at default LOG_LEVEL=INFO. A wave of expired/forged tokens hitting /ask produces zero signal.

STEP 7 — CI gates, decide deliberately:
ruff and pip-audit both run continue-on-error: true — lint failures and known CVEs never block. Advisory is a defensible choice; UNDOCUMENTED advisory is not. Either make them blocking or write the rationale into .github/workflows/ci.yml as a comment. Separately: npm run build:css is wired into neither CI nor deploy, so the committed tailwind.css can silently drift from templates — that one belongs to plan.md §19's frontend track, note it there rather than fixing it here.

VERIFY: `pytest -q` green; vercel.json committed; ENGINEERING_RULES.md's cron rule matches plan.md §14.6.1's amended wording; no health field reports a status it does not measure.

Do NOT re-open anything in plan.md §24.7. Update plan.md §24 and this file's Prompt 37 row. Stop and report.
```

## Prompt 38 — §25: fix the Supabase test-mock response shape (found during Prompt 33b)

**Small, self-contained, test-infrastructure only — no application behavior should change.** Read `plan.md` §25 in full before starting.

```
Read plan.md §25 in full, plus §2, §6. During Prompt 33b, tests/conftest.py's autouse mock_outbound_httpx fixture (:237-241) — which mocks every outbound POST to https://mock.supabase.co/.* with a single fixed body, httpx.Response(200, json={"data": [], "error": None}) — turned out not to match any real PostgREST response shape (a bare array for table operations, the function's return value directly for RPC calls). This silently made backend/cost_meter.py::_reserve_budget_or_deny() read bool(None) as a deny, 402-ing 13 unrelated /ask tests. The call-site parsing was already tightened as part of 33b (cost_meter.py's _reserve_budget_or_deny now requires isinstance(result.data, list) and an "allowed" key before trusting a row). This prompt fixes the fixture itself, which is the actual source of the gap.

STEP 0 — GOLDEN MASTER FIRST (§6.2):
Before touching the fixture, add a test that constructs a client the same way backend/app.py::_get_supabase_client() does, points it at the mocked https://mock.supabase.co base URL, calls both a .table("ai_usage_log").select(...).execute() and a .rpc("check_and_reserve_user_budget", {...}).execute() under the CURRENT fixture, and asserts what result.data actually is today (a dict, not a list — pin the current wrong shape explicitly). Confirm it passes against current code, report to me, THEN change the fixture.

STEP 1 — replace the blanket mock with shape-accurate, endpoint-specific ones:
Split mock_outbound_httpx's single catch-all POST mock into at least two respx routes matched by URL pattern: one for .../rest/v1/<table> table operations (insert/update/upsert), returning a bare JSON array (e.g. [{"id": "...", ...}] or [] for a no-op), and one for .../rest/v1/rpc/<function> RPC calls, returning a bare array of row objects shaped like the function's actual RETURNS TABLE(...) — check_and_reserve_user_budget's real shape is [{"allowed": true, "total_usd": 0.0, "reservation_id": "<uuid>"}]. Do not just special-case this one function; make the RPC mock's default row shape configurable per-test (a fixture parameter or a per-test respx override) since more RPC functions may exist by the time this lands — check for any added since 2026-08-20.

STEP 2 — make the shape itself pinned, not just today's callers:
Add a test asserting that round-tripping the mocked response through the real postgrest-py/supabase-py client produces isinstance(result.data, list) == True for both the table and RPC cases. This is the test that stops the fixture from silently regressing back to a wrapper-envelope shape — it must fail if someone reverts STEP 1's mock to the old {"data": ..., "error": ...} body.

STEP 3 — re-verify nothing was accidentally relying on the wrong shape:
Full pytest -q. If any test now fails, that test's assertions or mocks were themselves built around the wrong response shape (e.g. asserting result.data == {"data": [], "error": None} directly, or a monkeypatched Supabase client returning that same wrong envelope) — fix the test to reflect what a real response looks like. Do NOT loosen the STEP 1/STEP 2 shape accuracy to make an old test pass.

VERIFY: STEP 0's golden master is inverted (proves the fixture used to be wrong). STEP 2's shape-pin test exists and would fail against the old mock (temporarily revert STEP 1, confirm red, re-apply). No application code in backend/ changes as part of this prompt — if you find yourself editing backend/cost_meter.py or any other non-test file to make this pass, stop, that means the real defect is elsewhere and this prompt's scope is wrong. pytest -q green.

Do NOT touch application code, plan.md §16, or any other pending prompt's scope. Update plan.md §25 and this file's Prompt 38 row when done. Stop and report.
```

## Prompt 39 — §26: close the claude/gemini circuit-breaker gap, add dark-theme to the a11y CI gate, clean up test lint (found during Prompts 16–19)

**Three independent, small findings from implementing Prompts 16–19 (§8.D–H) — none blocks the others, do in any order.** Read `plan.md` §26 in full before starting; it has the full rationale, evidence, and exit criteria for each of the three sub-items below (§26.1, §26.2, §26.3).

```
Read plan.md §26 in full, plus §8.E (reliability) and §8.F (quality/accessibility). This prompt closes three independent gaps found while implementing Prompts 16-19: the claude/gemini circuit breakers are registered in backend/health_check.py but never consulted before the primary /ask AI call; the new pa11y-ci CI accessibility gate (added by Prompt 18) only ever checks light theme; and ruff reports lint errors concentrated in tests/. Do all three in this pass, but treat them as independent — a failure or scope surprise in one should not block the other two.

STEP 1 — claude/gemini circuit breakers (plan.md §26.1):
In backend/claude.py, find the primary Gemini call site and the Claude fallback-of-the-fallback call site (if it exists as a separate call, not just an except branch). Before each, check health.is_healthy('gemini') / health.is_healthy('claude') the same way backend/routes_calendar.py's nominatim call and the four hebcal call sites (zmanim_engine.py, search.py, calendar_service.py, routes_calendar.py — wired in this same work, use them as the direct template) already do: on an open circuit, skip the call and fall through to the next stage in the EXISTING fallback chain rather than attempting it. Wire record_success/record_failure around each attempt symmetrically. Do not change /ask's response shape, its existing fallback ordering, or what it returns when both circuits are closed — this only changes whether a doomed call is attempted when a circuit is already open. Add a test mirroring tests/test_ask.py::TestAskDegradationPath's existing pattern (both Flask and FastAPI transports) that forces is_healthy('gemini') and is_healthy('claude') to both return False and asserts /ask reaches the local-fallback path (get_halakhic_sources()) without either AI call being attempted (monkeypatch/spy on the call functions to prove this, not just assert on the response).

STEP 2 — dark-theme a11y CI coverage (plan.md §26.2):
Read templates/index.html's existing theme-detection script to see exactly how it reads/sets the stored theme preference (localStorage key name). Extend .pa11yci.json (or the CI step that invokes it in .github/workflows/ci.yml) so all 8 currently-configured pages are also checked in dark theme, using pa11y's `actions` array to set that same localStorage key before each check runs (and reload if the theme script only applies on load — verify this by testing locally, don't assume). Prefer duplicating each URL entry with a dark-theme action block over a second config file, unless the CI step structure makes a second config cleaner — your call, but keep it in the same "Accessibility scan" CI step rather than adding a new one, so a red run reads as one signal not two. Run pa11y-ci locally against a live dev server (same way Prompt 18's original pass verified 8/8 light-theme pages) and confirm all pages pass in both themes before touching CI config. Update docs/ACCESSIBILITY_AUDIT.md and templates/accessibility.html's automated-check bullet to say both themes are covered.

STEP 3 — ruff cleanup in tests/ (plan.md §26.3):
Run `ruff check .` and fix every reported error in tests/: move E402 (module-level import not at top) imports to the top UNLESS there's a genuine ordering dependency (e.g. a sys.path shim, a monkeypatch of an env var before import) — check each one before moving, don't blanket-move; remove F401 unused imports; rename E741 ambiguous single-letter variables (l/I/O) to something clear from context; split E401 combined imports onto separate lines. No behavior change — this is formatting/hygiene only. If ruff check . still reports anything after your pass that is a deliberate exception (not a mistake), add a narrowly-scoped # noqa with a one-line reason, don't suppress broadly.

VERIFY: pytest -q green after all three steps (STEP 1's new test included). ruff check . reports zero errors (or documented # noqa exceptions only). pa11y-ci passes 0 violations across all pages in both themes when run locally against a live server — paste that output, don't just claim it. No change to /ask's response shape or existing fallback test assertions from before this prompt.

Do NOT touch anything in plan.md §16 (rate limiting) or reopen any other closed prompt's scope. Update plan.md §26 (mark each of §26.1/§26.2/§26.3 done with evidence) and this file's Prompt 39 row when done. Stop and report.
```

---

## Prompt 40 — §27: findings from implementing Prompt 20 (§9 agentic tool-use) — for Opus 5

**Four independent findings surfaced while implementing Prompt 20 — none blocks the others, do in any order except where noted.** Read `plan.md` §27 in full before starting; it has the full rationale, evidence, and exit criteria for each sub-item (§27.1–§27.5). These are explicitly *not* part of Prompt 20's own deliverable — Prompt 20 is done and its own acceptance bar (`pytest -q` green with `AI_AGENTIC_TOOLS` both unset and `=true`) is met; this prompt is the follow-up envelope for what that implementation pass found but was out of scope to fix inline.

```
Read plan.md §27 in full, plus plan.md §9 (agentic tool-use, just implemented) and §22 (the ask_pipeline.py decision this prompt's STEP 3 revises). This prompt closes four independent findings from implementing Prompt 20: two pre-existing bugs now reachable through the new agentic tools (no User-Agent on the Wikipedia connectors; a None-timezone bug in get_monthly_events), one planning-document tension Prompt 20's own completion made half-stale (Prompt 35's "delete ask_pipeline.py" recommendation), and one plan.md table-accuracy gap. Treat all four as independent — a scope surprise in one should not block the others.

STEP 1 — Wikipedia connector User-Agent (plan.md §27.1):
In backend/search.py, add a descriptive User-Agent header to _get_async_client()'s httpx.AsyncClient(...) construction (:36-43) and to the _HTTP session used by the sync search_wikipedia (:25, :46-74) — match the existing precedent already in this same file at :214-215 / :348-349 (Mozilla/5.0 compatible ShelahBot string) rather than inventing a new one, unless you have reason to point it at Wikimedia's actual documented User-Agent policy (https://meta.wikimedia.org/wiki/User-Agent_policy) instead. Add a test asserting the outgoing request to en.wikipedia.org carries the header (mock at the httpx/requests transport level, assert on request.headers). If you have live network access, confirm a real call no longer 403s; if not, say so explicitly rather than claiming it's verified.

STEP 2 — get_monthly_events timezone bug (plan.md §27.2):
In backend/zmanim_engine.py, get_monthly_events (:445), the Hebcal URL built at :486 uses the raw timezone_str parameter instead of the already-resolved tz_name local variable (computed at :451 via _resolve_timezone specifically to handle a None/missing timezone, and already used correctly for the GeoLocation object at :452). Fix :486 to use tz_name. Add a regression test calling get_monthly_events(lat, lon, timezone_str=None) with the Hebcal HTTP call mocked, asserting the outgoing tzid query parameter is never the literal string "None". Confirm the existing 30-day solar-events behavior and caching key are unchanged.

STEP 3 — resolve the ask_pipeline.py tension (plan.md §27.3, revises Prompt 35 / plan.md §22):
Prompt 35 recommended deleting backend/ask_pipeline.py as fully dead code (verified 2026-08-19). Prompt 20 has since made half of it live: run_agentic_ask is called directly by both app.py and asgi.py behind the AI_AGENTIC_TOOLS flag. The other half — the pre-existing run_ask_pipeline/AskPipelineResult exports — remains exactly as dead as Prompt 35 found it (only importers are its own two dedicated test files). Re-scope: (a) delete ONLY run_ask_pipeline/AskPipelineResult and their dedicated tests (tests/test_ask_pipeline.py, tests/test_ask_pipeline_smoke.py) — confirm via repo-wide grep that nothing outside those two test files references either name before deleting, matching Prompt 35 STEP 1's original verification discipline; (b) keep the module and run_agentic_ask; (c) still build Prompt 35 STEP 2's tests/test_ask_transport_parity.py — pin the Flask and ASGI /ask handlers to each other on: identical top-level response key set on every path (success/strict-block/AI-failure fallback — extend tests/test_ask.py:366's existing partial coverage, don't duplicate it), identical retry/timeout behavior, identical prompt-selection thresholds, classify_safety() invoked before every synthesis call on both transports, a per-user budget check present on both (or an explicit test asserting the Flask route is unreachable in production — coordinate with Prompt 33b STEP 5 if that hasn't landed, don't implement the check twice), and identical meta key set. Update plan.md §22 and Prompt 35's own text in this same commit to reflect the actual split state (module partly live, partly deleted) rather than leaving a fourth/fifth document describing it as fully undecided.

STEP 4 — plan.md §9.2b table accuracy (plan.md §27.4):
get_community_profile and get_prayer_text in backend/ai_tools.py were implemented as backend-only reimplementations, not wrappers around the app.py-only functions plan.md §9.2b's table originally named (ai_tools.py may only import backend.*, never app, per Prompt 20's own constraint). Read each handler's docstring in backend/ai_tools.py and update plan.md §9.2b's two table rows to describe what was actually built, with a one-line note on why it differs. Documentation-only, no code change.

VERIFY: pytest -q green (both AI_AGENTIC_TOOLS unset and =true, matching Prompt 20's own bar) after STEPS 1-3. graphify update . Report the LOC/coverage delta from STEP 3's deletion.

Do not touch plan.md §16 (rate limiting) or reopen any other closed prompt's scope. Update plan.md §27 (mark each of §27.1-§27.4 done with evidence) and this file's Prompt 40 row when done. §27.5 (Gemini agentic follow-up) is a scoping note, not a task — no action required unless product direction changes. Stop and report.
```

---

## Prompt 41 — §28: findings from implementing Prompt 36 (§23 schema & documentation provenance) — for Opus 5

**Two independent findings surfaced while implementing Prompt 36 — read `plan.md` §28 in full before starting; it has the full rationale and evidence for each (§28.1–§28.2). These are explicitly *not* part of Prompt 36's own deliverable — Prompt 36 is done (§23.2.1/§23.2.3/§23.2.4/§23.2.5–§23.3 landed, see §23's updated status) — this prompt is the follow-up envelope for what that implementation pass found but was out of scope to fix inline. STEP 1 below is a human decision point, not a task to execute unilaterally — read its framing carefully before doing anything.**

```
Read plan.md §28 in full, plus plan.md §23 (schema & documentation provenance, just implemented). This prompt closes two independent findings from implementing Prompt 36: one repo-wide operational risk (~150 modified/untracked files that have sat uncommitted since 2026-08-20, spanning the accumulated work of many prompts this project has landed since) that needs the repo owner's judgment rather than an autonomous commit, and one small, safe doc-drift item (a SQL file named for a table it doesn't create). Treat both as independent.

STEP 1 — uncommitted working tree (plan.md §28.1) — HUMAN JUDGMENT REQUIRED, DO NOT COMMIT UNILATERALLY:
`git status --porcelain` currently shows roughly 150 modified/untracked paths accumulated since the last real commit (`97da76d`, 2026-08-20), spanning nearly every prompt landed since Prompt 12, including several the status tables in this file already call "✅ Done." Do not run a single `git add -A && git commit` for this — that would stop the growth of the risk but forfeit the per-prompt commit history that §23's own schema-provenance discipline (and any future `git bisect`/audit) depends on. Instead: confirm with the repo owner whether and how to proceed, then stage and commit in the increments the status tables already describe — roughly one commit per completed Prompt row, oldest first, using each row's own evidence paragraph as the commit-message basis. If the repo owner is not available to confirm, stop after reporting the current `git status` breakdown (file counts, oldest uncommitted change, a proposed grouping) rather than committing anything.

STEP 2 — misleading SQL filename (plan.md §28.2):
Rename `scripts/sql/rag_identity_cache_setup.sql` to something accurate (e.g. `community_knowledge_and_user_memories_setup.sql`) — it creates `community_knowledge` and `user_memories`, not a `rag_identity_cache` table. Update the four prose cross-references: `docs/PRIVACY_OPERATIONS.md:51`, `backend/routes_privacy.py:19` (comment), `docs/DATABASE.md`, and `scripts/README.md:33`. Confirm via repo-wide grep that nothing invokes this file by name programmatically before renaming (expected: it's hand-run in the Supabase SQL editor like every other file in `scripts/sql/`), matching the verification discipline used elsewhere in this document.

VERIFY: `pytest -q` green after STEP 2 (STEP 1 is not a code change and has no test surface of its own). `graphify update .` if STEP 2 touches tracked files. Report exactly what STEP 1 resulted in (committed / awaiting confirmation / declined) — do not silently skip reporting it just because it's a non-code step.

Do not touch plan.md §16 (rate limiting) or reopen any other closed prompt's scope. Update plan.md §28 (mark §28.1 and §28.2 done with evidence, or mark §28.1 explicitly deferred-pending-owner-decision if declined) and this file's Prompt 41 row when done. Stop and report.
```

---

## Prompt 42 — §29: findings from implementing Prompt 25 (§12 Phase 6 product surface) — for Opus 5

**Seven independent findings surfaced while implementing Prompt 25 — read `plan.md` §29 in full before starting; it has the full rationale and evidence for each (§29.1–§29.7). Prompt 25 itself landed §12.1 (About), §12.2 (Help + glossary), §12.3.1/§12.3.3/§12.3.4 (breadcrumbs, citation deep-links, community-knowledge circuit breaker), §12.4 (feedback loop), and §12.5.2–12.5.4 (JSON-LD, consent-gated analytics, robots.txt/sitemap.xml) — full pytest green (91%+ coverage), verified in-browser against a live dev server. §12.3.2 (search filter UI) was deliberately NOT built — see §29.1, it is the one real scope gap, not a "found while implementing" defect. Treat all seven items below as independent unless a step says otherwise.**

```
Read plan.md §29 in full, plus plan.md §12 (product surface, just implemented) and §11 (the Vercel rewrite this prompt's STEP 2 depends on). This prompt closes seven independent findings from implementing Prompt 25: one deliberately descoped sub-item (the library search filter UI), one planning-document/production-config contradiction that needs a live-deploy answer, one missing content page the sitemap spec assumed exists, three "plan.md said this was missing but it already shipped" corrections (documentation-only), one known double-counting limitation in the new feedback widget, and one live production bug found incidentally while browser-verifying this pass's own changes (unrelated to §12, but real). Treat all as independent except where a step says otherwise.

STEP 1 — build the library search filter UI (plan.md §29.1, closes §12.3.2):
`backend/search.py`'s `search_library(query, filters)` and the frontend's `appState.prefs.metadataFilters` / `normalizeMetadataFilters()` / `appendMetadataFilters()` (`templates/index.html`) already accept and forward `era`/`author`/`category`/`geography`/`nusach` — confirmed live plumbing, still exercised by zero UI control. This was deliberately not built in Prompt 25 (not a defect found — a risk-scoped cut): the topbar search area (`templates/index.html:175-205`) is already dense (search input, suggestions dropdown, settings menu, multiple z-index layers), and shipping an untested filter panel into it risked a visual regression with no time left in that pass to verify across mobile/RTL/dark-mode. Design and build a filter control (a collapsible panel or a dedicated `/library` filter row — your call) that writes into `appState.prefs.metadataFilters` and triggers `pushPrefsToServer()`/the next search call the same way the existing state does. Free-text inputs are acceptable for `author`/`geography`/`nusach` (no fixed taxonomy exists to build a dropdown from); `category` may use a dropdown if `get_library_index()`'s category tree is cheap to expose as options. Verify in-browser: light + dark theme, mobile viewport (375px), RTL (Hebrew mode), keyboard operability, 44×44 touch targets per `.agents/ENGINEERING_RULES.md`.

STEP 2 — resolve the vercel.json rewrites contradiction (plan.md §29.2, revises plan.md §11/§14.3.2):
`plan.md` §11 and §14.3.2 both assert a rewrite with a negative-lookahead regex already excludes `static/`, `favicon.ico`, `manifest.webmanifest`, `service-worker.js` (and, per §12.5.1, was meant to be extended for `robots.txt`/`sitemap.xml`) from being routed to the Python function. The live `vercel.json` (re-read fresh during this pass) has **no `rewrites` key at all** — only `functions`/`regions`/`crons`/`headers`. Given `favicon.ico`/`manifest.webmanifest`/`service-worker.js` are themselves Flask routes in `app.py` (not physical files at the repo root — confirmed by reading `app.py:1434-1449`), not "static assets" in the sense §11's text describes, this needs a real preview deploy (this prompt's own STEP 2 is gated on Prompt 24's verification checklist landing first, or being run alongside) to determine: (a) whether Vercel is in fact routing every path to the function via some other config this pass didn't find, (b) whether §11/§14.3.2's claims were simply never true and the negative-lookahead needs to be written for the first time, or (c) whether the routing works today for an unrelated reason (e.g. a project-level Vercel dashboard setting outside `vercel.json`). Given this uncertainty, Prompt 25 deliberately did NOT touch `vercel.json` — `robots.txt`/`sitemap.xml` were instead added as Flask routes in `backend/routes_pages.py` (same pattern as the pre-existing favicon/manifest/service-worker routes), so they inherit whatever routing behavior those already have in production, correct or not. Resolve the contradiction for real (via Prompt 24's preview-deploy checklist), then update `plan.md` §11/§14.3.2's text to match reality and, if a rewrite turns out to be genuinely missing, add it as its own reviewed change — not bundled silently into an unrelated commit.

STEP 3 — build a real parasha HTML page, or drop it from the sitemap spec (plan.md §29.3):
`plan.md` §12.5.1 lists "parasha page" among the sitemap's stable public routes. No such page exists — only `backend/routes_calendar.py:282` (`GET /api/parasha`, JSON only). `backend/routes_pages.py::sitemap_xml`'s `_SITEMAP_PATHS` list currently omits it for this reason. Either build a real crawlable `/parasha` (or similar) page — this is a content/IA decision (what does a permalink for "this week's parasha" even mean across weeks — a stable slug per parasha name, most likely, not a "this week" URL that goes stale), not a small template change — or update `plan.md` §12.5.1's text to drop the claim. Do not add a fake entry to `_SITEMAP_PATHS` pointing at the JSON endpoint just to satisfy the list.

STEP 4 — plan.md documentation corrections, no code (plan.md §29.4):
Three `plan.md` claims used to justify §12 scope were stale by the time this pass verified them against current code — all three are already-shipped, not defects:
  (a) §12.3.3's citation-deep-link framing ("net-new (small)") is stale — `templates/index.html`'s AI-source-box delegated click listener already called `readText(ref, {skipNavigationGrid:true})` on `a.source-local-link` clicks before this pass touched anything. No new work landed here; verified via code read only.
  (b) §12.5.2's "no JSON-LD exists" (also implicit in §12.0's claim-triage row) is stale — a `WebApplication` JSON-LD block already existed on `templates/index.html`'s `<head>`. This pass added `WebSite`+`Organization` alongside it (as §12.5.2 asked), not in place of it.
  (c) §12.3.1's framing of breadcrumbs as needing new backend work is stale — `backend/sefaria_library.py::get_text`/`_parse_v3_response` already returned a `categories` field on every `/api/text/<ref>` response before this pass. The breadcrumb work that landed was frontend-only (a `nav[aria-label="breadcrumb"]` wrap + reusing the existing JS write-site).
Update `plan.md` §12.0's claim-triage table and §12.3's numbered list to reflect (a)-(c) as already-shipped-before-Prompt-25, matching this project's own "claims that were wrong or already shipped are recorded, not re-planned" discipline stated in §12's own intro. Documentation-only.

STEP 5 — feedback-widget double-submit is a known, undocumented-until-now counting skew (plan.md §29.5):
`templates/index.html`'s `renderFeedbackWidget()` submits the bare verdict to `POST /api/feedback` immediately on 👍/👎 click (so a reader who never fills in the optional comment box — the common case — still gets counted, rather than losing the verdict to comment-box abandonment). For 👎, if the reader then submits a comment, a **second** `/api/feedback` row is inserted with the same verdict plus the comment. `backend/routes_devtools.py::feedback_digest`'s `helpful`/`not_helpful` counts are therefore inflated by 1 for every 👎 that gets a followed-through comment. A real fix needs the first insert to return its row id (Supabase's default `Prefer: return=representation` already provides this) and a second endpoint that PATCHes that row's `comment` field in place, rather than inserting twice. Decide whether this is worth building now or living with (documenting the skew in the digest view's own text is the minimum bar either way).

STEP 6 — `/api/user/preferences` UUID mismatch — live production bug, unrelated to §12 (plan.md §29.6):
Found incidentally while browser-verifying this pass's own changes against the real (non-mocked) Supabase project with a real signed-in Clerk session: `GET /api/user/preferences` (`backend/routes_user.py:143`, pre-existing code untouched by Prompt 25) threw `psycopg`/PostgREST error `22P02: invalid input syntax for type uuid: "user_3DJ2PONd1x9zBnfVRlfiGhMzQPr"`. The real `user_preferences.user_id` column is typed `uuid` in the live schema, but Clerk `sub` claims are strings like `user_3DJ2PONd1x9zBnfVRlfiGhMzQPr` — never valid UUIDs. This means preferences sync is currently broken for every real signed-in user in production, independent of anything in §12. Root-cause and fix (likely: the column should be `TEXT` like `ask_history.user_id` and `answer_feedback.user_id` both correctly are — check whether this is the *only* table with this mistyped column, since it may point at a copy-paste error in whichever migration created `user_preferences`).

STEP 7 — apply the answer_feedback migration + verify RLS live (plan.md §29.7):
`scripts/migrate_answer_feedback.sql` (Prompt 25) has not been run against any real Supabase project — confirmed via live browser test: `POST /api/feedback` currently 500s against the real dev database with `PGRST205: Could not find the table 'public.answer_feedback'`. Apply the migration, then confirm the feedback INSERT policy behaves as intended for both anonymous and signed-in callers, and — matching this project's own already-flagged §21 gap ("verify RLS is real, don't just assert it") — confirm no `anon`/`authenticated` role can SELECT from `answer_feedback` at all (the migration deliberately defines no SELECT policy, so only the backend's secret-key client should be able to read it for the devtools digest). This was asserted correct by policy design in Prompt 25 but never checked against a live RLS-enforcing Postgres, the same caveat every other RLS claim in this codebase carries until §21 lands its own acceptance test.

VERIFY: full `pytest -q` green after STEPS 1, 3, 4, 6 (STEP 2 depends on a live preview deploy and may not be code-verifiable in this environment; STEP 5's decision may or may not produce a code change; STEP 7 is a live-database action, not a local test). `graphify update .` for any code change. Report exactly what STEP 2 concluded (rewrite added / confirmed unnecessary / still unresolved) and what STEP 7 found when applied — do not silently skip reporting either just because they're not local-test-shaped.

Do not touch plan.md §16 (rate limiting) or reopen any other closed prompt's scope. Update plan.md §29 (mark each of §29.1-§29.7 done with evidence) and this file's Prompt 42 row when done. Stop and report.
```

---

## Prompt 43 — §31: findings from implementing Prompt 29a (§16 Phase 9a rate limiter unification) — for Opus 5 — ✅ DONE 2026-08-26 (all three steps closed: STEP 1 test coverage landed, STEP 2 confirmed set by operator, STEP 3 confirmed no change needed)

**Three independent findings surfaced while implementing Prompt 29a's remaining scope (the unified ASGI middleware + Upstash store) — read `plan.md` §31 in full before starting; it has the full rationale and evidence for each (§31.1–§31.3). Prompt 29a is done (`backend/rate_limit.py` shipped, Flask-Limiter and asgi.py's old independent limiter both removed, full `pytest -q` green — see `plan.md` §16.9) — this prompt is the follow-up envelope for what that implementation pass found but was out of scope to fix inline. STEP 2 below is an operator action, not a task to execute unilaterally — read its framing carefully before doing anything.**

```
Read plan.md §31 in full, plus plan.md §16.9 (the Phase 9a reconciliation this prompt follows up on) and §16.3-L2 (the asymmetric fail-open/fail-closed posture STEP 1 tests). This prompt closes three independent findings from implementing Prompt 29a: one real test-coverage gap on a security-critical code path (the fail-open/fail-closed posture has never actually been exercised by a test, despite the original Prompt 29a spec asking for exactly this), one infrastructure/env-var confirmation that needs the operator's Vercel dashboard access, and one documentation-currency question about DECISIONS.md. Treat all three as independent.

STEP 1 — test the fail-open/fail-closed posture (plan.md §31.1):
backend/rate_limit.py's _check() (lines 207-227) implements the one deliberately asymmetric piece of this whole module: the llm class fails CLOSED when the store raises _StoreUnavailable (policy.fail_open=False → allowed=False, i.e. /ask gets rejected during a store outage rather than running unmetered), every other class fails OPEN (policy.fail_open=True → allowed=True, i.e. reader traffic isn't blocked by a Redis blip). Confirmed via the full pytest run's coverage report: lines 215-227 (the entire `except _StoreUnavailable` branch) are uncovered — no test currently stubs the store to raise and checks either posture. Add tests/test_rate_limit_middleware.py: (a) monkeypatch backend.rate_limit._store to a fake object whose incr() raises backend.rate_limit._StoreUnavailable, hit /ask via fastapi_client with a fresh X-Forwarded-For IP (pick an unused one — see the TEST-NET-3 reservation note in plan.md §16.9's last bullet), and assert the response is the 429-shaped rejection (fail-closed for llm); (b) same stubbed-unavailable store, hit a cheap/fanout/feedback-class route (e.g. GET /api/stack/health or an /api/feedback POST) and assert it succeeds normally (fail-open). Also assert backend.logging_setup._capture_backend_error (or its Sentry-bound wrapper) is invoked in both cases — spy on it via monkeypatch, matching the pattern other tests in this suite already use for _capture_backend_error assertions. Do not use a real Redis connection; a stub store object exercising the _check()/_StoreUnavailable contract directly is the correct level of test.

STEP 2 — confirm RATE_LIMIT_REDIS_URL is set in production (plan.md §31.2) — OPERATOR ACTION, DO NOT ATTEMPT TO SET VERCEL ENV VARS UNILATERALLY:
This implementation pass had no access to Vercel's production environment configuration. backend/rate_limit.py falls back to an in-process store (D3's exact per-instance-counter defect, plan.md §16.1) whenever RATE_LIMIT_REDIS_URL is unset — confirm with the repo owner whether it's set in the live Vercel project. If not, that's the one remaining concrete action closing D3 in effect, not just in code: provision an Upstash Redis database (free tier, per plan.md §16.3-L2) and set RATE_LIMIT_REDIS_URL there. Verify post-deploy via GET /api/stack/health's security.limiter_store field reading "redis". If the repo owner is not available to confirm or act, stop after reporting the current unknown status rather than guessing or attempting to modify Vercel config from this environment.

STEP 3 — DECISIONS.md currency question (plan.md §31.3):
DECISIONS.md still describes "two independent rate limiters" as current fact in several places (its rate-limiting decision entry's "What was chosen"/"Where it lives" fields, its Clerk-JWKS-outage aside, production-readiness checklist items, its cost-model section — plan.md §31.3 has the specifics). This file reads as an ADR-style point-in-time snapshot elsewhere in this project's own usage, so before editing it: confirm with the repo owner (or infer from how other already-resolved DECISIONS.md entries have or haven't been retroactively updated elsewhere in the file) whether it's meant to stay frozen at time-of-writing or track current state. If the latter, update the rate-limiting entry to describe the unified backend/rate_limit.py middleware and cross-reference plan.md §16.9; if the former, leave it and note in plan.md §31.3 that the convention was confirmed as "frozen historical record, no update needed."

VERIFY: pytest -q green after STEP 1 (new tests plus full suite). graphify update . if STEP 1 adds a new tracked file. Report STEP 2's outcome (confirmed set / confirmed unset and provisioned / awaiting operator) and STEP 3's outcome (updated / confirmed frozen-by-design) explicitly — do not silently skip reporting either just because they're not code changes.

Do not touch plan.md §16.3-L1 (Vercel WAF) or §16.3-L3 (cost circuit breaker) — those are Phase 9b, Prompt 29b, still blocked on §20a per plan.md §16.7. Do not reopen any other closed prompt's scope. Update plan.md §31 (mark each of §31.1-§31.3 done with evidence) and this file's Prompt 43 row when done. Stop and report.
```

**✅ Done 2026-08-26.** STEP 1: `tests/test_rate_limit_middleware.py` added — a stub store whose `incr()` always raises `_StoreUnavailable`, monkeypatched onto `backend.rate_limit._store`; four tests via `fastapi_client` prove `POST /ask` (llm class) 429s and `POST /api/feedback` (feedback class) still 200s under the same simulated outage, each paired with a `_capture_backend_error` spy assertion. Full `.venv/bin/python3 -m pytest -q`: green, `backend/rate_limit.py` coverage 90%, the `_check()` `except _StoreUnavailable` branch no longer appears in the coverage report's missing lines. `graphify update .` run. STEP 2: not attempted from this session directly (no Vercel dashboard access), but the repo owner confirmed same-day that `RATE_LIMIT_REDIS_URL` is already set in the live Vercel production environment — D3 (plan.md §16.1) is closed in effect, not just in code. STEP 3: confirmed `DECISIONS.md` is a frozen point-in-time snapshot (its own header says "as of 2026-08-19," "re-verify before citing," and it carries zero retroactive-correction markers anywhere, unlike `plan.md`) — left unchanged, no edit made. Full evidence trail in `plan.md` §31.1-§31.3.

---

## Prompt 44 — §32: findings from implementing Prompts 26/27 (§13 Phase 7 Sonar debt, §14 Phase 8a Vercel quick wins) — for Opus 5

**Six independent findings, read `plan.md` §32 in full before starting — it has the complete radon hotspot list, evidence, and rationale for each (§32.1–§32.6). Prompts 26 and 27 are done (3 of 51 complexity hotspots refactored and individually committed — `search_provider.py`, `sefaria_library.py`, `app.py`; the `vercel.json` routing regression fixed and committed; the anthropic/google-genai SDK imports deferred in `backend/claude.py`; full `pytest -q` green — see `plan.md`'s §13 status note and §14.8) — this prompt is the follow-up envelope for what that pass found but was out of scope to fix inline. §32.2 is the most time-sensitive; §32.4 is an operator decision, not a task to execute unilaterally.**

```
Read plan.md §32 in full, plus plan.md's §13 status note and §14.8 (the Prompt 26/27 implementation this prompt follows up on). This prompt closes six independent findings. Treat each as independent except where noted.

STEP 1 — commit the templates/index.html dialog fix, and review the rest of that file's pending diff (plan.md §32.2, the most time-sensitive item):
templates/index.html currently has ~1,900 lines of uncommitted changes in the working tree (git diff --stat templates/index.html), only ~10 of which are this session's verified fix (removing a duplicate closeChapterGrid() call from the global keydown Escape handler — chapterGridModal's own native 'cancel' listener already handles Escape, so the duplicate call double-fired). The rest is pre-existing, unreviewed, uncommitted work from an earlier session: an analytics-consent-gate feature (initAnalyticsConsent/loadVercelAnalytics, §12.5.3/§8.A.4), two application/ld+json JSON-LD blocks (§12.5.5), a components/icons.html template import, and the already-noted #legalModal removal (§13.2's 2026-08-16 update). Review the full diff, split it into separate reviewed commits by feature (dialog fix; analytics consent; JSON-LD; anything else you find), test/verify each independently before committing (the dialog fix was verified in-browser this pass: open/close/backdrop-click confirmed for both calendarModal and chapterGridModal via the flask-dev launch config). Confirm both modals are still native <dialog> with .showModal()/.close() (they already are, per §32.2) — §13.2 item 1 is functionally done, just needs to land in git.

STEP 2 — continue the complexity-hotspot backlog (plan.md §32.1):
48 functions across app.py, backend/sefaria_library.py, backend/helpers.py, backend/routes_library.py, asgi.py, backend/utils/search_provider.py, and backend/utils/text_engine.py are still radon C-grade or worse (3 D-grade: app.py::_run_ask_question_ai_synthesis, asgi.py::ask_async, backend/utils/search_provider.py::_collect_external_global_sources; 45 C-grade — full list in plan.md §32.1). Continue exactly the pattern Prompt 26 established: pick the worst remaining offender, refactor it alone (guard clauses → flattened conditionals → early returns), test-anchor, commit — one function, one commit, one review. Do NOT batch into a bulk pass. Start with the 3 D-grade functions; check whether app.py::_run_ask_question_ai_synthesis and asgi.py::ask_async are near-duplicates of each other (Flask vs ASGI entry points for the same synthesis step) before refactoring both independently — a shared-helper extraction might resolve both at once.

STEP 3 — defer app.py's supabase import (plan.md §32.6):
backend/claude.py's anthropic/google-genai SDK imports were deferred to first-use this pass (commit de819c1) using an _ensure_X_loaded() lazy-singleton pattern. app.py's module-level `from supabase import create_client` is the same class of cold-start cost but was left eager — its blast radius is larger (referenced across many route/service files importing from app.py). Trace every supabase-client usage reachable from app.py's module scope, confirm none execute at import time (decorator arguments, route-registration defaults, or other import-time-evaluated contexts), then apply the same lazy-loader pattern. Full pytest green after.

STEP 4 — calendarModal backdrop-click-to-close (plan.md §32.4) — CONFIRM INTENT FIRST, DO NOT ADD SILENTLY:
chapterGridModal has a backdrop-click-to-close handler; calendarModal does not (only Escape and an explicit close control). This may be intentional (harder-to-accidentally-dismiss for calendar interactions) or an oversight. Ask the repo owner before adding one — if confirmed as an oversight, add the same pattern to calendarModal as its own small, in-browser-verified commit, not bundled with anything else.

STEP 5 — plan.md §13.1 accuracy note (plan.md §32.3): no action needed, already reads correctly after this pass's edit — just confirm it stays accurate if you touch that section further.

STEP 6 — testing-methodology note (plan.md §32.5): no code action. If you build the browser-test framework flagged in earlier prompts' gaps (Playwright/Cypress/jsdom), check whether it has the same synthetic-Escape-key limitation discovered this pass (Chromium's native <dialog> close-watcher did not respond to this session's automation tool's synthetic Escape keypress, confirmed via an isolated bare-<dialog> test page) before relying on it to verify <dialog> Escape-to-close end-to-end.

VERIFY: pytest -q green after STEP 2 and STEP 3 (each verified independently, not just at the end). graphify update . after any code change. Report STEP 1's outcome (which commits landed, what each contains) and STEP 4's outcome (confirmed intentional / added / awaiting operator) explicitly.

Update plan.md §32 (mark each of §32.1-§32.6 done with evidence) and this file's Prompt 44 row when done. Stop and report — do not start Prompt 28 (cache-tier work) or any other closed prompt's scope.
```

---

## Prompt 45 — §33: findings from implementing Prompts 28/29b (§14 Phase 8b cache tiers, §16 Phase 9b WAF + cost breaker) — for Opus 5

**Eight independent findings, read `plan.md` §33 in full before starting — it has the complete rationale for each (§33.1–§33.8). Prompts 28 and 29b are done (CDN cache tiers wired into both transports, cross-user cache-leak closed, global cost breaker shipped and wired into the real `/ask` path, frontend paused-state banner verified live in both themes — see `plan.md` §14.10/§16.10) — this prompt is the follow-up envelope for what that pass found but needed a decision, an operator action, or was out of scope to fix inline. §33.1 and §33.5 need a human decision before any further code; §33.4 and §33.8 are operator/deploy-only.**

```
Read plan.md §33 in full, plus plan.md's §14.10 and §16.10 status notes (the Prompt 28/29b implementation this prompt follows up on). This prompt closes eight independent findings. Treat each as independent except where noted.

STEP 1 — decide plan.md §33.1 (the cost-breaker primitive deviation) — CONFIRM INTENT, DO NOT SILENTLY RESOLVE EITHER WAY:
backend/cost_meter.py::is_global_cost_breaker_tripped() reuses backend/rate_limit.py's Redis-backed shared store instead of the plan's literal instruction to reuse backend/utils/search_provider.py's in-process APIHealth breaker. The rationale: search_provider.py's breaker is per-instance, not cross-instance-safe (exactly the D3 defect §16.1 already names), so reusing it for a "global" ceiling would silently make the ceiling per-instance instead. Read plan.md §33.1 in full, confirm this reasoning holds, and either (a) bless the deviation and correct §16.3-L3's text in place to name backend/rate_limit.py's store instead of search_provider.py's breaker, so a future pass doesn't "fix" this back to the D3-recreating version, or (b) direct a different resolution. Do not change the running code without first making this call explicitly.

STEP 2 — make the plan.md §20c / Prompt 33c decision (plan.md §33.5) — now unblocked:
Prompt 33c recommended option A (the per-user ceiling is a good-faith guardrail, not an anti-abuse control; the real ceiling is the global breaker + WAF) "conditional on Prompt 29b shipping." 29b has shipped. Read plan.md §20.2's three options and Prompt 33c's row, make the call, and record it in both plan.md §20.2 and the Prompt 33c row in this file — this project has flagged this decision as must-not-leave-unresolved twice already.

STEP 3 — verify the X-Deploy-Hash assumption (plan.md §33.2):
apply_response_cache_policy() assumes Vercel's CDN auto-purges cached entries on deploy. This was never checked against current Vercel docs. Confirm it (WebFetch Vercel's current CDN/edge-cache-invalidation-on-deploy documentation) and record the finding in plan.md §33.2 and docs/VERCEL_COST_OPTIMIZATION.md's X-Deploy-Hash caveat. If false, scope what changes (likely nothing beyond documentation, given the short TTLs already in place — but confirm rather than assume).

STEP 4 — decide plan.md §33.6 (the missing Retry-After countdown):
The frontend breaker-paused banner shipped with a static "try again after midnight UTC" string instead of the countdown the original Prompt 29b Part C text described, because the breaker response is a 200 with no Retry-After header to key a countdown off. Decide whether a real countdown is still wanted. If yes, it needs a new response field (e.g. meta.breaker_resets_at, an ISO timestamp, derived from the breaker's UTC-midnight reset boundary) threaded through backend/cost_meter.py -> asgi.py's _ask_async_breaker_paused_payload -> the frontend's existing formatCountdownDuration/startCountdown helpers. If no, close this finding with a one-line note in plan.md §33.6 confirming the static string is the accepted final state.

STEP 5 — operator actions, do not attempt to script these (plan.md §33.4, §33.8):
- §33.4: capture the §14.7.1 Vercel Usage-dashboard baseline (30 days, per meter, cached-vs-uncached ratio) if not already done, and run the §14.7.4 preview-deploy verification checklist (cache headers + CDN HIT on cacheable routes, no-store still correct on /ask and authed routes, static assets edge-served, no cross-user leakage fetched as two different users).
- §33.8: enter the docs/SECURITY.md §7 WAF rules into the actual Vercel dashboard (1 rate-limit rule + 3 custom rules), deploy in Log mode first, read a week of real traffic before switching to enforce.
Report back what you could and couldn't complete from this environment — do not fabricate dashboard results.

STEP 6 — confirm DAILY_BUDGET_USD gets a real value before relying on the breaker (referenced in the Prompt 29b row, ties into plan.md §24.4):
The global cost breaker is live code with an inert threshold as long as DAILY_BUDGET_USD ships empty (.env.example:98, and per docs/RUNBOOKS.md unset in production). This is the same operator-action class as §33.4/§33.8 — confirm the production value is actually set, or flag loudly if it is not, since an unset threshold means is_global_cost_breaker_tripped() always returns tripped=False regardless of real spend.

STEP 7 — plan.md §33.3/§33.7 need no action (informational, already fixed in the prior pass) — just confirm they still read accurately if you touch those sections further.

VERIFY: pytest -q green after any code change (STEP 4, if a countdown is built). graphify update . after any code change. Report STEP 1 and STEP 2's outcomes explicitly (which option was chosen and why) — these are the two findings this prompt cannot close without a human decision.

Update plan.md §33 (mark each of §33.1-§33.8 done with evidence) and this file's Prompt 45 row (add one, matching the Prompt 40-44 pattern) when done. Stop and report — do not start any other closed prompt's scope.
```

**✅ Done 2026-08-23** — see the Suggested execution order table row 15 below and `plan.md` §33 for the full evidence trail of what closed. Two findings from this pass are filed as Prompt 46/§34 immediately below.

---

## Prompt 46 — §34: findings from implementing Prompt 45 (§33 follow-up: cost-breaker sign-off, §20c decision, cache-purge verification) — for Opus 5

**Two independent findings surfaced while implementing Prompt 45 — read `plan.md` §34 in full before starting; it has the complete rationale for each (§34.1–§34.2). Prompt 45 is done (§33.1/§33.2/§33.3/§33.5/§33.6/§33.7 closed; §33.4/§33.8 remain the same operator/deploy-only blockers they always were — see `plan.md` §33.9) — this prompt is the follow-up envelope for what that pass found. §34.1 is a repeat of an already-flagged, unresolved finding that has gotten worse, not a new class of problem — it is the single most urgent item in this envelope and needs a repo-owner decision before anything else in this queue.**

```
Read plan.md §34 in full, plus plan.md §33.9 (the Prompt 45 pass this prompt follows up on) and plan.md §28.1 (Prompt 41's original version of this same uncommitted-tree finding, from 2026-08-20). This prompt closes two independent findings. Treat both as independent.

STEP 1 — the uncommitted working tree, again, larger — HUMAN JUDGMENT REQUIRED, DO NOT COMMIT UNILATERALLY (plan.md §34.1):
This is the third time this exact finding has surfaced (Prompt 36's implementation found it first at 2026-08-20; Prompt 41/§28.1 filed it at ~150 paths; it is now 181 paths, the last real commit is still ab98b59 from 2026-08-22, and templates/index.html's own uncommitted diff — separately flagged as "the most time-sensitive item" in Prompt 44/§32.2 — has grown from ~1,900 to 1,931 changed lines rather than landing). Re-run git status --porcelain yourself first to get the current true count (it will be higher than 181 by the time this runs). Do not run a single git add -A && git commit — same reasoning Prompt 41 gave: it would forfeit the per-prompt commit granularity this project's own §23 schema-provenance discipline depends on, and roughly a sixth of the current paths are .claude/ session files and graphify-out/ cache artifacts whose inclusion in version control is itself a judgment call, not a mechanical one. Confirm with the repo owner whether and how to proceed — Prompt 41's proposed plan (stage and commit in increments, roughly one commit per completed Prompt row this file already describes, oldest first, using each row's own evidence paragraph as the commit-message basis) is still the standing recommendation unless the repo owner directs otherwise. If the repo owner is not available to confirm, stop after reporting the current git status breakdown (file counts by category, oldest uncommitted change, a proposed grouping) rather than committing anything — matching the exact precedent Prompt 41 already set. This has now been left unresolved across three prompts; do not let a fourth pass repeat the same "stop and report" without also proposing a concrete owner-facing decision point (e.g., "confirm yes/no by the next prompt, or this stops being asked").

STEP 2 — refresh docs/SECURITY.md §5's stale rate-limiter description (plan.md §34.2):
§5's "Rate limiters confirmed active in prod" bullet and its following "Known, already-documented gap" bullet both still describe the pre-unification Flask-Limiter + independent asgi.py limiter architecture Prompt 43/§31 replaced on 2026-08-22 with the unified backend/rate_limit.py Starlette middleware backed by an Upstash Redis shared store. Confirm via grep that every remaining Flask-Limiter/flask_limiter reference in the codebase is a historical comment, not a live dependency (this was true as of 2026-08-23), then rewrite both bullets to describe backend/rate_limit.py's actual current shape: identity-aware keys (Clerk sub when authenticated, IP+JA4-ish fallback otherwise), the llm-class fail-closed / everything-else fail-open posture (Prompt 43/§31.1 added test coverage for this — tests/test_rate_limit_middleware.py), and the RATE_LIMIT_REDIS_URL-set-or-in-process-fallback behavior with its loud boot-time warning. Documentation-only, no code change.

VERIFY: no code change expected from either step (STEP 1 is a git-history action or a stop-and-report; STEP 2 is docs-only) — pytest -q need only be re-run if STEP 2's grep-verification process touches anything unexpected. graphify update . not needed unless STEP 1 results in commits that change tracked source files' history in a way graphify's cache should reflect.

Do not touch plan.md §16.3-L1/L3 implementation details or reopen any other closed prompt's scope beyond what STEP 1/STEP 2 describe. Update plan.md §34 (mark §34.1 and §34.2 done with evidence, or §34.1 explicitly deferred-pending-owner-decision if declined — matching Prompt 41's own precedent for this exact situation) and this file's Prompt 46 row (add one, matching the Prompt 40-45 pattern) when done. Stop and report.
```

---

## Prompt 47 — §35: findings from implementing Prompt 29c (§16 Phase 9c: identity-aware quotas, Turnstile, mitigation observability) — for Opus 5

**One finding, critical severity, code-only — read `plan.md` §35 in full before starting; it has the complete root-cause chain (§35.1) and the recommended fix. Prompt 29c is done (identity-aware keys, Turnstile gate, mitigation observability, and doc corrections all shipped and verified 2026-08-24 — see its status-table row) — this prompt is the follow-up envelope for the one severe, unrelated defect that pass's own new tests were the first in this codebase to expose. Treat this as the single highest-priority item in the queue: it is a complete, currently-live functional outage for every signed-in `/ask` caller, not a latent risk.**

```
Read plan.md §35 in full before starting. This prompt has one step.

STEP 1 — fix the authenticated-/ask crash (plan.md §35.1):
Every authenticated call to the real, production /ask endpoint currently returns HTTP 500. Root cause: backend/rag.py::_fetch_user_memory_summaries() calls app._get_user_scoped_supabase_client() with no guard around client construction; that chain (app.py::_get_user_scoped_supabase_client → _get_request_supabase_client → _extract_supabase_access_token → backend/auth.py::_extract_bearer_token) unconditionally reads Flask's global `request` proxy. asgi.py's native FastAPI /ask route (the one actually reachable in production — it is registered before the WSGIMiddleware mount, so it shadows app.py's own Flask /ask route) runs this via asyncio.to_thread with no Flask request context active at all, so any truthy user_id triggers `RuntimeError: Working outside of request context.`, caught by ask_async()'s outer handler (asgi.py:771-783) and converted to a silent 500 — no auth-related detail ever reaches the caller.

Fix by threading the already-parsed Authorization header value down explicitly instead of relying on Flask's implicit global `request`: asgi.py::ask_async() already parses this header once (for extract_user_id_from_bearer_value()) — pass that same value through to _fetch_user_memory_summaries(user_id, ..., bearer_token=<value>) → backend/rag.py's call site → app._get_user_scoped_supabase_client(bearer_token=...) → _get_request_supabase_client(bearer_token=...) → _extract_supabase_access_token(bearer_token=...), with each function falling back to its current Flask-request-reading behavior only when no explicit token is passed, so existing Flask-side callers (e.g. routes_user.py) keep working unchanged. Do not introduce a second, divergent token-extraction implementation (this project's own §2 anti-pattern) — the explicit-parameter fallback shape is what avoids that.

Once the real fix lands, remove the three test-only monkeypatch workarounds in tests/test_ask.py (search for "plan.md §35" / "Prompt 47" in that file's comments — TestAskIdentityAwareQuotas::test_authenticated_and_anonymous_share_ip_but_separate_buckets, ::test_authenticated_per_minute_allowance_exceeds_anonymous, TestAskTurnstileGate::test_authenticated_user_bypasses_turnstile_entirely all stub app._get_user_scoped_supabase_client to None specifically to isolate themselves from this bug) and add a new test that proves an authenticated /ask call with a real memory row actually succeeds end-to-end through the native FastAPI route — the exact case that was never covered before this finding.

VERIFY: pytest -q green with the three workaround monkeypatches removed; a new passing test exercising an authenticated /ask call through fastapi_client that reaches _fetch_user_memory_summaries() and does not raise; confirm the fix does not change behavior for the Flask-mounted /ask path (TestAskFlask tests must stay green); graphify update .. Stop and report.
```

> **✅ Done 2026-08-25.** Implemented the recommended fix (not the stopgap): `bearer_token`/`authorization` threaded explicitly through `backend/auth.py::_extract_bearer_token` → `app.py::_extract_supabase_access_token`/`_get_request_supabase_client`/`_get_user_scoped_supabase_client` → `backend/rag.py::_fetch_user_memory_summaries` → `asgi.py::_collect_ask_async_context`/`ask_async`, each function falling back to Flask's `has_request_context()`-gated `request` only when no explicit value is passed. All three workaround monkeypatches in `tests/test_ask.py` removed; new `TestAskAuthenticatedMemoryRetrieval::test_authenticated_call_with_real_memory_row_succeeds_end_to_end` proves the previously-500'ing path now returns 200 with real memory data. `tests/test_auth.py` and `tests/test_rag.py` gained direct regression tests for the new fallback behavior. Full suite: `RATE_LIMIT_REDIS_URL= .venv/bin/python3 -m pytest --no-cov -p no:warnings` → **1642 passed, 0 failed** (`TestAskFlask` stayed green). `graphify update .` clean. Full evidence in `plan.md` §35.1/§35.2. One new finding from this pass — a test-infrastructure `RATE_LIMIT_REDIS_URL` leak, unrelated to this bug — filed as **Prompt 48/§36** immediately below.

---

## Prompt 48 — §36: findings from implementing Prompt 47 (§35.1 authenticated `/ask` 500 fix) — for Opus 5

**One finding, medium severity, test-infrastructure-only — read `plan.md` §36 in full before starting; it has the complete root-cause chain (§36.1) and the recommended fix. Prompt 47 is done (see its status-table row) — this prompt is the follow-up envelope for a pre-existing test-infrastructure gap that pass's full-suite verification run surfaced. Not urgent (only manifests on a developer machine with a real `.env`), but cheap and worth closing before it causes another confusing debugging session.**

```
Read plan.md §36 in full before starting. This prompt has one step.

STEP 1 — stop RATE_LIMIT_REDIS_URL leaking from a developer's real .env into the test suite (plan.md §36.1):
tests/conftest.py already blanks SENTRY_DSN and SENTRY_DSN_BROWSER before any app-level import specifically because a developer's local .env may set them to real values and load_dotenv() (called on app import, override=False by default) won't clobber an already-set var — but RATE_LIMIT_REDIS_URL gets no equivalent treatment anywhere in that file. backend/rate_limit.py's module-level _store singleton is built once at import via _build_store(), which returns a real _RedisStore (wrapping redis.asyncio.Redis.from_url(...)) whenever RATE_LIMIT_REDIS_URL is truthy, and only falls back to the in-process _InMemoryStore when it's empty. On a machine with a real Upstash credential in .env, every test run therefore builds a real Redis-backed store instead of the intended offline fallback, with two consequences: (a) redis.asyncio's connection pool binds to whichever pytest-asyncio event loop is running at first use, and since _store is a session-wide singleton, a later test running under a fresh event loop hits RuntimeError: Event loop is closed, observed as a wide wave of seemingly-unrelated async test failures once enough /ask-hitting tests accumulate in one run; (b) every rate-limiter call any test makes is a genuine network round-trip against a live, shared Upstash instance, silently writing real counters.

Fix: add `os.environ.setdefault("RATE_LIMIT_REDIS_URL", "")` to tests/conftest.py's existing blank-before-import block, alongside the SENTRY_DSN/SENTRY_DSN_BROWSER lines, with a comment explaining why (mirror plan.md §36.1's suggested comment). No other file needs to change — backend/rate_limit.py's existing fallback logic already does the right thing once the var is actually empty.

VERIFY: run the full suite with a real RATE_LIMIT_REDIS_URL value present in the shell environment (simulating a developer's .env) both before and after the fix, confirming the "Event loop is closed" failure mode is eliminated, not just coincidentally absent; confirm tests/test_rate_limit.py (if it asserts anything about store selection) stays green; graphify update .. Stop and report.
```

> **✅ Done 2026-08-26, commit `9a7c159`.** Added `os.environ.setdefault("RATE_LIMIT_REDIS_URL", "")` to `tests/conftest.py`'s existing blank-before-import block exactly as specified. Found and fixed as a side effect of closing Prompt 49/§37 below — a real Upstash URL in this developer's `.env` was causing `tests/test_routes_feedback.py::TestFeedbackRateLimit` to fail against accumulated live counters instead of the expected in-process store, traced back to this exact pre-existing gap. Verified: full suite with a real `RATE_LIMIT_REDIS_URL` in the shell passes clean, all 9 `test_routes_feedback.py` tests green against `_InMemoryStore`, no `RuntimeError: Event loop is closed` anywhere. `graphify update .` clean. Full detail in `plan.md` §36.1.

---

## Prompt 49 — §37: the last of five stacked `ImportError`s — `backend/routes_devtools.py` still imports the deleted Flask-Limiter symbols and `backend/rate_limit.py` was never committed — for Opus 5

**One finding, critical severity, deploy-blocking — read `plan.md` §37 in full before starting; §37.1 has the complete file-by-file breakdown and §37.2 the ordering argument. `main` does not import.** Five independent `ImportError`s were stacked on the committed tree, each hiding the next; the 2026-08-25 session fixed and committed four of them (`5a018aa` `SECURITY_RESPONSE_HEADERS` + the 2026-08-24 CSP incident fix, `e7d2064` `submit_with_context`, `871a100` `_resolve_client_ip`/`_coarse_ai_error_reason`, `7a809b9` `routes_user.py`'s wrong-module auth import) and deliberately left the fifth, because it is an unfinished migration rather than a missing line. **This is the single highest-priority item in the queue — ahead of §35.1's framing and ahead of §34.1's backlog cleanup, both of which describe defects in a running application. This one is the reason there may not be a running application.**

```
Read plan.md §37 in full before starting, plus plan.md §16.8.1, §16.3-L2 and §16.9 for the rate-limiter unification's design rationale — that rationale is settled, do NOT re-derive or re-litigate it. Also read plan.md §34.1 (this is a strict, deploy-blocking subset of that same unresolved uncommitted-tree finding). This prompt has one step.

STEP 1 — land the rest of the §16 Phase 9a rate-limiter unification so HEAD imports again (plan.md §37.1):
Against the COMMITTED tree, python -c "import app" raises `ImportError: cannot import name 'limiter' from 'app'`, which Flask surfaces as `RuntimeError: Blueprint 'routes_devtools' from 'backend.routes_devtools' failed to load`. Reproduce it yourself the way it was found — export HEAD into a clean scratch directory and run the import there, NOT in the working tree, where it already passes. That difference is the entire bug.

Cause: Phase 9a was verified against a working tree and only partially committed. backend/rate_limit.py (350 lines, RateLimitMiddleware + _POLICIES + the store abstraction + get_shared_store()) is UNTRACKED — never committed once. asgi.py's `from backend.rate_limit import RateLimitMiddleware` (line 34) and `fastapi_app.add_middleware(RateLimitMiddleware)` (line 144) are correct in the working tree but uncommitted. app.py is already fully migrated AT HEAD (Flask-Limiter, RATE_LIMIT_ASK, RATE_LIMIT_DEFAULT all gone; SUPABASE_SERVICE_ROLE_KEY already renamed to SUPABASE_SECRET_KEY; only the explanatory comment at app.py:809-821 remains). backend/routes_devtools.py is the only straggler: its committed import block still asks app for `limiter`, `RATE_LIMIT_ASK`, `RATE_LIMIT_DEFAULT` and `SUPABASE_SERVICE_ROLE_KEY`, none of which exist anymore.

Commit the whole change as ONE reviewed commit — backend/rate_limit.py, asgi.py's middleware registration, backend/routes_devtools.py's corrected import block and /api/stack/health body, and the two untracked test files tests/test_rate_limit.py + tests/test_rate_limit_config.py. Do NOT fix routes_devtools.py's import line alone: importing backend.rate_limit from a tracked file while that module stays untracked just converts the ImportError into a ModuleNotFoundError and leaves main equally non-deployable.

The working tree already holds the corrected routes_devtools.py import block — but review it, do not paste it. Four things are entangled in that diff and each needs verifying against what the route handlers actually call (grep, don't trust): (a) limiter/RATE_LIMIT_ASK/RATE_LIMIT_DEFAULT are dropped but their USAGE was replaced, not deleted — /api/stack/health's security block (routes_devtools.py:66-76) now reads rate_limit.RATELIMIT_ENABLED / rate_limit.RATE_LIMIT_REDIS_URL / rate_limit._POLICIES off the module, which is the real implementation of §16.9's "/api/stack/health no longer lies" bullet and closes §16.8.1's confidently-wrong-answer finding, so this is a behavioral change riding along with an import fix and should be reviewed as one; while there, decide deliberately whether reading the underscore-prefixed _POLICIES across a module boundary is acceptable or should become a public accessor alongside the existing get_shared_store() — do not leave it ambiguous; (b) the SUPABASE_SERVICE_ROLE_KEY → SUPABASE_SECRET_KEY rename's consumer side (routes_devtools.py:29,82); (c) the newly added imports _extract_supabase_access_token (:199), _extract_client_ip (:275), _get_request_supabase_client (:221), STRICT_SUPABASE_RLS (:202) and `from backend import cost_meter, rate_limit` (:320) — all genuinely used, but confirm rather than assume, since an unused import here is the exact drift class that caused this; (d) the two test files cover the store abstraction and the RATELIMIT_ENABLED boot-time read, NOT RateLimitMiddleware's fail-open/fail-closed posture — so committing them does NOT close Prompt 43/§31.1's coverage gap, do not mark it closed. Two citation fixes while there: plan.md §16.9 already cites tests/test_rate_limit_config.py as existing coverage even though that file is not in git, and plan.md §31.1 cites a tests/test_rate_limit_middleware.py that does not exist under any name.

VERIFY: python -c "import app" and python -c "import asgi" both exit 0 against the COMMITTED tree (export HEAD to a scratch dir and run them there — that is the check that would have caught all five of these, and running it only in the working tree is what let them ship); pytest tests/test_rate_limit.py tests/test_rate_limit_config.py green; then the full suite with RATE_LIMIT_REDIS_URL blanked per plan.md §36.1 — that leak becomes reachable in CI for the first time once backend/rate_limit.py is actually tracked, so consider doing Prompt 48/§36.1 in the same pass; graphify update . and confirm no new backend/* → app top-level edge. Stop and report.
```

> **✅ Done 2026-08-26 — §37.2's three further blockers closed too, not just STEP 1's original scope.** STEP 1 itself (the rate-limiter unification's fifth `ImportError`) was already landed by a prior session as commit `a519a21` before this pass began. This pass verified that against a clean-room `git archive HEAD` export and found `main` *still* did not import — three further, previously-undocumented blockers (`plan.md` §37.2), closed here as three separate reviewed commits rather than one undifferentiated dump, each independently justified and independently verifiable:
> - **`7cf767c`** (§37.2.2, earliest in the import chain) — migrated `backend/zmanim_engine.py` off `import pytz` (not installed; `requirements.txt` deliberately dropped it for stdlib `zoneinfo`) to `from zoneinfo import ZoneInfo`. Verified specifically against this repo's `.venv` (matches `requirements.txt` exactly), since the machine's global `python3` has `pytz` installed and would have silently masked this exact bug.
> - **`f81ace6`** (§37.2.3) — landed `backend/cost_meter.py::check_daily_budget_and_alert()`, the function the newly-committed `/api/devtools/budget-check` cron route calls and which did not exist at `HEAD` (a live, wired-to-production-cron `AttributeError` waiting to fire), together with its full previously-verified-but-uncommitted §20a/§20b/§29b surface (`check_user_budget_and_enforce`, `expire_stale_budget_reservations`, `backend/logging_setup.py`'s budget-context helpers, `backend/health_check.py`'s `record_success`/`record_failure`/`reset`) rather than cherry-picking just the one function.
> - **`9a7c159`** (§37.2.1) — landed the four blueprint modules `app.py`'s already-committed `_BLUEPRINTS` list references but that were entirely untracked (`routes_legal`, `routes_privacy`, `routes_pages`, `routes_feedback`), plus their 7 template dependencies, `static/css/legal.css`, `static/data/glossary.json`, and `scripts/generate_glossary_json.py` — the exact closure needed for zero `ModuleNotFoundError`/`TemplateNotFound`, found iteratively via repeated clean-room export-and-import cycles rather than guessed upfront.
>
> **Verified:** `python -c "import app"` and `python -c "import asgi"` both exit 0 against a clean-room export of current `main` — for the first time since this investigation began. Full suite green except 3 known, deliberately-out-of-scope `TestLegalPagesCrossLinking` cases (see Prompt 50 below). `graphify update .` clean; no new `backend/*` → `app` top-level edge.
>
> **Deliberately left out of scope, by design** (matching this document's own "don't smuggle unreviewed content past an import fix" instruction): the large, content-sensitive `terms.html`/`privacy.html`/`accessibility.html`/`index.html`/shared-component diffs that add real cross-links to the new legal pages, and one test-isolation bug found in an untracked test file. Both filed as **Prompt 50/§38** immediately below, per this task's standing instruction to hand findings off as a new prompt rather than fix them inline.

---

## Prompt 50 — §38: findings from closing Prompt 49/§37.2 — legal-page cross-linking content review, and a test-isolation bug — for Opus 5

**Two findings, both low severity, neither deploy-blocking — `main` imports and the full suite is green. Read `plan.md` §38 in full before starting; §38.1 covers the content-review item, §38.2 the test bug, §38.3 confirms neither blocks the other or anything else in the queue. This is a genuine two-track prompt: STEP 1 is a content/UI review, STEP 2 is an isolated test fix; do them in either order or in parallel.**

```
Read plan.md §38 in full before starting. This prompt has two independent steps.

STEP 1 — review and commit the legal-page cross-linking content (plan.md §38.1):
templates/terms.html, templates/privacy.html, templates/accessibility.html, templates/index.html, templates/components/legal_topbar.html, templates/components/legal_scripts.html (all modified in the working tree) and templates/components/icons.html (untracked) add real cross-links between all 7 legal pages (the 4 committed by Prompt 49 — /ai-disclosure, /acceptable-use, /dmca, /licenses — plus the 3 pre-existing — /terms, /privacy, /accessibility). These were deliberately left uncommitted by Prompt 49 because each diff is large (600-1900 lines) and content-sensitive, not because anything is broken.

Before committing: read the diffs and determine whether the legal_topbar.html visual changes are a genuinely separate concern from the cross-linking additions (the icons.html split suggests they might be an unrelated topbar visual refresh riding along in the same working-tree state). If they are separable, land them as two commits — "add legal-page cross-links" and "refresh legal topbar visuals" — rather than one, since a visual change benefits from its own screenshot-backed review per .agents/ENGINEERING_RULES.md's UI/UX Pro Max standard (contrast in both themes, 44x44px touch targets, prefers-reduced-motion, no CLS). If they are not separable, land as one commit but say so explicitly in the commit message.

STEP 2 — fix the order-dependent test-isolation bug in tests/test_cost_meter_call_sites.py (plan.md §38.2):
This untracked file's TestAsyncGeminiPrimaryCallSite/TestSyncGeminiPrimaryCallSite classes monkeypatch backend/claude.py's _configure_gemini_client with a no-op lambda -- but the real _configure_gemini_client's body is what triggers _ensure_genai_loaded()'s one-time side effect of populating the module-level genai_types global. _call_gemini_httpx_model (backend/claude.py:1951) short-circuits with error: "gemini_sdk_missing" before ever reaching record_llm_call (:2017) if genai_types is still None. Run this file alone, the no-op lambda skips loading it and the test's captured_cost_calls fixture ends up empty (assert 0 == 1); run it as part of the full suite, an earlier test has already warmed the module-level cache, so it passes -- confirmed empirically both ways. Fix by either (a) monkeypatching genai_types (or the specific client call) directly instead of _configure_gemini_client wholesale, so the lazy-load side effect isn't bypassed -- more robust against future _configure_gemini_client changes -- or (b) calling the real _ensure_genai_loaded() in test setup before monkeypatching _configure_gemini_client -- smaller diff. Pick one; note which and why.

VERIFY: for STEP 1, pytest tests/test_routes_legal.py::TestLegalPagesCrossLinking -q --no-cov fully green (all 7 parametrized cases, not just the 4 that already pass) -- confirm the 3 currently-failing cases (/terms, /privacy, /accessibility) now pass; manual pass through both light and dark themes on the topbar specifically if it changed. For STEP 2, pytest tests/test_cost_meter_call_sites.py -q --no-cov green IN ISOLATION (not just as part of the full suite -- that is the entire point of this bug) both before confirming the failure and after confirming the fix. Then the full suite green end to end. graphify update .. Stop and report.
```

---

## Prompt 51 — §39: account-deletion completeness audit — a real orphaned table, and a way a user's own data can outlive their account entirely — for Opus 5

**Requested directly by Akiva (2026-08-26), not surfaced as a byproduct of another prompt — read `plan.md` §39 in full before starting; §39.1–§39.5 have the complete evidence and fixes. Isolation (no user can ever touch another's data) is already verified correct by construction and needs no code change. Completeness has one confirmed data gap (§39.1, Medium) and one confirmed structural gap (§39.2, High) — the second is the one that can silently defeat "delete my account" forever, for any user it happens to.**

```
Read plan.md §39 in full before starting, plus docs/PRIVACY_OPERATIONS.md §1/§3 for the existing DSR policy this extends. This prompt has two independent code steps plus one operator action plus two lower-urgency documentation follow-ups.

STEP 1 — close the answer_feedback gap (plan.md §39.1):
backend/routes_privacy.py's _USER_DATA_TABLES (:53-59) lists five tables; backend/routes_feedback.py's submit_feedback() (:48-63) writes a sixth, SUPABASE_ANSWER_FEEDBACK_TABLE ("answer_feedback", app.py:872), with a real Clerk sub in its user_id column for authenticated submitters plus a free-text comment field. It is in neither export_user_data() nor delete_account()'s loop today. Add ("feedback", SUPABASE_ANSWER_FEEDBACK_TABLE) to _USER_DATA_TABLES and import the constant from app the same way routes_devtools.py:35 and routes_feedback.py:19 already do -- this one change fixes both the export and delete endpoints simultaneously, since both iterate the same tuple. While there, double check no other app.py SUPABASE_*_TABLE constant is missing from _USER_DATA_TABLES the same way -- check against every route that ever writes a user_id-keyed row, not just this one instance.

STEP 2 — add a Clerk `user.deleted` webhook as a completeness backstop (plan.md §39.2):
delete_account() is currently the ONLY code path that deletes Supabase rows for a user. Two real operational paths bypass it entirely: (a) templates/index.html:4106-4118's openClerkUserProfile() opens Clerk's own hosted UserProfile UI, which ships a self-service "Delete account" control gated by a Clerk Dashboard setting this codebase has never recorded; (b) an operator deleting a user directly from the Clerk Dashboard (banned/abusive account, a manual support request) -- a completely normal, expected admin action that cannot be disabled. Either path deletes the Clerk identity without ever calling this app's endpoint, permanently orphaning every _USER_DATA_TABLES row (and answer_feedback, once STEP 1 lands) for that user_id -- with no Clerk JWT left to authenticate a retry.

Add a new backend/ router (e.g. backend/routes_webhooks.py, per .agents/ENGINEERING_RULES.md's "new routes go in backend/" rule) exposing a webhook endpoint that verifies the Svix signature Clerk signs its webhooks with (against a new CLERK_WEBHOOK_SIGNING_SECRET env var -- add to .env.example) and, on a user.deleted event, runs the same table-cascade delete already built (_delete_table_rows over _USER_DATA_TABLES, including answer_feedback once STEP 1 lands) keyed on the event payload's data.id. This must be idempotent -- if delete_account() already ran, the webhook's pass deletes zero additional rows, matching the existing idempotency note at routes_privacy.py:150. Do NOT remove the existing synchronous Clerk-identity-delete call from delete_account() -- keep both: the webhook is the backstop for doors this app doesn't control, the existing endpoint is what gives the user their immediate "ok": true confirmation.

STEP 3 -- operator action, do not attempt to script this (plan.md §39.2):
Check the Clerk Dashboard for whatever setting controls self-service account deletion from <UserProfile />. Record the outcome -- enabled / disabled / not applicable at this plan tier -- in docs/SECURITY.md, dated, matching the pattern already used for the Third-Party-Auth check (§21.2.1) and the WAF rules (§33.8). If it's enabled and STEP 2's webhook isn't built yet, flag that combination as the most urgent version of this gap, since it means a user could trigger it themselves today.

STEP 4 -- lower urgency, independent, slot in whenever (plan.md §39.3):
(a) Look up this Supabase project's actual backup/point-in-time-recovery retention window and document it, dated, in docs/PRIVACY_OPERATIONS.md -- it is currently undocumented anywhere in this repo, and most deletion-policy language is honestly phrased as "within N days," which needs a real N. (b) Hash user_id before it reaches Sentry at every _capture_backend_error call site that currently passes it raw (grep-confirmed at least 10 of 45 sites, including routes_privacy.py itself at :109-111 and :156-158) -- mirror the key_hash pattern backend/rate_limit.py's mitigation logging already uses (log_mitigation(..., key_hash, ...)) rather than inventing a new one. Do NOT build field-level per-user encryption / crypto-shredding for this -- plan.md §39.3 has the full options table and rationale for why that's the wrong tool here: every live-data store this app controls already gets a real DELETE (strictly stronger than crypto-shredding), and the one store where shredding would theoretically help (Supabase's platform-managed backups) has an exposure window that's already time-bounded by the provider's own retention -- shredding would narrow an already-bounded window, not close an unbounded one, at the cost of encrypting every content-bearing column app-wide and building key lifecycle management for it.

VERIFY: STEP 1 -- a test asserting SUPABASE_ANSWER_FEEDBACK_TABLE is in _USER_DATA_TABLES, plus an integration test inserting a feedback row for a fake user and confirming delete_account() removes it. STEP 2 -- a test simulating a valid-signature user.deleted webhook against a fake user with rows in every _USER_DATA_TABLES entry, asserting all are gone; a test asserting an invalid/missing Svix signature is rejected (signature verification IS this endpoint's auth); a test confirming a replayed/duplicate event is a no-op, not an error (Svix delivers at-least-once). Then the full suite green end to end. graphify update .. Stop and report.
```

---

### Suggested execution order

Revised for the 2026-07-28 status snapshot (Prompts 1–4 and 7 are already done — skipped below), with §16 inserted 2026-07-31 and **Prompts 33a–37 inserted 2026-08-19** from the `DECISIONS.md` audit.

> **⚠️ 2026-08-19 — the top of this queue changed.** Two of the new items are minutes of work that several other rows silently depend on, and one is a hard prerequisite for a row that was previously "next." The revised head of the queue is: **37-STEP-1 (commit `vercel.json`) → 36-STEP-1 (commit the 5 SQL files) → 33a (fix the ledger) → 34's operator action**. Only then does the pre-existing ordering resume. Rationale per row below.

| Order | Prompts | Track |
|---|---|---|
| 1 | ~~**27**~~ → **24** | Vercel: fix the invalid `memory` key + startup cost, then run the preview-deploy verification once, together. **24 is done (operator-verified live preview deploy, 2026-08-21) — done out of order, without 27 first.** **27 is now also done (2026-08-22)** — found and fixed a reintroduced routing regression in the uncommitted `vercel.json` (a literal `"/api/index"` rewrite destination, the exact bug `7c177b3` fixed) alongside the planned `memory` key removal + `ignoreCommand`; deferred `anthropic`/`google-genai` SDK imports for the startup-cost half. See `plan.md` §14.8 and Prompt 44/§32 for what's left (`app.py`'s `supabase` import still eager). |
| 2 | **4b** | §4 Phase 5 concurrency hardening (completes the backend sequence) |
| 0a | **37** (STEP 1 only) | 🟢 **Two minutes, do it now.** Commit `vercel.json` — the `iad1` region pin and both cron jobs exist only in the working tree, and §14.2, §16.3-L1 and §20/§8.D all currently rest on config that is not in git. |
| 0b | **36** (STEP 1 only) | 🟢 **One commit, do it now.** The five untracked schema-defining SQL files back load-bearing production columns, including the ones Prompt 33b depends on. Re-clone today and there is no record they exist. |
| 1a | 🔴 **33a** | **§20a — fix the cost ledger. Jumps to the front of the substantive queue.** `ai_usage_log.cost_usd` is \$0.00 for most production rows, so every cost claim in this project is currently false and both spend ceilings are inert. **Hard prerequisite for 29b and for 20.** |
| 1b | **34** (operator action only) | 🟡 Ask the Supabase dashboard whether Third-Party Auth for Clerk is enabled. Not code, blocks all of Prompt 34, and four production code paths silently depend on the answer. Do it early — the answer may take a day to get and the rest of 34 cannot start without it. New evidence for this exact question in `plan.md` §30.5 (2026-08-21, from a live Supabase Advisor scan) — a redundant, superseded `user_memories` RLS policy pair should be dropped once this is answered and 34's live check runs. |
| 3 | **29a** | ✅ **Done 2026-08-22; Prompt 43/§31 closed out 2026-08-26.** §16 Phase 9a — rate limiting. Unified ASGI middleware (`backend/rate_limit.py`) + Upstash-backed store shipped; Flask-Limiter and asgi.py's old independent limiter both removed. Fail-open/fail-closed posture now has dedicated test coverage (`tests/test_rate_limit_middleware.py`), and `RATE_LIMIT_REDIS_URL` is confirmed set in production by the operator — see `plan.md` §16.9 and §31 for the full trail. |
| 4 | 14 → 13 → 12 | Safety + legal (14 first: §9 depends on the classifier; 12 only needs the 4 missing legal pages) |
| 5 | **29b** | ✅ **Done 2026-08-22 (L3 + frontend); L1 entered 2026-08-26 (Akiva).** §16 Phase 9b — cost circuit breaker shipped and wired into the real `/ask` path; WAF (L1) dashboard rules entered per `docs/SECURITY.md` §7 — see §33.8's 2026-08-26 update for a correction found while doing it (Hobby tier is 3 total custom rules, not 1 rate-limit + 3 custom; no incident-response slot is held in reserve). **33c is decided (Option A, 2026-08-23, Prompt 45/§33.5)** — the one thing its honesty still rests on is `DAILY_BUDGET_USD`, still empty (§24.4). |
| 6 | 20 | ✅ **Done 2026-08-21, flag off by default.** Agentic tools implemented and merged; `AI_AGENTIC_TOOLS` is unconditionally off, so live behavior is unchanged and this did not need to wait for 29a to *land*. But the original rationale for gating on 29a still applies to *turning the flag on*: §9 multiplies per-question cost, and 29a's D3 (Upstash store + middleware unification) is still open (see row 3) — do not flip `AI_AGENTIC_TOOLS=true` in production before 29a-D3 closes. |
| 6b | ~~30~~ → **35** | ✅ **Done 2026-08-26, sixth pass.** `ask_pipeline.py` — **Prompt 30 is superseded by Prompt 35**, which makes the recommendation 30 withheld (delete + build a parity suite), re-scoped 2026-08-21 to delete only the still-dead half. Executed as re-scoped: `run_ask_pipeline`/`AskPipelineResult` and their two dead-code test files gone, `run_agentic_ask()` kept, `tests/test_ask_transport_parity.py` built covering all six invariants. See the Prompt 35 row/section for full evidence. |
| 6e | **33b** | §20b — atomic per-user budget ceiling. Needs 33a first. Its STEP 5 (Flask has no budget check) overlaps Prompt 35's parity invariant (e) — coordinate, do not implement twice. |
| 6f | **34** (code half) | §21 — RLS acceptance test, once 1b has an answer. Shares one credentials workflow with 36's schema-doc `--check`; build whichever runs first, reuse it for the other. |
| 6g | **36** (rest) | §23 — the schema-doc generator and the 17 dangling `plan.md` citations. STEP 6e's citation lint is the control that stops this recurring; STEP 7 fixes the dead `MODEL_REQUEST_TIMEOUT_SECONDS` config while there. |
| 6h | **37** (rest) | §24 — health endpoints, `.env.example` defaults, Sefaria disk cache, observability gaps. All small and independent; slot anywhere after 0a. |
| 6i | **33c** | §20c — the multi-account bypass decision. Can be *made* any time, but its recommended answer (A) is only honest once 29b ships. Do not close the roadmap without answering it. |
| 6j | **38** | §25 — fix the Supabase test-mock response shape. Small, test-infrastructure-only, no dependency on anything else; slot in whenever, but sooner is better since every `.rpc()`-calling test written before this lands inherits the same blind spot. |
| 6k | **39** | §26 — claude/gemini circuit breaker, dark-theme a11y CI, ruff cleanup in `tests/`. Three independent small fixes found while implementing 16–19; no dependency on anything else, slot in whenever. |
| 6l | **40** | §27 — findings from implementing 20: no-User-Agent Wikipedia connectors, a None-timezone bug in `get_monthly_events`, the now-half-stale Prompt 35 deletion premise (read before 6b/35), plan.md §9.2b table drift. Four independent small items; slot in whenever, but do before or alongside 6b/35 per §27.3. |
| 6m | **41** | §28 — findings from implementing 36: 🔴 **~150-file uncommitted working tree since 2026-08-20 — human judgment required, not an autonomous commit** (read STEP 1's framing before touching git); plus a small, safe rename of the misleading `rag_identity_cache_setup.sql` filename. Slot in whenever; STEP 1 is the more urgent half but is a decision, not code. |
| 6n | **42** | §29 — findings from implementing 25: 🔴 **STEP 6 (`/api/user/preferences` UUID-type mismatch) is a live, currently-broken production bug, unrelated to §12 — do this first regardless of the rest of this row.** STEP 7 (apply the `answer_feedback` migration) is a hard prerequisite for the new feedback loop to work at all in production. STEP 2 (vercel.json rewrites contradiction) was blocked on Prompt 24's live preview-deploy landing — that landed 2026-08-21, so STEP 2 is unblocked now, but the discrepancy itself (no `rewrites` key in the live file) was not specifically re-checked during that deploy and is still open. STEPS 1/3/4/5 are independent small items; slot in whenever. |
| 6o | **43** | §31 — findings from implementing 29a: 🔴 **STEP 1 (fail-open/fail-closed posture has zero test coverage) is the one with real risk — an untested security-critical branch in `backend/rate_limit.py`, do before or alongside 6b.** STEP 2 (confirm `RATE_LIMIT_REDIS_URL` is set in production) is an operator action, time-sensitive since D3 stays open in effect until it's answered. STEP 3 (DECISIONS.md currency) is optional, pending a convention confirmation. |
| 6c | **31** | ✅ **Decided 2026-08-15 (final) — Option 2 (defer, keep `'unsafe-inline'`). Nothing left to run.** CSP nonce hardening — Prompts 5/8/9/10 done as of 2026-08-01; Prompt 11's CSP item closed against this decision, see its status-table row. |
| 6d | **32** | 🟡 §19 frontend inline/module duplication — **not started, unblocked and ready.** Phase 0 jumps the queue (small, disarms a live semantic-bookmark double-POST tripwire); Phases 1–3 are disjoint from the §4 backend track and can run in parallel with it; Phase 4+ was previously waiting on Prompt 31's CSP decision — that decision is now final (row 6c), so nothing blocks Phase 4+ either. Verified 2026-08-26: zero phases have landed. |
| 7 | 11 → 9 → 10 → 8 → 5 | Frontend (11 first: CSP/Tailwind unblocks 9's tokens) — **superseded 2026-08-01: 9/10/8/5 done, 11 partial pending Prompt 31** |
| 8 | 6 → **6b** → 17 → **29c** → 15 → 16 → 18 | Ops/security/privacy (6b must follow 6; 29c brings identity quotas + Turnstile + mitigation logging). **29c done 2026-08-24** — see its status-table row above and Prompt 47/`plan.md` §35 for a severe, unrelated production bug (authenticated `/ask` always 500s) it surfaced. |
| 9 | **28** | ✅ **Done 2026-08-22.** Vercel cache tiers — the main cost win, shipped (needs §4 done ✅). Baseline capture and preview-deploy verification still pending, operator/deploy-only — see Prompt 45/§33.4. |
| 10 | 25 | ✅ **DONE 2026-08-21.** Phase 6 product surface — search filter UI closed as descoped (not built, not reopened). See `plan.md` §12 status note and Prompt 42/§29 for what else is still open (vercel.json rewrites contradiction, parasha page) and what was found (a live `/api/user/preferences` bug, the unapplied `answer_feedback` migration). |
| 11 | **26** | ✅ **DONE 2026-08-22.** Phase 7 Sonar debt — 3 of 51 complexity hotspots refactored and individually committed (worst-first: 2 E-grade, 1 D-grade); both modals confirmed already native `<dialog>`, a duplicate Escape-handler double-fire found and fixed (verified in-browser, not yet committed — see Prompt 44/§32.2). See `plan.md`'s §13 status note and Prompt 44/§32 for the 48-function backlog. |
| 12 | 21 → 22 → 19 | Docs/licensing/launch gate |
| 13 | 23 | ✅ **DONE 2026-08-21** — `github.com/akivayevdayev-debug/akivayevdayev-debug` created and pushed (item 1). Items 2-4 (pin repos, Sh'elah repo polish, bio/links) closed as descoped, not pursued. |
| 14 | **44** | §32 — findings from implementing 26/27: 🔴 **STEP 1 (commit the `templates/index.html` dialog fix + review the rest of that file's ~1,900-line pending diff) is the most time-sensitive — real, verified work currently sitting only in a local working tree.** STEP 2 (continue the 48-function complexity backlog, worst-first) and STEP 3 (defer `app.py`'s `supabase` import) are real, no-urgency backlog. STEP 4 (`calendarModal` backdrop-click parity) needs an operator decision before any code change. STEPS 5-6 are documentation/awareness only. |
| 15 | **45** | ✅ **Done as scoped 2026-08-23 — STEPS 1/2/3/4/6/7 closed, STEP 5 still operator-only.** §33 — findings from implementing 28/29b. STEP 1: blessed — `plan.md` §16.3-L3 corrected to name `backend/rate_limit.py`'s shared store. STEP 2: decided — §20c Option A recorded (`plan.md` §20.2, `.env.example`, `docs/SECURITY.md` §5, Prompt 33c row below). STEP 3: verified against current Vercel docs — the cache-purge-on-deploy assumption holds in effect (deployment-scoped cache keys, not an active purge); corrected `X-Deploy-Hash` to diagnostic-only in `plan.md` §33.2 and `docs/VERCEL_COST_OPTIMIZATION.md`. STEP 4: decided-to-defer — Retry-After countdown not built (no real-usage signal yet, breaker has never tripped in prod); rationale in `plan.md` §33.6. STEP 6: `DAILY_BUDGET_USD` confirmed still empty locally; **production value cannot be verified from this headless session — operator action, see `plan.md` §33.4.** STEP 7: §33.3/§33.7 reconfirmed accurate, no drift. **STEP 5 (WAF dashboard entry, baseline/preview-deploy verification) remains genuinely blocked on dashboard access this session doesn't have.** New findings from this pass — including the still-uncommitted ~180-file working tree, now larger than Prompt 41 flagged it at — filed as Prompt 46/§34 below. |
| 16 | **46** | §34 — findings from implementing 45: 🔴 **STEP 1 is the same uncommitted-working-tree finding Prompt 41/§28.1 raised on 2026-08-20, now larger (181+ paths) and unresolved across three prompts — human judgment required, not an autonomous commit; do this first regardless of the rest of this row.** STEP 2 (refresh `docs/SECURITY.md` §5's stale pre-unification rate-limiter description) is small, independent, no urgency. |
| 17 | **47** | ✅ **Done 2026-08-25.** §35 — findings from implementing 29c: every authenticated `/ask` call previously returned 500; fixed by threading an explicit `bearer_token`/`authorization` parameter down the full chain instead of reading Flask's global `request` unconditionally. Full suite 1642 passed, 0 failed. See `plan.md` §35.1/§35.2 and Prompt 48/§36 below for a test-infrastructure finding this pass's verification surfaced. |
| 18 | **48** | ✅ **Done 2026-08-26, commit `9a7c159`.** §36 — findings from implementing 47: `tests/conftest.py` never blanked `RATE_LIMIT_REDIS_URL`, so a developer's real `.env` credential leaked into test runs. Fixed exactly as specified (one `setdefault` line mirroring the existing `SENTRY_DSN` pattern); found necessary while closing row 0c/Prompt 49, landed alongside it. Verified: full suite green with a real `RATE_LIMIT_REDIS_URL` in the shell, no more `RuntimeError: Event loop is closed`. |
| 0c | **49** | ✅ **Done 2026-08-26.** §37 — `main` now imports. STEP 1 (the rate-limiter unification's fifth `ImportError`) landed as commit `a519a21` by a prior session; this pass verified that against a clean-room export and found `main` *still* didn't import, closing three further blockers as three separate commits: `7cf767c` (zoneinfo migration), `f81ace6` (cost-breaker cron function + full §20a/§20b surface), `9a7c159` (four untracked blueprints + templates). `python -c "import app"`/`"import asgi"` both exit 0 against current `main` for the first time. Two findings from this pass — legal-page cross-linking content (deliberately deferred) and a test-isolation bug — filed as **Prompt 50/§38** below. |
| 19 | **50** | 🟡 §38 — findings from closing 49/§37.2: two low-severity, non-blocking items. STEP 1 — the working tree's `terms.html`/`privacy.html`/`accessibility.html`/`index.html`/shared-component diffs add real cross-links between all 7 legal pages (600-1900 lines each, possibly bundled with an unrelated topbar visual refresh — review before committing); 3 of 7 `TestLegalPagesCrossLinking` cases currently fail without it. STEP 2 — `tests/test_cost_meter_call_sites.py` (untracked) has an order-dependent test-isolation bug: passes in the full suite, fails run alone, because its `_configure_gemini_client` monkeypatch bypasses a lazy-SDK-load side effect. Neither step depends on the other; slot in whenever. |
| 20 | **51** | 🔴 §39 — account-deletion completeness audit, requested directly (not surfaced as a byproduct). Isolation already verified correct by construction, no fix needed. **STEP 1 (`answer_feedback` excluded from both export and delete) is a real, currently-orphaned personal-data table — small fix.** **STEP 2 (no cleanup if a Clerk identity is ever deleted outside this app's own endpoint) is the urgent one — a normal admin action (deleting an abusive user from the Clerk Dashboard) or an undocumented Clerk UI setting can silently and permanently orphan a user's entire data footprint with no retry path; fix is a Svix-verified `user.deleted` webhook as a completeness backstop.** STEP 3 is an operator dashboard check. STEP 4 (backup-retention documentation, hashing `user_id` before it reaches Sentry) is lower-urgency — and explicitly recommends *against* building crypto-shredding/field-level encryption for this, see `plan.md` §39.3 for why. |

---

### Last — one pending commit, not a numbered prompt

**✅ Resolved 2026-08-26 — folded into the Prompt 35 commit.** `main` at `4042a8a` was internally inconsistent as committed: `app.py` calls `ask_pipeline.run_agentic_ask(...)`, but `backend/ask_pipeline.py` at that same commit only defined the old `run_ask_pipeline()`/`AskPipelineResult` — `run_agentic_ask` didn't exist yet in git history. The uncommitted working-tree state described below (found by a background session spun off from a checkout of this same broken commit) turned out to be exactly Prompt 35 STEP 1's deletion plus most of STEP 3/4, already sitting in the tree from an earlier unfinished pass — this session's Prompt 35 execution picked up from that same state, finished STEP 2 (the parity suite) on top of it, and committed all four steps together. `main` now imports consistently. Kept below as the historical record of how the inconsistency was found.

The fix already existed, uncommitted, in the primary working tree — this was essentially Prompt 35's STEP 1 (and likely STEP 3/STEP 4) already done by an earlier, unfinished session, just never landed:

- `backend/ask_pipeline.py` — dead code (`run_ask_pipeline`/`AskPipelineResult`/`_flatten_sources_for_ai`/`_safe_list`) already removed; `run_agentic_ask()`/`_tool_result_is_insufficient()` already present. 653-line diff, unstaged.
- `tests/test_ask_pipeline.py`, `tests/test_ask_pipeline_smoke.py` — already `git rm`'d and **staged** for deletion.

Verified via `git grep` across HEAD that nothing else in the repo depends on the still-uncommitted `backend/customs.py` (138-line diff, unstaged, likely STEP 4's decide-or-delete) or `.agents/ENGINEERING_RULES.md` (25-line diff, unstaged, likely STEP 3's named-cost rule) — those are safe to leave exactly as they are for now; only `ask_pipeline.py` is required to make `main` consistent again.

**Action taken:** rather than a separate small fix, this state was picked up as the starting point for a full Prompt 35 run (2026-08-26) — `backend/ask_pipeline.py`, the two test-file deletions, `backend/customs.py`'s docstring (STEP 4), and `.agents/ENGINEERING_RULES.md` (STEP 3) were all completed and committed together, plus the new `tests/test_ask_transport_parity.py` (STEP 2) this note's snapshot predates.
